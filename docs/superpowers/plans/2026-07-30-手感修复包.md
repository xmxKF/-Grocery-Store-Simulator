# 手感修复包（子项目 A）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修好收银台被钉死的视角、把货架挡板换成真价签、给上架与扫码补上平滑过渡动画。

**Architecture:** 三块改动都落在现有模块内，不新增文件。收银台引入由传送带几何反推的「站位（stance）」抽象，玩家进出收银台时显式保存/还原位姿；货架把「视觉」与「射线靶子」拆成两个物体（可见价签 + 不可见命中盒）；上架动画需要先把逐次全量重建的格位渲染改成增量更新，否则飞行中的临时 mesh 会被冲掉。

**Tech Stack:** Three.js r128 UMD（全局 `THREE`）、原生 JavaScript ES5 风格、无构建步骤、无 npm 依赖。

**依据文档：** `docs/superpowers/specs/2026-07-30-手感修复包-design.md`（下称 spec）

---

## Global Constraints

这些约束适用于**每一个** task，不再逐条重复：

- **Three.js r128 UMD**，经典 `<script>` 标签，全局 `THREE`。**r128 无 `CapsuleGeometry`**。
- **无构建步骤、无 ES modules、运行时零网络请求**。双击 `index.html`（`file://`）必须能玩。
- 每个 js 文件用 **IIFE + `'use strict'`**，只向全局命名空间 `window.G` 挂自己的模块。
- 代码风格用 **ES5**（`var`、`function`），与现有代码一致。不要引入 `let`/`const`/箭头函数/模板字符串。
- 游戏内 UI 文本一律**简体中文**。存档 key 固定 `localStorage['gss-save-v1']`。
- 圆角、间距、时长、配色一律以 `DESIGN.md` 为准。过渡时长只用 `120ms` / `180ms` / `300ms` 三档（本计划新增的 3D 位移动画按 spec §2.2 修订后的规定）。
- **不留 TODO / 占位符。** 每个文件改完必须通过 `node --check`。
- 不引入 `CONTRACTS.md` 之外的跨模块调用。本计划确实新增了接口，但都已在 Task 1 里先写进 `CONTRACTS.md`。

### 关于验证命令的重要限制

**本项目没有常规测试框架。** 测试 = 往 `js/main.js` 的 `runSelftest()` 里加 `ck(name, ok, detail)` 断言，然后跑无头 Edge 看 `document.title`。

**已实测确认：Claude Code 的工具会话无法启动 Edge**（直接调用 / `Start-Process` / `cmd /c` 三种方式均返回 Windows 错误「无法在此会话中打开」，这不是交互式桌面会话）。因此：

| 检查 | 谁来跑 |
|---|---|
| `node --check <file>` | **agent 可自动跑** |
| `?selftest=1` 无头自测 | **必须由用户跑** |

用户执行方式：在 Claude Code 输入框打 `!` 前缀，输出会直接进入对话：

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern '"name"|"ok"|SELFTEST'
```

期望：`document.title` 为 `SELFTEST:PASS`，且 `#selftest` 里所有 check 的 `ok` 均为 `true`。

**每个 task 的「验证测试失败」与「验证测试通过」两步都需要向用户请求执行。** 实施者在这两步必须停下来等用户回报结果，不得自行宣称通过。

### 基线

本计划开工前已确认：9 个 js 文件全部 `node --check` 通过；`git status` 干净；`main` 与 `origin/main` 同步（远端 `github.com/xmxKF/-Grocery-Store-Simulator`）。

---

## File Structure

不新增文件。改动分布：

| 文件 | 责任 | 本计划中的改动 |
|---|---|---|
| `DESIGN.md` | 视觉唯一权威 | §5 允许 CanvasTexture 文字贴图；§6 重写上架动画规定 |
| `docs/CONTRACTS.md` | 模块边界与接口 | 新增 `G.world.updateSlotTag`、`G.world.addItem` 第三参、`G.player.getPose/setPose/camera`、`G.checkout.stance` |
| `js/world.js` | 店铺几何、格位系统、可交互物 | 格位渲染增量化、价签与命中盒、上架飞行动画 |
| `js/player.js` | 第一人称控制器 | 暴露位姿接口、上架时传入飞行起点、移交收银模式的位姿还原 |
| `js/checkout.js` | 收银流程与 POS | 站位系统取代 `startCamLock`、滑动改 ease-out、移除 `G.player.init` 的 monkey-patch |
| `js/main.js` | 集成与自测 | 新增自测断言 |

**为什么不拆新文件**：三块改动分别是既有模块的内部行为修正，各自代码量在 50–120 行之间，拆出去反而制造新的跨模块耦合。`world.js` 改完约 700 行，仍在可维护范围；真正需要拆分的是子项目 C 扩张之后的 `world.js`，届时按「建筑 / 格位 / 导航」拆才有意义。

**Task 顺序是硬依赖，不可乱序**：Task 2 是 Task 5 的前置（不增量化则飞行 mesh 被冲掉）；Task 3 是 Task 4 的前置（不先建出价签物体则无处渲染贴图）。

---

## Task 1：文档修订（DESIGN.md + CONTRACTS.md）

先改权威文档再改代码，这是 `CLAUDE.md` 的硬性要求。本 task 不含代码，因而没有自测断言。

**Files:**
- Modify: `DESIGN.md`（§5 材质、§6 交互反馈）
- Modify: `docs/CONTRACTS.md`（`G.world` / `G.player` / `G.checkout` 接口段）

**Interfaces:**
- Consumes: 无
- Produces: 后续所有 task 的接口签名依据

- [ ] **Step 1: 修订 DESIGN.md §5 材质**

把这一行：

```
- **材质**：统一 `MeshLambertMaterial`，`flatShading: true`，无金属/粗糙度参数。半透明仅用于冷藏柜玻璃（`transparent, opacity .35`）。
```

改为：

```
- **材质**：统一 `MeshLambertMaterial`，`flatShading: true`，无金属/粗糙度参数。半透明用于冷藏柜玻璃（`transparent, opacity .35`）与货架格命中盒（`transparent, opacity 0`，纯交互用、不可见）。
```

并把 §5 开头「**几何**」条目里的这一句：

```
禁止贴图与法线贴图，全部纯色。
```

改为：

```
禁止外部图片素材与法线贴图。允许 `CanvasTexture` 在运行时生成的纯文字 / 标识贴图，仅用于价签一类信息载体；其余表面仍为纯色。此例外不引入任何外部文件，运行时仍为零网络请求。
```

- [ ] **Step 2: 修订 DESIGN.md §6 交互反馈**

把这一行：

```
- **上架成功**：商品方块以 90ms 从 0.8 倍缩放到 1.0 倍出现。
```

改为：

```
- **上架成功**：商品自纸箱位置直线飞向目标格位，180ms `ease-out`；落位后接 90ms 由 0.8 倍缩放到 1.0 倍。
```

并在该节「所有过渡时长只用 `120ms`…」那一行之前，新增一行：

```
- **所有 3D 位移动画统一使用 `ease-out` 缓动**（`f(t) = 1 - (1-t)³`）。
```

- [ ] **Step 3: 修订 CONTRACTS.md 的 `G.world` 段**

在 `G.world` 代码块里，把 `addItem` 那一行改为并新增一行：

```js
G.world.addItem(slot, pid, fromPos /*可选 THREE.Vector3，飞行动画起点；省略则无飞行*/) /*->bool*/
G.world.updateSlotTag(slot)   // 幂等；按 slot 当前 productId/count 与 G.state.prices 重绘价签贴图
```

- [ ] **Step 4: 修订 CONTRACTS.md 的 `G.player` 段**

在 `G.player` 代码块里新增：

```js
G.player.camera                 // 只读；init() 时记下的相机引用
G.player.getPose()              // -> {x, z, yaw, pitch}（纯数值快照，非引用）
G.player.setPose(pose)          // 设定玩家位姿并立即同步到相机；收银台进出用
```

并在该段说明文字里补一句：

```
- `G.checkout.inRegister` 为真时 player 完全让出相机控制权，位姿由 checkout 显式还原（不再由 player 从相机反推）。
```

- [ ] **Step 5: 修订 CONTRACTS.md 的 `G.checkout` 段**

在 `G.checkout` 代码块里新增：

```js
G.checkout.stance   // null | {pos: THREE.Vector3, yaw: Number, pitch: Number}（只读，供自测断言）
```

- [ ] **Step 6: 在 CONTRACTS.md「补充事件与职责裁定」段末尾追加职责裁定**

```
- **上架飞行动画由 `world.js` 自行在内部 `requestAnimationFrame` 中驱动**，不接入 `main.js` 主循环的 dt。理由：与现有 `popInItem` 的驱动方式一致；飞行是纯视觉表现，与游戏逻辑解耦——`addItem` 返回时商品在逻辑上已在格内。
- **价签贴图按 `(productId, price, state)` 三元组缓存 `CanvasTexture` 并复用；但每个价签持有各自的 `Material` 实例**，否则准星高亮会让同商品的所有价签一起发光。
```

- [ ] **Step 7: 检查改动并提交**

```bash
git -C "C:/Users/quito/超市模拟器" diff --stat
git -C "C:/Users/quito/超市模拟器" add DESIGN.md docs/CONTRACTS.md
git -C "C:/Users/quito/超市模拟器" commit -m "docs: 手感修复包的规范修订（价签贴图例外、上架飞行动画、位姿接口）"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 2：格位渲染增量化

Task 5 的前置。现状 `world.js:306 updateSlotVisual` 每次调用都销毁重建该格全部商品方块（且每个都 `new THREE.BoxGeometry`），飞行动画一旦引入会被它冲掉。

**Files:**
- Modify: `js/world.js:306-328`（`updateSlotVisual`）
- Modify: `js/world.js`（模块常量区，约第 20 行后）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: 无
- Produces: `itemLocalPos(i, faceZ) -> THREE.Vector3`（Task 5 用它算飞行终点）；`slot._visPid`（内部字段，记录当前已渲染的商品 id）

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 `ck('world.emptyBox', ...)` 那一行，在它**之后**插入：

```js
      /* --- 格位渲染增量化（Task 2）--- */
      var incSlot = G.world.findSlotWithProduct('f_noodle');
      var incBefore = incSlot ? incSlot.itemGroup.children.length : -1;
      var incGeo = (incSlot && incSlot.itemGroup.children[0]) ? incSlot.itemGroup.children[0].geometry : null;
      if (incSlot) { G.world.removeItem(incSlot); G.world.removeItem(incSlot); }
      var incAfter = incSlot ? incSlot.itemGroup.children.length : -1;
      var incGeoSame = !!(incGeo && incSlot && incSlot.itemGroup.children[0] &&
        incSlot.itemGroup.children[0].geometry === incGeo);
      if (incSlot) { G.world.addItem(incSlot, 'f_noodle'); G.world.addItem(incSlot, 'f_noodle'); }
      ck('world.slotIncremental', incBefore > 2 && incAfter === incBefore - 2 &&
        incSlot.itemGroup.children.length === incBefore,
        '增删前 ' + incBefore + ' → 删2后 ' + incAfter + ' → 补2后 ' + (incSlot ? incSlot.itemGroup.children.length : -1));
      ck('world.slotSharedGeo', incGeoSame, '格位商品方块必须复用同一 BoxGeometry 实例');
```

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

请用户执行：

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|slotIncremental|slotSharedGeo' -Context 0,2
```

期望：`SELFTEST:FAIL`，且 `world.slotSharedGeo` 的 `ok` 为 `false`（现状每次 `new BoxGeometry`，实例不相同）。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 加入共享几何体与材质缓存**

在 `js/world.js` 模块常量区（`var SLOT_HEIGHTS = [0.9, 1.4];` 之后）新增：

```js
  // 格位商品方块：几何体与材质全局共享（每格最多 16 个 × 最多 48 格，绝不逐个 new）
  var ITEM_GEO = null;
  var ITEM_MATS = {};

  function itemGeo() {
    if (!ITEM_GEO) ITEM_GEO = new THREE.BoxGeometry(0.16, 0.22, 0.16);
    return ITEM_GEO;
  }

  function itemMat(color) {
    var k = String(color);
    if (!ITEM_MATS[k]) ITEM_MATS[k] = flatMat(color);
    return ITEM_MATS[k];
  }

  /* 每行 4 个、最多 4 行（DESIGN §5）：列距 0.14 使一格的堆宽 0.58 < 格间距 0.6；
     行沿格位深度向货架内侧排（层高只有 0.5m，竖着堆会穿过上层隔板） */
  function itemLocalPos(i, faceZ) {
    var col = i % 4, row = Math.floor(i / 4);
    var depth = -(faceZ || 1);
    return new THREE.Vector3((col - 1.5) * 0.14, 0.11, depth * (0.16 + row * 0.16));
  }
```

- [ ] **Step 4: 把 `updateSlotVisual` 改成增量更新**

把 `js/world.js:306-328` 整个 `updateSlotVisual` 函数替换为：

```js
  function updateSlotVisual(slot) {
    var group = slot.itemGroup;
    var want = (!slot.productId || slot.count <= 0) ? 0 : Math.min(slot.count, 16);

    // 商品种类变了，整格重来；否则只增删末尾
    if (slot._visPid !== slot.productId) {
      while (group.children.length) group.remove(group.children[group.children.length - 1]);
      slot._visPid = slot.productId;
    }
    while (group.children.length > want) group.remove(group.children[group.children.length - 1]);
    if (want === 0) return;

    var product = G.data.productById(slot.productId);
    if (!product) return;
    while (group.children.length < want) {
      var i = group.children.length;
      var cube = new THREE.Mesh(itemGeo(), itemMat(product.color));
      cube.position.copy(itemLocalPos(i, slot.faceZ));
      group.add(cube);
    }
  }
```

**关键：不再调用 `geometry.dispose()` / `material.dispose()`。** 几何体与材质现在是全局共享的，dispose 会连带废掉其它格位。移除 mesh 只从 group 摘除即可。

- [ ] **Step 5: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/world.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

期望：无输出（通过）。

- [ ] **Step 6: 请用户运行自测，确认全部通过**

同 Step 2 的命令。期望：`SELFTEST:PASS`，`world.slotIncremental` 与 `world.slotSharedGeo` 均为 `true`，且**原有 24 条断言无一回归**（特别注意 `world.stock`、`save.shelves`）。

**停下等用户回报结果再继续。**

- [ ] **Step 7: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/world.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "refactor(world): 格位商品渲染改为增量更新，几何体与材质全局共享"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 3：价签物体与不可见命中盒

删掉挡板 `marker`，换成一个 `THREE.Group`，内含「不可见命中盒」（射线靶子）与「可见价签条」（视觉 + 高亮载体）。本 task 只建几何体，**贴图留到 Task 4**——价签先用纯色，以便本 task 独立可验。

**Files:**
- Modify: `js/world.js:260-275`（`buildRack` 内的格位循环）
- Modify: `js/world.js`（模块常量区）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: Task 2 的 `itemLocalPos`（不直接用，但同一常量区）
- Produces: `slot.tagMesh`（价签 Mesh，Task 4 往它贴贴图）、`slot.hitMesh`（不可见命中盒）、`slot.marker`（改为指向外层 Group，保持字段名以免波及 `serializeShelves` 等既有代码）

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 `ck('world.slotShape', ...)` 那一行，在它**之后**插入：

```js
      /* --- 价签与命中盒（Task 3）--- */
      var tagOk = true, hitOk = true, tagDetail = '';
      for (var si = 0; si < G.world.slots.length; si++) {
        var sl = G.world.slots[si];
        if (!sl.tagMesh || !sl.tagMesh.isMesh) { tagOk = false; tagDetail = sl.id + ' 缺价签'; break; }
        if (!sl.hitMesh || !sl.hitMesh.isMesh) { hitOk = false; tagDetail = sl.id + ' 缺命中盒'; break; }
        if (sl.hitMesh.material.opacity !== 0) { hitOk = false; tagDetail = sl.id + ' 命中盒必须 opacity 0'; break; }
        if (sl.tagMesh.material === sl.hitMesh.material) { tagOk = false; tagDetail = sl.id + ' 价签与命中盒不得共用材质'; break; }
      }
      ck('world.slotTagMesh', tagOk, tagDetail || '全部格位均有独立价签');
      ck('world.slotHitBox', hitOk, tagDetail || '全部格位均有 opacity 0 命中盒');
      // 价签材质必须逐格独立，否则高亮一个会让同类全亮
      var tagMatShared = G.world.slots.length > 1 &&
        G.world.slots[0].tagMesh.material === G.world.slots[1].tagMesh.material;
      ck('world.slotTagMatUnique', !tagMatShared, '相邻两格价签共用了材质' );
```

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|slotTagMesh|slotHitBox|slotTagMatUnique' -Context 0,2
```

期望：`SELFTEST:FAIL`，三条新断言全部 `ok: false`（现在只有 `marker` 挡板，没有 `tagMesh` / `hitMesh`）。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 加入价签与命中盒的共享几何体**

在 `js/world.js` 常量区（Task 2 加的那段之后）新增：

```js
  // 价签条：宽同原挡板 0.55，高 0.06，厚 0.04（与层板等厚，见 buildRack 的 board）
  // 命中盒：占据该格商品应在的那块空气；高 0.46 而非 0.5，给上下两格留 0.04 空隙避免拾取歧义
  var TAG_GEO = null, HIT_GEO = null;
  function tagGeo() {
    if (!TAG_GEO) TAG_GEO = new THREE.BoxGeometry(0.55, 0.06, 0.04);
    return TAG_GEO;
  }
  function hitGeo() {
    if (!HIT_GEO) HIT_GEO = new THREE.BoxGeometry(0.55, 0.46, 0.5);
    return HIT_GEO;
  }
```

- [ ] **Step 4: 替换 `buildRack` 里的格位循环**

把 `js/world.js:260-275`（`SLOT_HEIGHTS.forEach(function (y, rowIdx) { ... });` 整块）替换为：

```js
    SLOT_HEIGHTS.forEach(function (y, rowIdx) {
      SLOT_LOCAL_X.forEach(function (lx, ci) {
        var id = idPrefix + '_' + rowIdx + '_' + ci;
        var pos = new THREE.Vector3(centerX + lx, y, frontZ);

        var holder = new THREE.Group();

        // 不可见命中盒：射线靶子。用 opacity 0 而非 visible=false，确保必定可被 raycast 命中。
        var hit = new THREE.Mesh(hitGeo(), flatMat(0xFFFFFF, {
          transparent: true, opacity: 0, depthWrite: false
        }));
        hit.position.set(pos.x, y + 0.23, frontZ - facing * 0.25);
        holder.add(hit);

        // 可见价签：贴在该格下方层板（y-0.04）的前缘；同时是准星高亮的载体
        var tag = new THREE.Mesh(tagGeo(), flatMat(0x8C9AA6));
        tag.position.set(pos.x, y - 0.055, frontZ + facing * 0.02);
        holder.add(tag);

        sceneRef.add(holder);

        var itemGroup = new THREE.Group();
        itemGroup.position.copy(pos);
        sceneRef.add(itemGroup);

        var slot = {
          id: id, pos: pos, productId: null, count: 0, fridge: !!isFridge,
          marker: holder, tagMesh: tag, hitMesh: hit,
          itemGroup: itemGroup, aisleSpot: aisleSpot, faceZ: facing
        };
        groupSlots.push(slot);
        allSlots.push(slot);
        registerInteractable(holder, { type: 'shelfSlot', data: { slot: slot }, prompt: '' });
      });
    });
```

要点说明（写给实施者）：

- `registerInteractable` 注册的是 **Group**，这在本项目已有先例——`spawnBox`（`world.js:438`）注册的就是 Group，`player.js:274` 的射线检测与 `userData.interactable` 回溯都支持。
- `slot.marker` 字段名保留，指向外层 Group。这样任何遗留的 `slot.marker` 引用不会变成 `undefined`。
- `highlight()`（`player.js:371`）会 `traverse` 整个 Group 并给**所有含 `emissive` 的材质**上高亮色。命中盒 opacity 0 看不见，价签会发光——正是我们要的效果。
- `flatMat` 每次调用都 `new` 一个新材质，所以价签材质天然逐格独立，满足 `world.slotTagMatUnique`。

- [ ] **Step 5: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/world.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

- [ ] **Step 6: 请用户运行自测，确认全部通过**

同 Step 2 的命令。期望 `SELFTEST:PASS`。**特别关注 `world.slotShape` 不得回归**——它断言 `G.world.slots.indexOf(slotEntries[0].data.slot) !== -1`，改成 Group 后 `data.slot` 仍是同一个 slot 对象，应当照常通过。

**停下等用户回报结果再继续。**

- [ ] **Step 7: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/world.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "feat(world): 货架挡板换成价签条 + 不可见命中盒"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 4：价签文字贴图与三状态

给 Task 3 建出的价签贴上 `CanvasTexture` 文字，并实现三状态与刷新时机。

**Files:**
- Modify: `js/world.js`（新增 `updateSlotTag` 及贴图缓存；在 `addItem` / `removeItem` / `restoreShelves` 中调用）
- Modify: `js/shop.js`（`setPrice` 后刷新受影响格位的价签）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: Task 3 的 `slot.tagMesh`
- Produces: `G.world.updateSlotTag(slot)`；`slot.tagState` —— 取值为 `'stocked'` / `'out'` / `'empty'` 三者之一的字符串，供断言与调试

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 Task 2 插入的 `ck('world.slotSharedGeo', ...)` 那一行，在它**之后**插入：

```js
      /* --- 价签三状态（Task 4）--- */
      var stockedSlot = G.world.findSlotWithProduct('f_noodle');
      var emptySlot = null;
      for (var ei = 0; ei < G.world.slots.length; ei++) {
        if (G.world.slots[ei].productId === null) { emptySlot = G.world.slots[ei]; break; }
      }
      ck('world.tagStateStocked', !!stockedSlot && stockedSlot.tagState === 'stocked',
        '有货格 tagState = ' + (stockedSlot && stockedSlot.tagState));
      ck('world.tagStateEmpty', !!emptySlot && emptySlot.tagState === 'empty',
        '空格 tagState = ' + (emptySlot && emptySlot.tagState));

      // 取空一格 → 应变 'out'（缺货）；再补回去 → 变回 'stocked'
      var outSlot = G.world.findSlotWithProduct('d_water');
      var outGuard = 0;
      while (outSlot && outSlot.count > 0 && outGuard++ < 40) G.world.removeItem(outSlot);
      ck('world.tagStateOut', !!outSlot && outSlot.tagState === 'out',
        '取空后 tagState = ' + (outSlot && outSlot.tagState));
      if (outSlot) G.world.addItem(outSlot, 'd_water');
      ck('world.tagStateBack', !!outSlot && outSlot.tagState === 'stocked',
        '补货后 tagState = ' + (outSlot && outSlot.tagState));

      // 改价必须刷新价签贴图（换了 texture 实例即视为已刷新）
      var texBefore = stockedSlot ? stockedSlot.tagMesh.material.map : null;
      G.shop.setPrice('f_noodle', 3.7);
      ck('world.tagPriceRefresh', !!texBefore && stockedSlot.tagMesh.material.map !== texBefore,
        '改价后价签贴图未刷新');
      G.shop.setPrice('f_noodle', 2.2);   // 还原，后续断言依赖 r=1.0 必买
```

**注意**：`removeItem` 在 `count` 归零时会把 `slot.productId` 置为 `null`（`world.js:384`）。这会让「缺货」状态无从区分。因此本 task 必须同时调整 `removeItem` 的语义——见 Step 4。

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|tagState|tagPriceRefresh' -Context 0,2
```

期望：`SELFTEST:FAIL`，五条新断言全部 `ok: false`。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 实现价签贴图生成与缓存**

在 `js/world.js` 的 `tagGeo/hitGeo` 之后新增：

```js
  // 价签贴图缓存：键为 (state|pid|price)，texture 跨格复用；材质仍逐格独立（见 CONTRACTS 职责裁定）
  var TAG_TEX = {};
  var TAG_BG = { stocked: null, out: '#E8B54C', empty: '#8C9AA6' };   // stocked 用类目色，运行时填

  function tagTexture(state, product, price) {
    var key = state + '|' + (product ? product.id : '-') + '|' + (price == null ? '-' : price);
    if (TAG_TEX[key]) return TAG_TEX[key];

    var W = 256, H = 28;
    var cv = document.createElement('canvas');
    cv.width = W; cv.height = H;
    var g = cv.getContext('2d');

    var bg = TAG_BG[state] || '#8C9AA6';
    if (state === 'stocked' && product) bg = '#' + ('000000' + (CAT_COLORS[product.cat] || 0x8C9AA6).toString(16)).slice(-6);
    g.fillStyle = bg;
    g.fillRect(0, 0, W, H);

    g.fillStyle = '#12161B';
    g.textBaseline = 'middle';
    if (state === 'empty') {
      g.font = '600 15px "Microsoft YaHei","PingFang SC",sans-serif';
      g.fillText('空', 8, H / 2);
    } else {
      g.font = '600 15px "Microsoft YaHei","PingFang SC",sans-serif';
      g.fillText(product ? product.name : '', 8, H / 2);
      var right = (state === 'out') ? '缺货' : ('¥' + price.toFixed(1));
      g.textAlign = 'right';
      g.fillText(right, W - 8, H / 2);
      g.textAlign = 'left';
    }

    var tex = new THREE.CanvasTexture(cv);
    tex.needsUpdate = true;
    TAG_TEX[key] = tex;
    return tex;
  }

  function updateSlotTag(slot) {
    if (!slot || !slot.tagMesh) return;
    var product = slot.productId ? G.data.productById(slot.productId) : null;
    var state = !product ? 'empty' : (slot.count > 0 ? 'stocked' : 'out');
    var price = product ? ((G.state && G.state.prices && G.state.prices[product.id]) || product.market) : null;

    slot.tagState = state;
    slot.tagMesh.material.map = tagTexture(state, product, price);
    slot.tagMesh.material.color.setHex(0xFFFFFF);   // 贴图自带底色，材质色需为白，否则相乘变暗
    slot.tagMesh.material.needsUpdate = true;
  }
```

- [ ] **Step 4: 让空格与缺货可区分，并在增删货后刷新价签**

把 `js/world.js` 的 `addItem` 与 `removeItem` 改为（替换 `world.js:366-387` 两个函数）：

```js
  function addItem(slot, pid, fromPos) {
    if (!slot) return false;
    var product = G.data.productById(pid);
    if (!product) return false;
    if (slot.fridge && product.cat !== '生鲜') return false;
    if (!slot.fridge && product.cat === '生鲜') return false;
    if (slot.productId && slot.productId !== pid) return false;
    if (slot.productId === pid && slot.count >= product.slotCap) return false;
    slot.productId = pid;
    slot.count += 1;
    updateSlotVisual(slot);
    updateSlotTag(slot);
    if (slot.count <= slot.itemGroup.children.length) popInItem(slot.itemGroup.children[slot.count - 1]);
    return true;
  }

  function removeItem(slot) {
    if (!slot || !slot.productId || slot.count <= 0) return false;
    slot.count -= 1;
    // count 归零后保留 productId，价签才能显示「缺货」而非「空」；
    // findEmptyOrMatchingSlot 视 count===0 的格为可用（见该函数的 slot.productId === pid 分支 + count < slotCap）
    updateSlotVisual(slot);
    updateSlotTag(slot);
    return true;
  }
```

**这一处语义改动必须连带检查两个既有调用点：**

1. `findEmptyOrMatchingSlot`（`world.js:349-355`）：原来靠 `slot.productId === null` 找空格。现在取空的格 `productId` 不再归 `null`，会导致该格无法再放**别的**商品。修法——在该函数的第一个分支之后、第二个分支之前插入一行，把「有 pid 但 count 为 0」也视作可复用的空格：

```js
        if (slot.productId === null) return slot;
        if (slot.count === 0) return slot;                              // 缺货格可改放任意商品
        if (slot.productId === pid && slot.count < product.slotCap) return slot;
```

  但 `addItem` 里有 `if (slot.productId && slot.productId !== pid) return false;` 会挡住改放。故 `addItem` 的该行同时改为：

```js
    if (slot.productId && slot.count > 0 && slot.productId !== pid) return false;
```

2. `serializeShelves` / `restoreShelves`：`count === 0` 且 `productId` 非空的格现在是合法状态，存读档需原样往返。检查 `world.js:445-460` 附近的实现，确保它写入并读回 `productId` 与 `count` 两个字段（现有实现已是如此，无需改动，但要**读一遍确认**）。

- [ ] **Step 5: 建格时初始化价签，读档后刷新价签**

在 `buildRack` 的格位循环末尾（`registerInteractable(holder, ...)` 之后）加一行：

```js
        updateSlotTag(slot);
```

在 `restoreShelves` 的 `updateSlotVisual(slot);` 之后加一行：

```js
      updateSlotTag(slot);
```

- [ ] **Step 6: 把 `updateSlotTag` 挂上 `G.world`**

在 `world.js` 末尾的导出对象里（`registerInteractable: registerInteractable,` 附近）新增：

```js
    updateSlotTag: updateSlotTag,
```

- [ ] **Step 7: 让 `setPrice` 刷新受影响格位的价签**

在 `js/shop.js` 的 `setPrice` 函数体末尾（写入 `G.state.prices` 之后）追加：

```js
    if (G.world && G.world.updateSlotTag) {
      var slots = G.world.slots || [];
      for (var i = 0; i < slots.length; i++) {
        if (slots[i].productId === pid) G.world.updateSlotTag(slots[i]);
      }
    }
```

- [ ] **Step 8: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/world.js"
node --check "C:/Users/quito/超市模拟器/js/shop.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

- [ ] **Step 9: 请用户运行自测，确认全部通过**

同 Step 2 的命令，但改看全量：

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|"ok": false' -Context 1,1
```

期望 `SELFTEST:PASS`。**重点盯 `world.stock`、`world.slotShape`、`save.shelves`、`customer.queued` 四条**——Step 4 改了空格判定语义，这几条最可能回归。

**停下等用户回报结果再继续。**

- [ ] **Step 10: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/world.js js/shop.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "feat(world): 价签 CanvasTexture 文字与有货/缺货/空格三状态"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 5：上架飞行动画

**Files:**
- Modify: `js/world.js`（`addItem` 接飞行起点、新增飞行驱动）
- Modify: `js/player.js:519-535`（`tryPlaceItem` 传入纸箱位置）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: Task 2 的 `itemGeo` / `itemMat` / `itemLocalPos`；Task 4 改造后的 `addItem(slot, pid, fromPos)`
- Produces: `G.world._flights`（内部数组，仅供自测断言飞行 mesh 不污染 itemGroup）

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 Task 4 插入的 `ck('world.tagPriceRefresh', ...)` 之后插入：

```js
      /* --- 上架飞行动画（Task 5）--- */
      var flySlot = G.world.findSlotWithProduct('f_noodle');
      var flyBefore = flySlot ? flySlot.itemGroup.children.length : -1;
      var flyFrom = new THREE.Vector3(flySlot.pos.x + 1.5, 1.2, flySlot.pos.z + 1.5);
      var flyOk = G.world.addItem(flySlot, 'f_noodle', flyFrom);
      ck('world.flightAccepted', flyOk === true, 'addItem 带 fromPos 应正常返回 true');
      // 飞行期间格内正式方块数暂时比 count 少 1（落位后补齐），故断言「格内 + 飞行中 === count」
      var flying = (G.world._flights || []).filter(function (f) { return f.slot === flySlot; }).length;
      ck('world.flightCount', flySlot.itemGroup.children.length + flying === flySlot.count,
        '格内方块 ' + flySlot.itemGroup.children.length + ' + 飞行中 ' + flying +
        ' 应等于 count ' + flySlot.count + '（进入前 ' + flyBefore + '）');
      ck('world.flightNotInGroup',
        (G.world._flights || []).length === 0 ||
        G.world._flights.every(function (f) { return f.mesh.parent !== flySlot.itemGroup; }),
        '飞行中的临时 mesh 不得挂进 itemGroup');
      // 省略 fromPos 时不应产生飞行（自测与顾客取货都走这条路径）
      var noFlyBefore = (G.world._flights || []).length;
      G.world.addItem(flySlot, 'f_noodle');
      ck('world.flightOptional', (G.world._flights || []).length === noFlyBefore,
        '不传 fromPos 不应产生飞行');
```

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|flight' -Context 0,2
```

期望：`SELFTEST:FAIL`，`world.flightNotInGroup` 与 `world.flightOptional` 因 `G.world._flights` 为 `undefined` 而失败。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 实现飞行动画驱动**

在 `js/world.js` 的 `popInItem` 函数**之前**新增：

```js
  /* DESIGN §6：商品自纸箱位置直线飞向目标格位，180ms ease-out。
     纯视觉，与逻辑解耦——addItem 返回时商品在逻辑上已在格内。自驱 rAF，不接主循环 dt。 */
  var flights = [];
  var flightRaf = false;

  function easeOutCubic(t) { return 1 - Math.pow(1 - t, 3); }

  function flyItem(slot, product, fromPos, targetLocal) {
    if (!sceneRef || typeof requestAnimationFrame !== 'function') return null;
    var mesh = new THREE.Mesh(itemGeo(), itemMat(product.color));
    mesh.position.copy(fromPos);
    sceneRef.add(mesh);

    var to = slot.itemGroup.position.clone().add(targetLocal);
    var f = { mesh: mesh, from: fromPos.clone(), to: to, t0: Date.now(), slot: slot, onDone: null };
    flights.push(f);
    if (!flightRaf) { flightRaf = true; requestAnimationFrame(stepFlights); }
    return f;
  }

  function stepFlights() {
    var now = Date.now();
    for (var i = flights.length - 1; i >= 0; i--) {
      var f = flights[i];
      var k = Math.min(1, (now - f.t0) / 180);
      f.mesh.position.lerpVectors(f.from, f.to, easeOutCubic(k));
      if (k >= 1) {
        if (f.mesh.parent) f.mesh.parent.remove(f.mesh);
        flights.splice(i, 1);
        if (f.onDone) f.onDone();
      }
    }
    if (flights.length) requestAnimationFrame(stepFlights);
    else flightRaf = false;
  }
```

- [ ] **Step 4: 让 `addItem` 在收到 `fromPos` 时走飞行路径**

把 Task 4 Step 4 写下的 `addItem` 尾部三行：

```js
    updateSlotVisual(slot);
    updateSlotTag(slot);
    if (slot.count <= slot.itemGroup.children.length) popInItem(slot.itemGroup.children[slot.count - 1]);
    return true;
```

替换为：

```js
    var idx = slot.count - 1;
    updateSlotTag(slot);

    if (fromPos && idx < 16) {
      // 先飞，落位后才让正式方块出现；飞行期间格内少一个方块（180ms，肉眼等价于「正在放上去」）
      var local = itemLocalPos(idx, slot.faceZ);
      var f = flyItem(slot, product, fromPos, local);
      if (f) {
        f.onDone = function () {
          updateSlotVisual(slot);
          if (slot.count > idx && idx < slot.itemGroup.children.length) popInItem(slot.itemGroup.children[idx]);
        };
        return true;
      }
    }

    updateSlotVisual(slot);
    if (slot.count <= slot.itemGroup.children.length) popInItem(slot.itemGroup.children[slot.count - 1]);
    return true;
```

**为什么飞行期间不调用 `updateSlotVisual`**：让正式方块在商品「飞到」之后才出现，否则会出现商品既在飞、又已经躺在格子里的重影。代价是这 180ms 内 `itemGroup.children.length` 比 `slot.count` 少 1——Step 1 的 `world.flightCount` 断言已按此写成「格内 + 飞行中 === count」。

**逻辑与表现解耦**：`slot.count` 在 `addItem` 返回时就已是最终值，顾客取货、存档、库存统计读的都是它，不受飞行动画影响。

- [ ] **Step 5: 导出 `_flights` 供自测**

在 `world.js` 末尾导出对象里新增：

```js
    _flights: flights,
```

- [ ] **Step 6: 让 `tryPlaceItem` 传入纸箱世界坐标**

把 `js/player.js:528` 这一行：

```js
    var ok = G.world.addItem(slot, pid);
```

改为：

```js
    // 飞行起点取手持纸箱的世界坐标（syncCarriedBox 每帧更新它）
    var fromPos = (carrying.mesh && carrying.mesh.position) ? carrying.mesh.position.clone() : null;
    var ok = G.world.addItem(slot, pid, fromPos);
```

- [ ] **Step 7: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/world.js"
node --check "C:/Users/quito/超市模拟器/js/player.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

- [ ] **Step 8: 请用户运行自测，确认全部通过**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|"ok": false' -Context 1,1
```

期望 `SELFTEST:PASS`。

**停下等用户回报结果再继续。**

- [ ] **Step 9: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/world.js js/player.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "feat(world): 上架商品自纸箱直线飞向货架格，180ms ease-out"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 6：扫码滑动改 ease-out

最小的一个 task，独立可验。

**Files:**
- Modify: `js/checkout.js:256-265`（`stepSlides`）
- Modify: `js/checkout.js`（`_test` 导出）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: 无
- Produces: `G.checkout._test.ease(t) -> Number`（缓动函数，供断言）

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 `ck('checkout.scan', ...)` 那一行，在它**之后**插入：

```js
      /* --- 扫码滑动缓动（Task 6）--- */
      var easeFn = G.checkout._test && G.checkout._test.ease;
      ck('checkout.easeExists', typeof easeFn === 'function', '未暴露缓动函数');
      ck('checkout.easeOut',
        typeof easeFn === 'function' &&
        Math.abs(easeFn(0) - 0) < 1e-9 &&
        Math.abs(easeFn(1) - 1) < 1e-9 &&
        Math.abs(easeFn(0.5) - 0.875) < 1e-9,
        typeof easeFn === 'function' ? 'ease(0.5)=' + easeFn(0.5) + '，应为 0.875（1-(1-t)³）' : 'n/a');
```

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|ease' -Context 0,2
```

期望：`SELFTEST:FAIL`，两条新断言 `ok: false`。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 实现缓动并接入 `stepSlides`**

把 `js/checkout.js:256-265` 的 `stepSlides` 替换为：

```js
  /* DESIGN §6：所有 3D 位移动画统一 ease-out（f(t) = 1 - (1-t)³） */
  function easeOutCubic(t) { return 1 - Math.pow(1 - t, 3); }

  function stepSlides(dt) {
    if (!tx) return;
    for (var i = 0; i < tx.items.length; i++) {
      var s = tx.items[i].slide;
      if (!s) continue;
      s.t = Math.min(1, s.t + dt / 0.15);
      tx.items[i].mesh.position.lerpVectors(s.from, s.to, easeOutCubic(s.t));
      if (s.t >= 1) tx.items[i].slide = null;
    }
  }
```

- [ ] **Step 4: 把缓动函数挂上 `_test`**

`_test` 对象定义在 `js/checkout.js:592`（导出对象里的一个字段，注释为「自测钩子：仅 main.js 的 ?selftest 脚本化场景调用」）。在它的第一项 `tx: function () { return tx; },` **之前**插入：

```js
      ease: easeOutCubic,
```

- [ ] **Step 5: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/checkout.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

- [ ] **Step 6: 请用户运行自测，确认全部通过**

同 Step 2 的命令。期望 `SELFTEST:PASS`。

**停下等用户回报结果再继续。**

- [ ] **Step 7: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/checkout.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "fix(checkout): 扫码滑动改 ease-out，符合 DESIGN §6"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 7：收银台站位系统

本计划最大的一块。引入由传送带几何反推的站位，替换 `startCamLock`；同时把 `checkout.js` 对 `G.player.init` 的 monkey-patch 换成正规接口。

**Files:**
- Modify: `js/player.js:44-48`（导出对象）、`js/player.js:51-69`（`init`）、`js/player.js:176-183`（`wasBlocked` 分支）
- Modify: `js/checkout.js:62-72`（删除 monkey-patch）、`js/checkout.js:409-449`（进出与站位）
- Test: `js/main.js`（`runSelftest()` 内新增断言）

**Interfaces:**
- Consumes: 无
- Produces: `G.player.camera`、`G.player.getPose() -> {x, z, yaw, pitch}`、`G.player.setPose({x, z, yaw, pitch})`、`G.checkout.stance -> {pos, yaw, pitch}|null`

- [ ] **Step 1: 写失败的测试**

在 `js/main.js` 的 `runSelftest()` 里，找到 `ck('customer.queued', ready, ...)` 那一行，在它**之后**插入：

```js
      /* --- 收银台站位系统（Task 7）--- */
      ck('player.poseApi',
        typeof G.player.getPose === 'function' && typeof G.player.setPose === 'function' && !!G.player.camera,
        '缺 getPose/setPose/camera');

      var poseBefore = G.player.getPose ? G.player.getPose() : null;
      G.checkout.enterRegister();
      var st = G.checkout.stance;
      ck('checkout.stanceComputed', !!st && !!st.pos && isFinite(st.yaw) && isFinite(st.pitch),
        'stance = ' + JSON.stringify(st && { x: round2(st.pos.x), z: round2(st.pos.z), yaw: round2(st.yaw), pitch: round2(st.pitch) }));

      pump(20, function () { return false; });   // 推进 1.0s，让 300ms 过渡走完
      ck('checkout.stanceReached',
        !!st && camera.position.distanceTo(st.pos) < 0.02,
        '相机 ' + JSON.stringify({ x: round2(camera.position.x), y: round2(camera.position.y), z: round2(camera.position.z) }) +
        ' 应到达 ' + JSON.stringify({ x: round2(st.pos.x), y: round2(st.pos.y), z: round2(st.pos.z) }));

      // 站位必须在柜台背侧（x < 柜台中心 -7.0），而非顾客侧
      ck('checkout.stanceBehind', !!st && st.pos.x < -7.0,
        '站位 x = ' + (st && round2(st.pos.x)) + '，应 < -7.0（柜台背侧）');

      // 看得到就点得到：从站位朝 stance 朝向，传送带上每一件商品都应可被射线命中
      var txAim = currentTx();
      var aimCam = new THREE.PerspectiveCamera(70, 16 / 9, 0.1, 200);
      aimCam.position.copy(st.pos);
      aimCam.rotation.order = 'YXZ';
      aimCam.rotation.set(st.pitch, st.yaw, 0);
      aimCam.updateMatrixWorld(true);
      var frustum = new THREE.Frustum();
      frustum.setFromProjectionMatrix(
        new THREE.Matrix4().multiplyMatrices(aimCam.projectionMatrix, aimCam.matrixWorldInverse));
      var visN = 0, totN = txAim ? txAim.items.length : 0;
      for (var ai = 0; ai < totN; ai++) {
        if (frustum.containsPoint(txAim.items[ai].mesh.position)) visN++;
      }
      ck('checkout.stanceFraming', totN > 0 && visN === totN,
        '传送带 ' + totN + " 件中 " + visN + ' 件在视锥内');

      G.checkout.exitRegister();
      pump(20, function () { return false; });
      var poseAfter = G.player.getPose ? G.player.getPose() : null;
      ck('checkout.poseRestored',
        !!poseBefore && !!poseAfter &&
        Math.abs(poseAfter.x - poseBefore.x) < 0.02 &&
        Math.abs(poseAfter.z - poseBefore.z) < 0.02,
        JSON.stringify({ before: poseBefore, after: poseAfter }));
      ck('checkout.stanceCleared', G.checkout.stance === null, '退出后 stance 应置空');
```

- [ ] **Step 2: 请用户运行自测，确认新断言失败**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|poseApi|stance|poseRestored' -Context 0,2
```

期望：`SELFTEST:FAIL`，`player.poseApi` 等新断言 `ok: false`。

**停下等用户回报结果再继续。**

- [ ] **Step 3: 在 player.js 暴露位姿接口**

把 `js/player.js:44-48` 的导出对象改为：

```js
  G.player = {
    init: init,
    update: update,
    carrying: null,
    camera: null,
    getPose: getPose,
    setPose: setPose
  };
```

在 `init` 函数体末尾（`G.bus.on('screen', onScreenEvent);` 之后）加一行：

```js
    G.player.camera = camera;
```

在 `init` 函数之后新增两个函数：

```js
  /* 位姿快照（纯数值，非引用）。收银台进出时由 checkout 显式保存与还原。 */
  function getPose() {
    return { x: pos ? pos.x : 0, z: pos ? pos.z : 0, yaw: yaw, pitch: pitch };
  }

  function setPose(p) {
    if (!p || !pos || !camera) return;
    if (isFinite(p.x)) pos.x = p.x;
    if (isFinite(p.z)) pos.z = p.z;
    if (isFinite(p.yaw)) yaw = p.yaw;
    if (isFinite(p.pitch)) pitch = G.clamp(p.pitch, -MAX_PITCH, MAX_PITCH);
    camera.position.set(pos.x, EYE_HEIGHT, pos.z);
    camera.rotation.set(pitch, yaw, 0);
  }
```

- [ ] **Step 4: 让 player 在收银模式下不再从相机反推位姿**

把 `js/player.js:176-183` 的 `wasBlocked` 分支：

```js
    if (wasBlocked) {
      // 界面/收银模式可能已改动相机朝向，重新同步内部状态，避免视角跳变
      yaw = camera.rotation.y;
      pitch = G.clamp(camera.rotation.x, -MAX_PITCH, MAX_PITCH);
      pos.x = camera.position.x;
      pos.z = camera.position.z;
      wasBlocked = false;
    }
```

改为：

```js
    if (wasBlocked) {
      // 全屏界面不移动相机，但可能改了朝向，仍需同步；
      // 收银模式的位姿由 checkout.exitRegister() 经 setPose 显式还原，这里不得再从相机反推，
      // 否则会把站位坐标（柜台背侧）当成玩家位置写回去。
      if (!registerJustExited) {
        yaw = camera.rotation.y;
        pitch = G.clamp(camera.rotation.x, -MAX_PITCH, MAX_PITCH);
        pos.x = camera.position.x;
        pos.z = camera.position.z;
      }
      registerJustExited = false;
      wasBlocked = false;
    }
```

在模块状态区（`var wasBlocked = false;` 之后）新增：

```js
  var registerJustExited = false;
```

并在 `setPose` 函数体开头加一行，让 checkout 的还原能标记这次同步已由它负责：

```js
    registerJustExited = true;
```

- [ ] **Step 5: 删除 checkout.js 对 player.init 的 monkey-patch**

删除 `js/checkout.js:62-72` 整段（包含注释与 `hookPlayerInit` IIFE）：

```js
  /* 契约也没有暴露 camera：在 main.js 调用 G.player.init(camera, dom) 时顺手记下相机引用。
     只是旁观一个已有的契约调用，不新增跨模块 API；player 的行为完全不变。 */
  (function hookPlayerInit() { ... })();
```

把 `camera()` 函数（`checkout.js:74-80`）简化为：

```js
  function camera() {
    if (camRef) return camRef;
    if (G.player && G.player.camera && G.player.camera.isCamera) { camRef = G.player.camera; return camRef; }
    var sc = scene();
    if (sc) sc.traverse(function (o) { if (!camRef && o.isCamera) camRef = o; });
    return camRef;
  }
```

（`G.player.camera` 现在是正规接口，兜底的场景遍历保留以防 init 顺序问题。）

- [ ] **Step 6: 实现站位计算**

在 `js/checkout.js` 的 `enterRegister` 之前新增：

```js
  /* 站位由传送带几何反推，不硬编码坐标——子项目 C 做多收银台时每台自动各有各的站位。
     后退 0.93m 落在柜台背侧与西墙之间的空隙（现有布局下约 x=-7.75，即雇佣收银员站桩处）。 */
  var STANCE_BACK = 0.93;      // 沿 dir 后退距离（dir 由顾客侧指向柜台）
  var STANCE_EYE = 1.65;       // 与 player.js EYE_HEIGHT 一致
  // 相机向右偏航 → 画面内容左移，避开右侧 340px 的 #pos 面板。
  // 注意符号：增大 camera.rotation.y 会让物体在画面中右移，所以这里是「减」。
  var STANCE_YAW_BIAS = 0.18;

  function itemFieldCenter() {
    var f = beltFrame();
    if (!f) return null;
    // 商品实际落点由 beltPos(i) 决定，取前 8 个位置的均值作为商品场中心，
    // 比 f.origin（柜台近侧边缘）准确得多——原 startCamLock 看向 f.origin 正是取景偏的成因之一。
    var c = new THREE.Vector3();
    for (var i = 0; i < 8; i++) c.add(beltPos(i));
    return c.multiplyScalar(1 / 8);
  }

  function computeStance() {
    var f = beltFrame();
    var target = itemFieldCenter();
    if (!f || !target) return null;

    var p = f.origin.clone().addScaledVector(f.dir, STANCE_BACK);
    p.y = STANCE_EYE;

    // 相机在 YXZ 顺序下的前向为 (-sinYaw·cosPitch, sinPitch, -cosYaw·cosPitch)，
    // 故对准方向 d 时 yaw = atan2(-d.x, -d.z)。
    var d = target.clone().sub(p);
    var yaw = Math.atan2(-d.x, -d.z) - STANCE_YAW_BIAS;
    var pitch = Math.atan2(d.y, Math.sqrt(d.x * d.x + d.z * d.z));
    return { pos: p, yaw: yaw, pitch: pitch };
  }
```

- [ ] **Step 7: 用站位替换 `startCamLock` / `stepCamLock`**

把 `js/checkout.js:409-449`（`enterRegister` / `exitRegister` / `startCamLock` / `stepCamLock` 四个函数）整体替换为：

```js
  function enterRegister() {
    if (inRegister) return;
    var st = computeStance();
    if (!st) return;

    savedPose = (G.player && G.player.getPose) ? G.player.getPose() : null;
    stance = st;
    G.checkout.stance = st;

    inRegister = true;
    G.checkout.inRegister = true;   // player.js 据此屏蔽移动/指针锁定
    G.bus.emit('screen', { name: 'pos' });
    var el = posEl();
    if (el) el.style.display = 'block';

    // 站位与收银员站桩重合，进入期间临时隐藏（退出时按 G.state.cashier 还原）
    cashierWasVisible = !!(G.state && G.state.cashier);
    if (cashierWasVisible && G.world && G.world.setCashierVisible) G.world.setCashierVisible(false);

    startTween(st.pos, st.yaw, st.pitch);
    posDirty = true;
    renderPos();
  }

  /* 回程过渡走完之前不放开 inRegister：否则 player.update 会在这 300ms 里
     每帧执行 camera.position.set(pos.x, EYE_HEIGHT, pos.z)（player.js:200）与过渡争夺相机。 */
  function exitRegister() {
    if (!inRegister || exiting) return;
    exiting = true;
    stance = null;
    G.checkout.stance = null;

    var el = posEl();
    if (el) el.style.display = 'none';
    G.ui.prompt(null);

    if (cashierWasVisible && G.world && G.world.setCashierVisible) G.world.setCashierVisible(true);
    cashierWasVisible = false;

    var restore = savedPose;
    savedPose = null;

    function finish() {
      exiting = false;
      inRegister = false;
      G.checkout.inRegister = false;
      G.bus.emit('screen', { name: null });
      if (restore && G.player && G.player.setPose) G.player.setPose(restore);
    }

    if (restore) {
      startTween(new THREE.Vector3(restore.x, STANCE_EYE, restore.z), restore.yaw, restore.pitch, finish);
    } else {
      tween = null;
      finish();
    }
  }

  /* 300ms ease-out 同时插值位置与朝向（DESIGN §6 的面板过渡档位） */
  function startTween(toPos, toYaw, toPitch, onDone) {
    var cam = camera();
    if (!cam) { if (onDone) onDone(); return; }
    cam.rotation.order = 'YXZ';
    tween = {
      fromPos: cam.position.clone(),
      fromYaw: cam.rotation.y, fromPitch: cam.rotation.x,
      toPos: toPos.clone(), toYaw: toYaw, toPitch: toPitch,
      t: 0, onDone: onDone || null
    };
  }

  function stepTween(dt) {
    if (!tween) return;
    var cam = camera();
    if (!cam) { tween = null; return; }
    tween.t = Math.min(1, tween.t + dt / 0.3);
    var k = easeOutCubic(tween.t);
    cam.position.lerpVectors(tween.fromPos, tween.toPos, k);
    cam.rotation.set(
      tween.fromPitch + (tween.toPitch - tween.fromPitch) * k,
      tween.fromYaw + (tween.toYaw - tween.fromYaw) * k,
      0
    );
    if (tween.t >= 1) {
      var done = tween.onDone;
      tween = null;
      if (done) done();
    }
  }
```

在模块状态区新增（与 `inRegister` 等变量同处）：

```js
  var stance = null;
  var savedPose = null;
  var tween = null;
  var exiting = false;
  var cashierWasVisible = false;
```

并删除原有的 `var camLock = null;` 声明。

**本 task 复用 Task 6 在 `checkout.js` 里定义的 `easeOutCubic`**，所以 Task 6 必须先完成。`world.js` 里另有一份同名函数（Task 5 Step 3）——两个模块各自持有一份是本项目无模块系统下的正常做法，不要试图共享。

- [ ] **Step 8: 把 `stepTween` 接入 update，并导出 `stance`**

`stepCamLock(dt)` 的唯一调用点在 `js/checkout.js:575`，位于 `update(dt)` 内：

```js
    if (tx) stepTx(dt);
    stepSlides(dt);
    stepCamLock(dt);          // ← 改为 stepTween(dt);
    if (scanCd > 0) scanCd -= dt;
```

改为 `stepTween(dt);`。

**注意 `stepTween` 不得加 `if (!inRegister) return;` 守卫**——原 `stepCamLock` 有这个守卫（`checkout.js:444`），但退出收银台的回程过渡必须在 `inRegister` 已经开始收尾时继续推进。守卫由 `tween` 是否为 `null` 承担。

在 `G.checkout` 导出对象里新增：

```js
    stance: null,
```

- [ ] **Step 9: 运行语法检查**

```bash
node --check "C:/Users/quito/超市模拟器/js/player.js"
node --check "C:/Users/quito/超市模拟器/js/checkout.js"
node --check "C:/Users/quito/超市模拟器/js/main.js"
```

- [ ] **Step 10: 请用户运行自测，确认全部通过**

```
! & "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" | Select-String -Pattern 'SELFTEST|"ok": false' -Context 1,1
```

期望 `SELFTEST:PASS`（全部 40 条左右断言）。

若 `checkout.stanceFraming` 失败（部分商品不在视锥内），调整 `STANCE_BACK`（增大 = 退远 = 视野更宽）与 `STANCE_YAW_BIAS`，重跑直到通过。**这就是 spec §3.3 标记的唯一需实测联调的参数。**

**停下等用户回报结果再继续。**

- [ ] **Step 11: 提交**

```bash
git -C "C:/Users/quito/超市模拟器" add js/player.js js/checkout.js js/main.js
git -C "C:/Users/quito/超市模拟器" commit -m "feat(checkout): 收银台固定站位系统，取代被钉死的相机锁定"
git -C "C:/Users/quito/超市模拟器" push
```

---

## Task 8：人工验收

自测覆盖不到的两项，必须真人开浏览器跑一遍（spec §6.2）。

**Files:** 无改动（除非发现问题）

- [ ] **Step 1: 请用户在真实浏览器里开局**

双击 `index.html`（或用 `python -m http.server 8080` 后访问）。新游戏 → 订一箱方便面 → 等送达。

- [ ] **Step 2: 确认上架飞行**

搬起纸箱走到货架前，长按 `E` 连续上架。确认：

- 商品是从**纸箱**平滑飞到货架格，不是原地出现
- 连续上架时多件同时在飞，画面不乱、不卡
- 箱子清空后变暗，可拿去垃圾桶丢弃

- [ ] **Step 3: 确认价签可读性**

站在离货架约 1.5m 处（正常购物距离），确认价签上的商品名与价格清晰可读。准星对准某格时，**价签**应变高亮黄 `#FFD666`。

若文字糊，回到 Task 4 Step 3 提高 canvas 分辨率（`W`/`H` 加倍），并把 `tex.anisotropy` 设为 renderer 支持的最大值。

- [ ] **Step 4: 确认收银台取景**

按 `O` 开门，等顾客排到收银台并把商品放上传送带（尽量等一个买得多的顾客）。按 `E` 进收银台，确认：

- 视角平滑移到柜台背侧，不再是奇怪的斜角
- **满单时最边上那件商品仍完整可见，未被右侧 POS 面板遮挡，且可点击扫码**
- 逐件点击扫码，商品平滑滑向台面另一侧（有加速度感，不是匀速）
- 按 `Esc` 退出，玩家回到进入前的位置与朝向，可正常走动

- [ ] **Step 5: 记录结果**

若全部通过，本计划完成。若某项不达标，回到对应 task 修正后重跑自测与本 task。

- [ ] **Step 6: 更新 spec 状态并提交**

把 `docs/superpowers/specs/2026-07-30-手感修复包-design.md` 顶部的 `状态：待实现` 改为 `状态：已完成（2026-XX-XX）`（填实际完成日期）。

```bash
git -C "C:/Users/quito/超市模拟器" add docs/superpowers/specs/2026-07-30-手感修复包-design.md
git -C "C:/Users/quito/超市模拟器" commit -m "docs: 手感修复包完成"
git -C "C:/Users/quito/超市模拟器" push
```

---

## 附：spec 覆盖对照

| spec 章节 | 对应 task |
|---|---|
| §2.1 DESIGN.md §5 材质修订 | Task 1 Step 1 |
| §2.2 DESIGN.md §6 动画修订 | Task 1 Step 2 |
| §2.3 CONTRACTS.md 新增接口 | Task 1 Steps 3–6 |
| §3.1 stance 抽象 | Task 7 Step 6 |
| §3.2 坐标推导 | Task 7 Step 6（`computeStance` / `itemFieldCenter`） |
| §3.3 取景验证与调参 | Task 7 Step 1（`checkout.stanceFraming`）、Step 10、Task 8 Step 4 |
| §3.4 进出流程与位姿交接 | Task 7 Steps 3, 4, 7 |
| §3.5 已雇佣收银员的边界情况 | Task 7 Step 7（`cashierWasVisible`） |
| §4.1 价签视觉与三状态 | Task 3 Step 4、Task 4 Steps 3–5 |
| §4.1 贴图缓存、材质逐格独立 | Task 4 Step 3、Task 3 Step 1（`slotTagMatUnique`） |
| §4.2 不可见命中盒 | Task 3 Step 4 |
| §5.1 格位渲染增量化 | Task 2 |
| §5.2 上架飞行 | Task 5 |
| §5.3 扫码滑动加缓动 | Task 6 |
| §6.1 自测新增断言 | 各 task 的 Step 1 |
| §6.2 人工确认两项 | Task 8 |
| §7 明确不做 | 全计划未触及地图布局、材质升级、多收银台准备 |
