# 大卖场扩张（子项目 C）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 16×12 小店扩为 32×20 大卖场：分区购买开放、3 收银台、仓库存储架、顾客寻路、经济重平衡、存档 v2。

**Architecture:** 大壳静态一次建成（建筑/collider 不动态化），区域=卷帘门+collider+寻路节点三重门控、购买时解除；checkout 单例状态全部下沉 `registers[i]`；寻路为布局表自动生成的走廊节点图 + Dijkstra；存档断代 v2 + v1 折现迁移。

**Tech Stack:** Three.js r128 UMD、ES5、IIFE + window.G。

**依据文档：** `docs/superpowers/specs/2026-08-02-大卖场扩张-design.md`（spec）

## Global Constraints

- ES5、无构建、零网络请求；push 用 `git -c credential.helper=wincred push origin main`；**禁 git add .**
- 实施者自跑无头自测：`& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\edge-cN" --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1"`
- 基线：HEAD `7ee4a27`，自测正常态 86 / lowfx 80 全绿（注：正常路径 ck 共 85+1 条件项，报告口径以「无 ok:false」为准）
- 铁律：InstancedMesh 相关禁令（DESIGN §5.8）；传送带材质 color 恒 0x4E5866 + `userData.belt`；壳体 addShell 不 cast；价签几何不动；商品堆排布 itemLocalPos 不动
- 顾客不做互避（GDD §6 既有条款）；玩家不寻路；员工不寻路（站桩）
- 新世界 AABB：x∈[-20,20]、z∈[-11,11]、y∈[0,3.6]（阴影视锥/边界 collider 以此为准）
- 数值以 spec §2/§6/§8 表为准；坐标以本计划 T1 布局表为准（spec 的 ASCII 图为示意，本计划表为权威）

---

## File Structure

| 文件 | C 期改动 |
|---|---|
| `docs/GDD.md` | T0：§4 布局表换、§6 公式换、§9 等级表换、§11 经济换、§14 删两条裁剪 |
| `docs/CONTRACTS.md` | T0：寻路承诺作废 + nav.findPath/registers/storage/zones 契约；T8 校准 |
| `DESIGN.md` | T0：§5.8 预算/§5.10 比例雾预写；T8 实测回写 |
| `js/world.js` | T1 壳体/布局/门控；T2 nav 图；T3 registers 建造；T4 storage |
| `js/state.js` | T1 zones 字段；T6 存档 v2 + migrateV1 |
| `js/checkout.js` | T3 per-register 重构（最大单文件改动） |
| `js/customers.js` | T2 moveTo/lane；T5 公式；T7 手持上限 |
| `js/shop.js` | T3 buyZone；T4 延迟送达；T5 hireCashier(i) |
| `js/player.js` | T3 register 传参；T4 storage 交互分支 |
| `js/main.js` | T1 视锥/fog + 断言迁移；各 task 断言；T5 endDay 房租 |
| `js/textures.js` | T7 TAG_TEX 治漏（该缓存在 world.js——见 T7，实际改 world.js） |
| `js/ui.js` | T5 雇佣界面按台（许可页最小改动） |

任务依赖：T0→T1→{T2→T3→T4 串行（都动 world.js）}→T5→T6→T7→T8。

---

## Task 0：文档先行

**Files:** Modify `docs/GDD.md`、`docs/CONTRACTS.md`、`DESIGN.md`

**Interfaces:** Produces 全部后续 task 的规范依据。

- [ ] **Step 1: GDD.md 修订**

- §4 货架/布局表替换为 spec §2 的分区表（区域 A/B/C/W、120 格、收银台 3、卸货区双点位），并写明「区域=等级+金钱双门槛购买」
- §6 公式替换：`spawnInterval = clamp(20 − 0.8(day−1) − 1.2(level−1) − 2.0·zonesOpen, 3.0, 20) × rand(0.75,1.25)`；`concurrentCap = clamp(4 + level + 3·zonesOpen, 5, 20)`（zonesOpen = 已开购物区数−1）；补「顾客按走廊节点图寻路，仍互不碰撞可穿过」
- §9 等级表替换为 spec §6 表（Lv3 仓库★¥900 / Lv5 区域B★¥1500 附R2 / Lv8 区域C★¥3200 附R3 / Lv10 每台可雇员）
- §11 经济替换：房租分区累加 `40+W20+B30+C40`（删 Lv8 翻倍）；工资 `staffed 台数×60`；破产规则不变
- §14 修订：删除「自助收银/多收银台」「员工寻路/搬货 AI」两行中**多收银台**部分（改为正文特性）；「员工寻路/搬货 AI」保留为仍不做，注明「收银员为站桩，不移动」

- [ ] **Step 2: CONTRACTS.md 修订**

- 删除「店内布局保证：入口→过道→货架→收银台之间为直线可达（顾客走 waypoint 直线，无需寻路）」，替换为：

```
店内寻路：`G.world.nav.findPath(from, to) -> [Vector3]`（走廊节点图 + Dijkstra；未解锁区域节点禁用，
不可达返回 []）。顾客经 findPath 折线移动，互不碰撞可穿过；玩家与员工不寻路。
```

- G.world 段新增：

```js
G.world.registers   // [{index, mesh, beltMesh, front:Vector3, queueSpots:[Vector3×5], zone}]（按区域解锁惰性建造）
G.world.storageSlots // [{id, pos, marker, box:null|箱}]（仓库购买后建造，24 位）
G.world.storeBox(slot, box) /*->bool*/  G.world.takeBox(slot) /*->box|null*/
G.world.buildZone(z /*'B'|'C'|'W'*/)   // 幂等；开门+除collider+启用节点+建造该区设施
G.world.spawnBox(pid, atPos /*可选 Vector3；省略走 activeYard 空位，满则返回 null*/)
```

- G.state 段：`zones:{A,B,C,W}`、`registers:[{owned,staffed}×3]`、`cashier 字段废弃（迁移期兼容读）`
- G.shop 段：`buyZone(z) /*->bool 等级+金钱双校验*/`、`hireCashier(i)/fireCashier(i)`；`'cashier'` 事件 payload `{hired, index}`
- G.checkout 段：`_test.tx(i=0)/scanAll(i=0)/settle(i=0)/payCard(i=0)/registers()`；`enterRegister(reg)` 语义；`activeRegisterId`
- nav 段：`nav.registerFront/queueSpots` 标注废弃（由 `nav.registers` 取代——T3 落地时删）
- 存档条款：`gss-save-v2` 结构（spec §7 代码块照录）+ v1 迁移语义 + 双键 reset

- [ ] **Step 3: DESIGN.md 预写**

- §5.8：draw call 预算表换 spec §8（满配 ≈691、闸门 760、手持视觉上限 6）；价签图集预案一句留档
- §5.10：店面 32×20、墙高 3.6、fog 45/140、阴影 ±26/far 60/mapSize 4096（写为目标值，T8 实测回写）

- [ ] **Step 4: 提交推送**

```bash
git add docs/GDD.md docs/CONTRACTS.md DESIGN.md
git commit -m "docs(C-T0): 大卖场契约先行——GDD/CONTRACTS/DESIGN 修订"
# 末尾 Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>；push 用 wincred
```

（纯文档，无自测变化；跑一遍确认仍 86/86 无回归即可。）

---

## Task 1：32×20 壳体与布局（地基，最大几何改动）

**Files:**
- Modify: `js/world.js`（常量区、buildRoom 整体重写、buildRack 两处地基、buildCheckout→buildRegister(i)、syncLayout 分区化、YARD 双点位）
- Modify: `js/state.js`（G.state 加 `zones`）
- Modify: `js/main.js`（阴影视锥/fog；断言迁移 5 处 + 新增 3 条）

**Interfaces:**
- Produces: `ZONES/SHELF_TABLE/REGISTER_TABLE/STORAGE_TABLE` 布局常量；`G.world.buildZone(z)`；`G.world.registers`（本 task 只建 R1，结构齐全）；`zoneOf(x,z)` 内部；卷帘门 mesh（`type:'shutter'` 交互 T3 接）；双卸货点位 `activeYardPositions()`
- Consumes: T0 契约

### 布局常量（唯一权威，全部逐字进代码）

```js
  // ---- C 期布局（32×20 大壳；原点居中）----
  var ROOM_HALF_X = 16, ROOM_HALF_Z = 10;
  var WALL_H = 3.6, WALL_T = 0.2;

  /* 区域范围（AABB，含义：该区的地面活动范围） */
  var ZONE_RECTS = {
    A:    { minX: 0.2,  maxX: 15.8, minZ: -3.5, maxZ: 3.5 },
    B:    { minX: -15.8, maxX: -0.2, minZ: -3.5, maxZ: 3.5 },
    C:    { minX: -15.8, maxX: 15.8, minZ: -9.8, maxZ: -4.2 },
    W:    { minX: -15.8, maxX: -8.0, minZ: 4.2,  maxZ: 9.8 },
    core: { minX: -8.0,  maxX: 15.8, minZ: 4.0,  maxZ: 9.8 }   // 前厅+收银，恒开
  };

  /* 卷帘门：购买后 mesh 隐藏 + collider 移除 + 节点启用 */
  var SHUTTERS = [
    { zone: 'B', minX: -5, maxX: -3, z: 4,  price: 1500, lv: 5, label: '区域 B' },   // 内墙 z=+4 上
    { zone: 'C', minX: 3,  maxX: 5,  z: -4, price: 3200, lv: 8, label: '区域 C' },   // 内墙 z=-4 上
    { zone: 'W', x: -8, minZ: 6, maxZ: 8,   price: 900,  lv: 3, label: '仓库' }      // 仓库东墙 x=-8 上
  ];

  /* 货架表：zone + 中心 + aisleZ（buildRack 朝向与服务点由 aisleZ 推导） */
  var SHELF_TABLE = [
    // 区域 A（开局 4 架）
    { zone:'A', x: 3.0,  z:-1.6, aisleZ: 0 }, { zone:'A', x: 7.5, z:-1.6, aisleZ: 0 },
    { zone:'A', x: 12.0, z:-1.6, aisleZ: 0 }, { zone:'A', x: 3.0, z: 1.6, aisleZ: 0 },
    // 区域 B（6 架）
    { zone:'B', x:-13.5, z:-1.6, aisleZ: 0 }, { zone:'B', x:-10.0, z:-1.6, aisleZ: 0 },
    { zone:'B', x:-6.5,  z:-1.6, aisleZ: 0 }, { zone:'B', x:-13.5, z: 1.6, aisleZ: 0 },
    { zone:'B', x:-10.0, z: 1.6, aisleZ: 0 }, { zone:'B', x:-6.5,  z: 1.6, aisleZ: 0 },
    // 区域 C（6 架，双行，走廊 z=-7.0）
    { zone:'C', x:-12.0, z:-8.6, aisleZ:-7.0 }, { zone:'C', x:-6.0, z:-8.6, aisleZ:-7.0 },
    { zone:'C', x: 0.0,  z:-8.6, aisleZ:-7.0 }, { zone:'C', x: 6.0, z:-8.6, aisleZ:-7.0 },
    { zone:'C', x:-12.0, z:-5.4, aisleZ:-7.0 }, { zone:'C', x:-6.0, z:-5.4, aisleZ:-7.0 }
  ];
  var FRIDGE_TABLE = [
    { zone:'A', x: 7.5, z: 1.6, aisleZ: 0 }, { zone:'A', x: 12.0, z: 1.6, aisleZ: 0 },
    { zone:'B', x:-2.5, z:-1.6, aisleZ: 0 }, { zone:'B', x:-2.5,  z: 1.6, aisleZ: 0 }
  ];
  // 合计 16 组货架 + 4 冷藏 = 120 格 ✓

  /* 收银台：柜台长轴沿 x（Box 2.0×1.0×1.2），中心 z=7.6；R1 开局，R2/R3 随 B/C */
  var REGISTER_TABLE = [
    { index: 0, x: -2, zone: 'core' },
    { index: 1, x: 4,  zone: 'B' },
    { index: 2, x: 10, zone: 'C' }
  ];
  var REGISTER_Z = 7.6;
  // front=(x, 0, 6.8)；queueSpots z = 6.2, 5.6, 5.0, 4.4, 3.8（沿 -z 排）

  /* 仓库存储位：单层地面网格 6 列 × 4 排 = 24 位 */
  var STORAGE_XS = [-14.9, -13.7, -12.5, -11.3, -10.1, -8.9];
  var STORAGE_ZS = [5.0, 6.3, 7.6, 8.9];
  var STORAGE_TABLE = [];
  STORAGE_ZS.forEach(function (z) {
    STORAGE_XS.forEach(function (x) { STORAGE_TABLE.push({ x: x, z: z }); });
  });   // 24 位（T4 消费；T1 只定义常量不建造）
```

```js
  /* 卸货区双点位：仓库未购 = 东侧人行道北段；已购 = 西侧仓库外 */
  var YARD_EAST = [];
  [16.6, 17.4, 18.2, 19.0].forEach(function (x) {
    [6.9, 7.7, 8.5].forEach(function (z) { YARD_EAST.push({ x: x, z: z }); });
  });   // 12 位
  var YARD_WEST = [];
  [-19.2, -18.4, -17.6, -16.8].forEach(function (x) {
    [4.5, 5.5, 6.5].forEach(function (z) { YARD_WEST.push({ x: x, z: z }); });
  });   // 12 位
  function activeYardPositions() {
    return (G.state && G.state.zones && G.state.zones.W) ? YARD_WEST : YARD_EAST;
  }
```

旧 `SHELF_POSITIONS/FRIDGE_POSITIONS/YARD_XS/YARD_ZS/YARD_POSITIONS` 全部删除；`freeYardSlot` 内 `YARD_POSITIONS` 引用改 `activeYardPositions()`（T4 再改返回 null 语义，本 task 保持兜底返回 `[0]` 以免破坏现有断言）。

### buildRoom 重写要点（完整新函数，实施者按下列规格逐段写）

1. 地板/天花板 `PlaneGeometry(32, 20)`，纹理 repeat 地板 `(16,10)`、天花 `(26.7,16.7)`
2. 外墙：西墙全长（x=-16，**留送货门缺口 z∈[6,8]**→两段：z[-10,6] 与 z[8,10]）；北墙全长（z=-10）；南墙全长（z=+10）；东墙**留顾客门缺口 z∈[4.4,6.2]**→两段 z[-10,4.4] 与 z[6.2,10]。墙纹理 repeat 按段长/2
3. 内隔墙（同外墙材质做法，addShell）：
   - z=-4 全宽 x[-16,16]，**留卷帘门 C 缺口 x∈[3,5]**→两段
   - z=+4 半宽 x[-16,0]，**留卷帘门 B 缺口 x∈[-5,-3]**→两段
   - x=-8 段 z[4,10]，**留卷帘门 W 缺口 z∈[6,8]**→两段（仓库东墙）
4. 卷帘门 mesh：`BoxGeometry(宽, 3.0, 0.12)` 灰色 `0x9AA4AE` 带 `shelfMetal(2,3)` 纹理，挂 `userData.shutter = zone`，`registerInteractable(mesh, {type:'shutter', data:{zone, price, lv, label}, prompt:''})`（提示文案 T3 的 computePrompt 生成）；`castShadow=false, receiveShadow=true`
5. 门框柱：顾客门两根（东墙缺口两端）+ 送货门两根（西墙缺口两端），addShell
6. 卸货区地面 ×2（东：`Plane(4,3)` @ (18, 0.001, 7.7)；西：`Plane(4,3.2)` @ (-18, 0.001, 5.7)），人行道 `Plane(4,6)` @ (18, 0.001, 6)——东侧卸货地面与人行道重叠区并为一块 `Plane(4, 8)` @ (18, 0.001, 6)（简化：东侧只铺一块大混凝土 x[16,20] z[2,10]）
7. **collider 清单**（外墙 6 段 + 内墙 6 段 + 卷帘门 3 + 边界围栏）：
   - 外墙每段一条 AABB（厚 ±0.2）
   - 内墙每段一条
   - 卷帘门每扇一条（购买时由 buildZone 移除——collider 对象加 `zoneGate: zone` 标记，移除时按标记 splice）
   - 世界边界：东侧人行道外缘 x=20、北 z=-11 至南 z=11 的三条围栏（防走出世界）；西侧卸货区外缘 x=-20 同理三条
8. nav 基础点：`nav.entry=(15.2,0,5.3)`、`nav.exit=(18,0,5.3)`；`nav.registers=[]`（T3 填充）；旧 `nav.registerFront/queueSpots` **本 task 起指向 R1 的等价值**（front=(-2,0,6.8)、5 个 queueSpots）以保持 checkout 单台代码继续工作——T3 重构后删除
9. `buildComputer` 移位 (14, 0.45, 9.4)（screen 相应 +z 面朝 -z：screen z=9.18，rotation 不变但注意朝向——电脑面朝 z=-方向即店内；实施者按现函数结构平移坐标并把 screen 放在 body 的 -z 侧）；`buildTrash` 移位 (-15, 0.3, 9.4)（仓库内；仓库未购时玩家进不去——**垃圾桶改放前厅 (-7.4, 0.3, 9.4)**，仓库版位置作废，全期一处不随区域动）
10. `buildRegister(i)`（新函数，替换 buildCheckout；本 task 只在 init 时建 R1；world.js 模块级新增 `var registers = [];` 并在导出对象加 `registers: registers`，与 `nav.registers` 同源同引用）：

```js
  function buildRegister(i) {
    var def = REGISTER_TABLE[i];
    var body = new THREE.Mesh(new THREE.BoxGeometry(2.0, 1.0, 1.2),
      flatMat(G.tex.on ? 0xFFFFFF : 0x6E7A88, G.tex.on ? { map: G.tex.counterLaminate(1.2, 2) } : null));
    body.position.set(def.x, 0.5, REGISTER_Z);
    addMesh(body, true, true);
    var belt = new THREE.Mesh(new THREE.BoxGeometry(1.6, 0.08, 0.9),
      flatMat(0x4E5866, G.tex.on ? { map: G.tex.beltRubber(3, 5) } : null));
    belt.position.set(def.x, 1.04, REGISTER_Z);
    belt.userData.belt = true;
    belt.userData.registerId = i;
    addMesh(belt, true, true);
    colliders.push({ minX: def.x - 1.0, maxX: def.x + 1.0, minZ: REGISTER_Z - 0.6, maxZ: REGISTER_Z + 0.6 });
    var front = new THREE.Vector3(def.x, 0, 6.8);
    var spots = [];
    [6.2, 5.6, 5.0, 4.4, 3.8].forEach(function (z) { spots.push(new THREE.Vector3(def.x, 0, z)); });
    var reg = { index: i, mesh: body, beltMesh: belt, front: front, queueSpots: spots, zone: def.zone };
    registers.push(reg);
    nav.registers.push(reg);
    registerInteractable(body, { type: 'register', data: { register: reg }, prompt: '[E] 进入收银台' });
    return reg;
  }
```

11. `buildZone(z)`（幂等）：`zones[z]` 置 true 由调用方（shop.buyZone/T6 读档）负责；本函数做视觉与物理：隐藏该 zone 卷帘门 mesh、按 `zoneGate` 标记移除 collider、启用 nav 节点（T2 接）、按 SHELF_TABLE/FRIDGE_TABLE 建该区货架（复用 syncLayout 的建造循环）、zone==='B' 建 R2 / zone==='C' 建 R3 / zone==='W' 建存储架（T4 接）
12. `syncLayout()` 分区化：遍历 SHELF_TABLE/FRIDGE_TABLE，`G.state.zones[row.zone]`（core/A 恒真）且未建造 → `buildRack('shelf'+idx, row.x, row.z, isFridge, row.aisleZ)`。**建造去重**：rack 记 `row._built = true`（模块内），或维护 `builtRacks` 计数表——实施者选简单可靠的一种并在报告说明。冷藏仍受生鲜许可门槛（`licenses.indexOf('生鲜')!==-1`）叠加约束

### buildRack 地基改造（两处，spec §2.1）

```js
  function buildRack(idPrefix, centerX, centerZ, isFridge, aisleZ) {
    ...
    var facing = (aisleZ > centerZ) ? 1 : -1;   // 朝向本行的走廊
    ...
    var aisleSpot = new THREE.Vector3(centerX, 0, aisleZ);
```

（其余函数体不动；所有既有调用点补 aisleZ 实参。）

### state.js

`G.state` 初始化加一行（`cashier: false,` 之后）：

```js
    zones: { A: true, B: false, C: false, W: false },
```

### main.js

- buildScene：`scene.fog = new THREE.Fog(0xAECBE0, 45, 140);`；阴影 `left/right/top/bottom = ∓26/±26`、`far = 60`、`mapSize.set(4096, 4096)`；相机初始 `position.set(14, 1.65, 5.3)`、`rotation.set(0, Math.PI / 2, 0)`（进门朝店内 -x）
- **断言迁移（5 处改）**：
  1. `boot.nav`（:350）改：`!!G.world.nav.entry && G.world.nav.registers.length >= 1 && G.world.nav.registers[0].queueSpots.length === 5 && G.world.nav.aisleSpots.length === 4`
  2. `checkout.stanceBehind`（:751）改：`!!st && st.pos.z > 8.2`，文案「站位 z 应 > 8.2（柜台南侧员工通道）」
  3. `world.cashierFigure` 块 `findCashierGroup`（:665）坐标改 `o.position.x === -2 && Math.abs(o.position.z - 8.55) < 0.2`（R1 站桩新位——T3 落地站桩时以实际反推值回填，本 task 先按 (-2, 8.55) 写，若 T3 实测不同步修）
  4. `world.cashierFits`（:684）包络轴换 z：`cfBox.min.z >= 8.2 && cfBox.max.z <= 9.0`（0.8m 员工通道内）且旋转判据改 `Math.abs(cf.group.rotation.y) < 1e-6 || Math.abs(cf.group.rotation.y - Math.PI) < 1e-6`（站桩面朝 -z 顾客方向：rotation.y = π）
  5. `perf.drawCallCeiling`（:883）天花板 `< 550` 暂改 `< 700`（T1 壳体+围栏增 ~30 drawable；T7 再统一定 760）
- **新增 3 条断言**（插在 `boot.state` 之后）：

```js
      /* --- C-T1 壳体与分区（新布局地基）--- */
      ck('world.shellSize', G.world.colliders.length >= 18, '大壳 collider 数 ' + G.world.colliders.length);
      var gatedOk = true, builtB = 0;
      for (var zi = 0; zi < G.world.slots.length; zi++) {
        var zs = G.world.slots[zi];
        if (zs.pos.x < -0.2 && zs.pos.z > -4 && zs.pos.z < 4) builtB++;   // B 区范围内的格位
      }
      ck('world.zoneGating', builtB === 0, '未购区域 B 不得有格位（现 ' + builtB + '）');
      ck('world.zoneState', G.state.zones && G.state.zones.A === true && G.state.zones.B === false,
        JSON.stringify(G.state.zones));
```

- [ ] **Step 1: 写失败断言**（上面 3 条 + 5 处迁移里能先行的 boot.nav/zoneState —— 迁移断言与实现同 commit，先插新 3 条跑 FAIL）
- [ ] **Step 2: 自跑自测确认 FAIL**（world.shellSize 因旧 collider 数、zoneState 因无 zones 字段）
- [ ] **Step 3: 按上述规格实现**（world.js + state.js + main.js）
- [ ] **Step 4: node --check 三文件 + 自跑自测**——**关键回归面**：`boot.scene`（开局仍 24 格 ✓）、`shop.delivery`（卸货区东移后箱子仍生成）、`world.boxShape`、`customer.queued`（单台 R1 + 旧 nav 兼容字段仍工作）、`checkout.stance*`（R1 新位置，站位断言已迁移）、`shadow.frustumCovers`（新视锥必须罩住新 collider 全集）、`cust.*`、`save.shelves`。lowfx 态也跑。
- [ ] **Step 5: 提交推送** `git add js/world.js js/state.js js/main.js`，信息 `feat(C-T1): 32×20 大壳分区布局与门控地基`

---

## Task 2：寻路（走廊节点图 + Dijkstra）

**Files:**
- Modify: `js/world.js`（nav 图生成 + findPath + 门控启用接线）
- Modify: `js/customers.js`（moveTo 换实现 + lane 偏移）
- Modify: `js/main.js`（断言）

**Interfaces:**
- Produces: `G.world.nav.findPath(from, to) -> [Vector3]`；`G.world.nav.rebuildGraph()`（buildZone 后调）；node 结构 `{p:Vector3, zone, enabled, edges:[{to,idx,w}]}`
- Consumes: T1 的 ZONE_RECTS/SHUTTERS/布局表、colliders

- [ ] **Step 1: 写失败断言（main.js，C-T1 断言块后）**

```js
      /* --- C-T2 寻路 --- */
      ck('nav.api', typeof G.world.nav.findPath === 'function', '缺 findPath');
      var p1 = G.world.nav.findPath(G.world.nav.entry, G.world.nav.aisleSpots[0]);
      ck('nav.pathFound', Array.isArray(p1) && p1.length >= 1, 'entry→A区走廊 路径点 ' + (p1 && p1.length));
      var pB = G.world.nav.findPath(G.world.nav.entry, new THREE.Vector3(-10, 0, 0));
      ck('nav.gateBlocks', Array.isArray(pB) && pB.length === 0, '未购区域 B 必须不可达（返回 []）');
      var clearOk = true;
      if (p1 && p1.length) {
        var prev = G.world.nav.entry;
        for (var pi = 0; pi < p1.length && clearOk; pi++) {
          if (G.world.nav._segBlocked(prev, p1[pi])) clearOk = false;
          prev = p1[pi];
        }
      }
      ck('nav.pathClear', clearOk, '路径各段不得穿 collider');
```

- [ ] **Step 2: 自跑自测确认 FAIL**（nav.api false）

- [ ] **Step 3: world.js 实现**

在 nav 声明区后新增（全部逐字）：

```js
  // ---- C-T2 走廊节点图（从布局表推导；Dijkstra；门控 = node.enabled）----
  var navNodes = [];
  var NAV_INFLATE = 0.45;   // 线段-AABB 判定的膨胀半径（人身半径）

  function segBlocked(a, b) {
    for (var i = 0; i < colliders.length; i++) {
      var c = colliders[i];
      if (segHitsBox(a.x, a.z, b.x, b.z,
        c.minX - NAV_INFLATE, c.maxX + NAV_INFLATE, c.minZ - NAV_INFLATE, c.maxZ + NAV_INFLATE)) return true;
    }
    return false;
  }

  /* 2D 线段与 AABB 相交（slab 法，含端点在盒内的情形） */
  function segHitsBox(x1, z1, x2, z2, minX, maxX, minZ, maxZ) {
    var dx = x2 - x1, dz = z2 - z1;
    var t0 = 0, t1 = 1;
    var p = [-dx, dx, -dz, dz];
    var q = [x1 - minX, maxX - x1, z1 - minZ, maxZ - z1];
    for (var i = 0; i < 4; i++) {
      if (Math.abs(p[i]) < 1e-9) { if (q[i] < 0) return false; continue; }
      var r = q[i] / p[i];
      if (p[i] < 0) { if (r > t1) return false; if (r > t0) t0 = r; }
      else { if (r < t0) return false; if (r < t1) t1 = r; }
    }
    return true;
  }

  function addNavNode(x, z, zone) {
    var node = { p: new THREE.Vector3(x, 0, z), zone: zone, enabled: true, edges: [] };
    navNodes.push(node);
    return node;
  }

  function zoneOfPoint(x, z) {
    var keys = ['W', 'C', 'B', 'A'];   // core 兜底
    for (var i = 0; i < keys.length; i++) {
      var r = ZONE_RECTS[keys[i]];
      if (x >= r.minX && x <= r.maxX && z >= r.minZ && z <= r.maxZ) return keys[i];
    }
    return 'core';
  }

  function rebuildGraph() {
    navNodes.length = 0;
    // 1) 固定节点：entry/exit、主通道点、卷帘门内外点、收银台 front、走廊端点
    addNavNode(nav.entry.x, nav.entry.z, 'core');
    addNavNode(nav.exit.x, nav.exit.z, 'core');
    // 前厅横向走廊（z=5.3，贯穿 core）
    [-6, -2, 2, 6, 10, 14].forEach(function (x) { addNavNode(x, 5.3, 'core'); });
    // A/B 主过道（z=0）：区界两侧 + 各 rack aisleSpot 由下面统一加
    [1, 5, 9, 13].forEach(function (x) { addNavNode(x, 0, 'A'); });
    [-14, -10, -6, -2].forEach(function (x) { addNavNode(x, 0, 'B'); });
    // 前厅↔主过道竖向连接点（A 区开口 x=14 与 x=2 两条南北通路）
    addNavNode(14, 2.6, 'A'); addNavNode(2, 2.6, 'A');
    // C 区走廊（z=-7）
    [-12, -6, 0, 6].forEach(function (x) { addNavNode(x, -7, 'C'); });
    // 卷帘门内外点
    addNavNode(-4, 4.9, 'core'); addNavNode(-4, 3.1, 'B');    // 门 B
    addNavNode(4, -3.1, 'A');   addNavNode(4, -4.9, 'C');     // 门 C
    addNavNode(-7.1, 7, 'core'); addNavNode(-8.9, 7, 'W');    // 门 W
    // rack aisleSpot（已建的进 aisleSpots；未建区域的按表预置节点，enabled 随 zone）
    for (var si = 0; si < SHELF_TABLE.length; si++) {
      var st = SHELF_TABLE[si];
      addNavNode(st.x, st.aisleZ, st.zone);
    }
    for (var fi = 0; fi < FRIDGE_TABLE.length; fi++) {
      var ft = FRIDGE_TABLE[fi];
      addNavNode(ft.x, ft.aisleZ, ft.zone);
    }
    // 收银台 front + 队尾（core）
    for (var ri = 0; ri < REGISTER_TABLE.length; ri++) {
      addNavNode(REGISTER_TABLE[ri].x, 6.8, 'core');
      addNavNode(REGISTER_TABLE[ri].x, 3.8, 'core');
    }
    // 仓库内部两点 + 卸货区两点
    addNavNode(-11.9, 7, 'W'); addNavNode(-14, 7, 'W');
    addNavNode(18, 7.7, 'core'); addNavNode(-18, 5.5, 'W');
    // 2) 启用态
    syncNavGates();
    // 3) 连边：轴对齐 + ≤10m + 无遮挡
    for (var i = 0; i < navNodes.length; i++) {
      for (var j = i + 1; j < navNodes.length; j++) {
        var a = navNodes[i], b = navNodes[j];
        var ax = Math.abs(a.p.x - b.p.x), az = Math.abs(a.p.z - b.p.z);
        if (ax > 0.05 && az > 0.05) continue;
        var d = a.p.distanceTo(b.p);
        if (d > 10 || d < 0.01) continue;
        if (segBlocked(a.p, b.p)) continue;
        a.edges.push({ idx: j, w: d });
        b.edges.push({ idx: i, w: d });
      }
    }
  }

  function syncNavGates() {
    var zones = (G.state && G.state.zones) || { A: true };
    for (var i = 0; i < navNodes.length; i++) {
      var n = navNodes[i];
      n.enabled = (n.zone === 'core') || !!zones[n.zone];
    }
  }

  function nearestNode(p) {
    var best = -1, bd = Infinity;
    for (var i = 0; i < navNodes.length; i++) {
      var n = navNodes[i];
      if (!n.enabled) continue;
      var d = n.p.distanceToSquared(p);
      if (d < bd && !segBlocked(p, n.p)) { bd = d; best = i; }
    }
    return best;
  }

  function findPath(from, to) {
    if (!navNodes.length) return [];
    var s = nearestNode(from), t = nearestNode(to);
    if (s < 0 || t < 0) return [];
    // Dijkstra（N~70，线性扫描堆足够）
    var dist = [], prev = [], done = [];
    for (var i = 0; i < navNodes.length; i++) { dist[i] = Infinity; prev[i] = -1; done[i] = false; }
    dist[s] = 0;
    for (var iter = 0; iter < navNodes.length; iter++) {
      var u = -1, ud = Infinity;
      for (var k = 0; k < navNodes.length; k++) {
        if (!done[k] && dist[k] < ud) { ud = dist[k]; u = k; }
      }
      if (u < 0 || u === t) break;
      done[u] = true;
      var edges = navNodes[u].edges;
      for (var e = 0; e < edges.length; e++) {
        var v = edges[e].idx;
        if (!navNodes[v].enabled || done[v]) continue;
        var nd = dist[u] + edges[e].w;
        if (nd < dist[v]) { dist[v] = nd; prev[v] = u; }
      }
    }
    if (dist[t] === Infinity) return [];
    var out = [];
    var cur = t;
    while (cur !== -1) { out.unshift(navNodes[cur].p.clone()); cur = prev[cur]; }
    out.push(new THREE.Vector3(to.x, 0, to.z));
    // 首节点若与起点视线通畅且距离近可省略——保持简单：不裁剪
    return out;
  }
```

接线：`init()` 里 `buildRoom(); syncLayout();` 后加 `rebuildGraph();`；`buildZone(z)` 尾部加 `syncNavGates();`（节点集固定，只需开关）+ **该区货架建造会新增 collider → 边失效**：buildZone 尾部改调 `rebuildGraph()`（全量重建，微秒级，一次性）。nav 导出：`nav.findPath = findPath; nav.rebuildGraph = rebuildGraph; nav._segBlocked = segBlocked;`（放 buildRoom 尾部 nav 赋值区）。

- [ ] **Step 4: customers.js 两处**

`moveTo`（:261）改：

```js
      moveTo: function (v) {
        var path = (G.world.nav && G.world.nav.findPath)
          ? G.world.nav.findPath(this.mesh.position, v) : null;
        this.path = (path && path.length) ? path : [new THREE.Vector3(v.x, 0, v.z)];
      },
```

（findPath 空结果回退直线——防御：正常流程不可达目标不会被选中。）

lane 偏移：spawn 的 c 对象加 `lane: G.rand(-0.35, 0.35),`；`stepMove` 在计算 `dx/dz` 前对**中途点**加法向偏移（终点不偏）：

```js
  function stepMove(c, dt) {
    if (!c.path.length) return;
    var t = c.path[0], p = c.mesh.position;
    var tx = t.x, tz = t.z;
    if (c.path.length > 1 && c.lane) {   // 中途节点沿行进法向偏移，终点/队列点不偏
      var ndx = t.x - p.x, ndz = t.z - p.z;
      var nl = Math.sqrt(ndx * ndx + ndz * ndz) || 1;
      tx += (-ndz / nl) * c.lane;
      tz += (ndx / nl) * c.lane;
    }
    var dx = tx - p.x, dz = tz - p.z;
    ...（其余与原函数一致，d/step/到点判定用 dx/dz；到点时 p.x = tx; p.z = tz;）
  }
```

- [ ] **Step 5: node --check 三文件 + 自跑自测**（重点：`customer.queued` —— 顾客现在走折线进店、绕货架到走廊点再排队，全链路必须仍绿；`nav.gateBlocks`）
- [ ] **Step 6: 提交推送** `git add js/world.js js/customers.js js/main.js`，信息 `feat(C-T2): 走廊节点图寻路与区域门控`

---

## Task 3：多收银台（checkout 重构 + 扩建购买）

**Files:**
- Modify: `js/checkout.js`（单例→registers[]，本计划最大单文件改动）
- Modify: `js/world.js`（buildZone 里建 R2/R3 已在 T1 架好；站桩 per-register）
- Modify: `js/shop.js`（buyZone）
- Modify: `js/player.js`（register/shutter 交互传参）
- Modify: `js/main.js`（断言 + serveTx/currentTx 调用点）

**Interfaces:**
- Produces: `G.checkout.enterRegister(reg)`、`activeRegisterId`、`_test.tx(i=0)/scanAll(i)/settle(i)/payCard(i)/registers()`；`G.shop.buyZone(z)`；`G.world.setCashierVisible(i, visible)`（签名扩展，旧单参兼容废除——调用点全改）
- Consumes: T1 的 `G.world.registers`/nav.registers、SHUTTERS 交互体

### checkout.js 重构映射表（每项精确到函数）

模块状态：删 `queue/tx/frame/stance`；新增：

```js
  var registers = [];      // [{ref, queue:[], tx:null, frame:null, stance:null}]，与 G.world.registers 对齐
  var activeReg = null;    // 玩家当前占用台（对象引用）
```

`ensureRegisters()`（update 开头调）：`G.world.registers` 比 `registers` 长则补 `{ref: wreg, queue: [], tx: null, frame: null, stance: null}`。

| 函数 | 改法 |
|---|---|
| `beltFrame()` | 改 `frameOf(reg)`：`if (reg.frame) return reg.frame;` 用 `reg.ref.beltMesh` 的 Box3 + `reg.ref.front` 算（原函数体的 surf 换 `reg.ref.beltMesh`，front 换 `reg.ref.front`，behind 换 `reg.ref.queueSpots[0]`）；**删除 findBeltMesh/findRegisterMesh 两个函数** |
| `beltPos(i)/scannedPos(k)` | 加 reg 参：`beltPos(reg, i)`，内部 `frameOf(reg)` |
| `queueCap()` | `queueCap(reg)`：`reg.ref.queueSpots.length \|\| 5` |
| `joinQueue(c)` | 选最短队：遍历 registers 取 `queue.length` 最小（并列取低 index）；满（全部 ≥cap）返回 false；`c._reg = reg` 记归属；`retarget(reg)` |
| `retarget()/prune()` | 加 reg 参逐台调；`retarget` 里 `nav.registerFront/queueSpots` 换 `reg.ref.front/queueSpots`；`prune` 的 tx 清理只碰 `reg.tx` |
| `stepTx(dt)` | `stepTx(reg, dt)`；`tx` 全换 `reg.tx`；雇员分支：`if (reg.tx.phase === 'ready' && staffedOf(reg) && activeReg !== reg)`（`staffedOf(reg) = G.state.registers[reg.ref.index].staffed`）；`autoComplete(reg)` |
| `addBeltItem` | `addBeltItem(reg, pid, price)`；mesh.userData 加 `{registerId: reg.ref.index, txId: reg.tx.id}`（tx 创建时 `id: ++txSeq`） |
| `tryScan(mesh)` | 用 `activeReg`；命中后核验 `mesh.userData.registerId === activeReg.ref.index && mesh.userData.txId === activeReg.tx.id` |
| mousedown 射线 | `tx` 换 `activeReg && activeReg.tx`；meshes 从 `activeReg.tx.items` 收集 |
| `completeSale(pay, xp)` | `completeSale(reg, pay, xp)`；全部 `tx`→`reg.tx`、`queue`→`reg.queue`；`retarget(reg)` |
| `onSettle/onConfirmChange/autoComplete` | 加 reg 参（POS 面板只服务 activeReg：`onSettle()` 无参内部取 activeReg；`autoComplete(reg)` 显式） |
| `enterRegister()` | `enterRegister(reg)`：`if (activeReg) { if (activeReg !== reg) G.ui.toast('先退出当前收银台', 'warn'); return; }`；stance 用 `computeStance(reg)`（内部 frameOf(reg)/itemFieldCenter(reg)）；站桩隐藏改 `G.world.setCashierVisible(reg.ref.index, false)`（条件 `staffedOf(reg)`，记 `reg._cashierHidden = true`） |
| `exitRegister()` | finish 里恢复 `if (reg._cashierHidden) { G.world.setCashierVisible(reg.ref.index, true); reg._cashierHidden = false; }`；`activeReg = null` 在 finish 中置空（进入时置 `activeReg = reg`） |
| `update(dt)` | `ensureRegisters(); for 每 reg { prune(reg); retarget(reg); 队首就位则开 tx（reg.tx 版）; if (reg.tx) stepTx(reg, dt); }` 然后 `stepSlides(dt)`（内部改遍历 activeReg 的 items——slide 只发生在玩家扫码台）、`stepTween(dt)`、scanCd、renderPos |
| `renderPos()` | 数据源 `activeReg.tx` |
| `_test` | `tx(i)`→`registers[i\|\|0].tx`；`scanAll(i)/settle(i)/payCard(i)` 同理（内部临时把 activeReg 语义放开：scanAll 直接对 `registers[i].tx.items` 逐件走 tryScan 的核心逻辑——实施者提一个 `scanItem(reg, item)` 内部函数供两处复用）；新增 `registers: function(){ return registers; }`、`frame: function(i){ return frameOf(registers[i \|\| 0]); }` |
| 导出 | `activeRegisterId: null`（enter/exit 维护）；`stance` 语义不变（activeReg 的） |

### world.js：站桩 per-register

`buildCashierFigure()` 加参 `buildCashierFigure(reg)`：位置 `(reg.front.x, 0, 8.55)`、`rotation.y = Math.PI`（面朝 -z 顾客方向）；`setCashierVisible(i, visible)`：`cashierGroups[i]` 数组管理（建造/挂载/移除逐台）。调用点全改：main.js `startGame` 的同步、`onCashierChanged`（payload 带 index）。

### shop.js：buyZone + hireCashier(i)

```js
  function buyZone(z) {
    var def = null;
    // SHUTTERS 定义在 world——shop 侧用 G.data.CONFIG.zonePrices = {B:1500,C:3200,W:900} 与 zoneLevels = {B:5,C:8,W:3}（T5 入 CONFIG；本 task 先以字面量对象写在 shop.js 顶部，T5 迁移）
    var prices = { B: 1500, C: 3200, W: 900 }, lvs = { B: 5, C: 8, W: 3 };
    if (!(z in prices)) return false;
    if (G.state.zones[z]) return false;
    if (G.state.level < lvs[z]) return false;
    if (G.state.money < prices[z]) return false;
    G.addMoney(-prices[z], 'zone');
    G.state.zones[z] = true;
    G.world.buildZone(z);
    G.bus.emit('zone', { zone: z });
    return true;
  }
```

`hireCashier(i)/fireCashier(i)`：门槛 `G.state.level >= 10`、`G.state.registers[i].owned`（owned = 该台已建造——T1/T3 在 buildRegister 时置 `G.state.registers[i].owned = true`；state.js 初始化 `registers: [{owned:false,staffed:false},×3]`，R1 在 init 后置 owned）、未 staffed、钱够；成功 `G.state.registers[i].staffed = true; G.bus.emit('cashier', {hired:true, index:i});`。**`G.state.cashier` 布尔删除**（state.js 初始化去掉；读取点全改：checkout staffedOf、main.js :180/:250、ui.js 雇佣按钮）。

### player.js：交互分支

- `type === 'register'`：`G.checkout.enterRegister(entry.data.register 对应的 checkout reg)`——checkout 导出 `enterRegisterByIndex(i)` 更简单：player 调 `G.checkout.enterRegisterByIndex(entry.data.register.index)`
- `type === 'shutter'`：`computePrompt` 生成 `[E] 开放{label}（¥{price} · 需 Lv{lv}）`，不满足显示红字原因（钱不够/级不够/已开放）；按 E 调 `G.shop.buyZone(zone)`，失败 toast

### main.js

- `currentTx()` 改 `G.checkout._test.tx(0)`；`serveTx()` 改遍历 `_test.registers()` 对每台 ready 的 tx 走 scanAll(i)/settle(i)/payCard(i)
- **新增断言**（C-T2 块后）：

```js
      /* --- C-T3 多收银台 --- */
      var regs = G.checkout._test.registers();
      var f0 = G.checkout._test.frame(0);
      ck('checkout.perRegisterFrame', regs.length >= 1 && !!f0 &&
        Math.abs(f0.origin.x - G.world.registers[0].front.x) < 1.2 &&
        Math.abs(f0.origin.z - 7.6) < 1.2,
        'R1 frame.origin 必须落在自家柜台（' + (f0 && f0.origin.x.toFixed(2)) + ',' + (f0 && f0.origin.z.toFixed(2)) + '）');
      ck('checkout.shortestQueue', typeof G.checkout.joinQueue === 'function', 'joinQueue 存在（多台分流逻辑生效）');
      var bzBefore = G.state.money;
      ck('shop.buyZoneGates', G.shop.buyZone('B') === false && G.state.money === bzBefore,
        'Lv1 买区域 B 必须被等级门槛拒绝且不扣钱');
```

（多台并行交易的强断言 `checkout.parallelTx` 需要 B/C 已开——放 T7 满配场景里：`G.state.level=10; buyZone 全开; 两台各塞队列 …`；本 task 的断言以「单台重构后语义不变 + 门槛正确」为界。）

- [ ] **Step 1-2: 断言先行 + FAIL 确认**（shop.buyZoneGates 因 buyZone 不存在 FAIL）
- [ ] **Step 3: 按映射表实施 checkout.js 重构**（一次到位，函数逐个过）
- [ ] **Step 4: world/shop/player/main 接线**
- [ ] **Step 5: node --check 五文件 + 两态自测**——**最高风险回归面**：`checkout.stance*` 六条、`checkout.scan/settle/sale/money/xp/dayStats`、`customer.queued`、`world.cashierFigure/Fits`（站桩新逻辑）、`summary.*`。任何 FAIL 原文报回。
- [ ] **Step 6: 提交推送** `git add js/checkout.js js/world.js js/shop.js js/player.js js/main.js`，信息 `feat(C-T3): 收银台 per-register 化与区域扩建购买`

---

## Task 4：仓库存储架与卸货区上限

**Files:**
- Modify: `js/world.js`（storageSlots 建造 + storeBox/takeBox + spawnBox(pid, atPos) + freeYardSlot null 语义）
- Modify: `js/shop.js`（延迟送达）
- Modify: `js/player.js`（storage 交互分支）
- Modify: `js/main.js`（断言）

**Interfaces:**
- Produces: `G.world.storageSlots`、`storeBox(slot, box)->bool`、`takeBox(slot)->box|null`、`serializeStorage()/restoreStorage(data)`、`spawnBox(pid, atPos?)`（满则 null）
- Consumes: T1 的 STORAGE_TABLE/activeYardPositions、T3 的 buildZone('W') 挂点

- [ ] **Step 1: 写失败断言（main.js，C-T3 块后）**

```js
      /* --- C-T4 仓库 --- */
      ck('world.storageApi', typeof G.world.storeBox === 'function' && typeof G.world.takeBox === 'function',
        '缺 storeBox/takeBox');
      // 直接开仓测试（绕过钱：手动置区+建造——T6 前 zones 尚不入存档，安全）
      G.state.zones.W = true;
      G.world.buildZone('W');
      ck('world.storageBuilt', (G.world.storageSlots || []).length === 24,
        '存储位 ' + (G.world.storageSlots || []).length + '（应 24）');
      var stBox = G.world.spawnBox('f_noodle');
      var stSlot = G.world.storageSlots[0];
      var stOk = G.world.storeBox(stSlot, stBox);
      ck('world.storageStore', stOk === true && stSlot.box === stBox &&
        Math.abs(stBox.mesh.position.x - stSlot.pos.x) < 0.01, '放箱入位');
      var back = G.world.takeBox(stSlot);
      ck('world.storageTake', back === stBox && stSlot.box === null, '取箱出位');
      // 序列化往返
      G.world.storeBox(stSlot, stBox);
      var stData = G.world.serializeStorage();
      ck('world.storageRoundtrip', Array.isArray(stData) && stData.length === 1 &&
        stData[0].id === stSlot.id && stData[0].pid === 'f_noodle', JSON.stringify(stData));
      G.world.takeBox(stSlot);   // 清场；stBox 留在场上由后续断言消耗或无害
      // 卸货区满 → spawnBox 返回 null
      var spawned = 0, lastSpawn = true;
      for (var yb = 0; yb < 14 && lastSpawn; yb++) {
        lastSpawn = !!G.world.spawnBox('d_water');
        if (lastSpawn) spawned++;
      }
      ck('world.freeYardSlotNull', lastSpawn === false && spawned <= 12,
        '卸货区满后 spawnBox 必须返回 null（本轮成功 ' + spawned + ' 箱，含前序残留合计 ≤12 位）');
```

（stBox 与 spawned 的箱子会计入后续 perf 场景——T7 的满配灌店本就要 24 箱，兼容；`world.boxShape` 等早期断言在此块**之前**已执行不受影响。）

- [ ] **Step 2: FAIL 确认**（world.storageApi）

- [ ] **Step 3: world.js 实现**

```js
  // ---- C-T4 仓库存储架 ----
  var storageSlots = [];

  function buildStorage() {
    if (storageSlots.length) return;
    // 两条金属存储架横梁（纯视觉）+ 24 个地面位标记
    for (var i = 0; i < STORAGE_TABLE.length; i++) {
      var p = STORAGE_TABLE[i];
      var marker = new THREE.Mesh(new THREE.BoxGeometry(0.6, 0.02, 0.6),
        flatMat(G.tex.on ? 0xFFFFFF : 0x8C9AA6, G.tex.on ? { map: G.tex.shelfMetal(1, 1) } : null));
      marker.position.set(p.x, 0.01, p.z);
      addMesh(marker, false, true);
      var slot = { id: 'st' + i, pos: new THREE.Vector3(p.x, 0, p.z), marker: marker, box: null };
      storageSlots.push(slot);
      registerInteractable(marker, { type: 'storage', data: { slot: slot }, prompt: '' });
    }
  }

  function storeBox(slot, box) {
    if (!slot || slot.box || !box || !box.mesh) return false;
    slot.box = box;
    box.mesh.position.set(slot.pos.x, 0.225, slot.pos.z);
    box.mesh.rotation.set(0, 0, 0);
    // 重新注册为可交互箱（玩家举起时已被 removeInteractable；放回存储位即恢复）
    var prod = G.data.productById(box.productId);
    registerInteractable(box.mesh, {
      type: 'box', data: { box: box },
      prompt: '[E] 搬起纸箱（' + (prod ? prod.name : box.productId) + ' ×' + box.itemsLeft + '）'
    });
    return true;
  }

  function takeBox(slot) {
    if (!slot || !slot.box) return null;
    var box = slot.box;
    slot.box = null;
    return box;
  }

  function serializeStorage() {
    var out = [];
    for (var i = 0; i < storageSlots.length; i++) {
      var s = storageSlots[i];
      if (s.box) out.push({ id: s.id, pid: s.box.productId, left: s.box.itemsLeft });
    }
    return out;
  }

  function restoreStorage(data) {
    if (!Array.isArray(data)) return;
    for (var i = 0; i < data.length; i++) {
      var d = data[i];
      var slot = null;
      for (var j = 0; j < storageSlots.length; j++) { if (storageSlots[j].id === d.id) { slot = storageSlots[j]; break; } }
      if (!slot || slot.box) continue;
      var box = spawnBox(d.pid, slot.pos);
      if (!box) continue;
      box.itemsLeft = d.left || 0;
      updateBoxVisual(box);
      slot.box = box;
    }
  }
```

- `buildZone('W')` 内挂 `buildStorage()`（T3 已留挂点）
- `spawnBox(pid, atPos)`：签名加第二参；`var slotPos = atPos || freeYardSlot(); if (!slotPos) return null;`
- `freeYardSlot()`：末行 `return YARD_POSITIONS[0];` 改 `return null;`（引用已是 activeYardPositions()）
- 存储位上的箱被玩家搬走时（player pickUpBox 走 removeInteractable）需清 `slot.box`——`takeBox` 由交互层调；**直接对箱按 E 搬起的路径**：pickUpBox 前查 `storageSlots` 中 `box === entry.data.box` 的位并清空（world 导出 `releaseStorageOf(box)` 一行函数，player 在 pickUpBox 里调）
- 导出：`storageSlots/storeBox/takeBox/serializeStorage/restoreStorage/releaseStorageOf`

- [ ] **Step 4: shop.js 延迟送达**

`update` 的 `entry.remaining <= 0` 分支改：

```js
      if (entry.remaining <= 0) {
        var box = G.world.spawnBox(entry.pid);
        if (box) {
          G.bus.emit('delivery', { productId: entry.pid });
        } else {
          entry.remaining = 2;   // 卸货区满：2 秒后重试
          if (!yardFullToasted) { G.ui.toast('卸货区已满，配送车在门外等待', 'warn'); yardFullToasted = true; }
          remaining.push(entry);
        }
      } else { remaining.push(entry); }
```

（模块级 `var yardFullToasted = false;`，每日 `dayStart` 重置——shop 监听 `'dayStart'` 或在 orderBoxes 成功时清零，实施者取后者。）

- [ ] **Step 5: player.js storage 分支**

`computePrompt`：`type === 'storage'` → 持箱且位空 `[E] 放入纸箱`；空手且位占 走不到（占位时 marker 被箱体遮挡，箱子自身是 box 交互）；持箱位占 `此位已占用`（红字）。
`doInteractions`：持箱对 storage 且位空 → `if (G.world.storeBox(entry.data.slot, carrying)) { G.player.carrying = null; }`（比照 putDownBox 的收尾：清 hover）。
`pickUpBox`：起手处加 `if (G.world.releaseStorageOf) G.world.releaseStorageOf(box);`

- [ ] **Step 6: node --check 四文件 + 两态自测**（重点：`shop.delivery`——卸货区仍东侧且未满时行为不变；`world.freeYardSlotNull`；`world.storageRoundtrip`）
- [ ] **Step 7: 提交推送** `git add js/world.js js/shop.js js/player.js js/main.js`，信息 `feat(C-T4): 仓库存储架与卸货区上限、延迟送达`

---

## Task 5：等级/经济重平衡

**Files:**
- Modify: `js/data.js`（LEVELS 换表 + CONFIG 加 zonePrices/zoneLevels/rentPerZone）
- Modify: `js/customers.js`（spawnInterval/concurrentCap 公式）
- Modify: `js/shop.js`（buyZone 改读 CONFIG；hireCashier 门槛敘述同步）
- Modify: `js/main.js`（endDay 房租分区 + 断言）
- Modify: `js/ui.js`（许可页雇佣按钮按台——最小改动：三个按钮或下拉，实施者按现有 UI 结构最小实现，文案「雇佣收银台② 收银员」）

**Interfaces:**
- Produces: `CONFIG.zonePrices={B:1500,C:3200,W:900}`、`CONFIG.zoneLevels={B:5,C:8,W:3}`、`CONFIG.rentPerZone={W:20,B:30,C:40}`；LEVELS 新表；`zonesOpenCount()`（customers 内部）
- Consumes: T3 buyZone/hireCashier(i)

- [ ] **Step 1: 写失败断言（main.js，C-T4 块后）**

```js
      /* --- C-T5 经济 --- */
      ck('level.tableV2', G.data.LEVELS.length === 10 &&
        G.data.CONFIG.zonePrices && G.data.CONFIG.zonePrices.B === 1500 &&
        G.data.CONFIG.rentPerZone && G.data.CONFIG.rentPerZone.C === 40,
        'LEVELS/zonePrices/rentPerZone 新表');
```

`summary.rent` 迁移预告：开局仅 A 区 4 架，`expRent = 40 + 4*2 = 48` **不变**（W 在自测 C-T4 块被打开——**注意**：T4 断言把 zones.W 置 true 了！日结在其后 → 房租会 +20。**修正 summary.rent 期望**：`expRent = 40 + 20 /*W(自测中开)*/ + 4 * 2`，并在注释说明。实施者落地时同步改 :824 的 expRent 行与注释。）

- [ ] **Step 2: FAIL 确认**（level.tableV2）

- [ ] **Step 3: data.js 换表**

LEVELS 10 行按 spec §6 表逐字重写（`unlock` 文案含 ★ 项；机器字段：Lv3 `warehouseAvailable:true`、Lv5 `zoneB:true`、Lv7 `maxBoxesPerOrder:12`、Lv8 `zoneC:true`、Lv10 `cashierAvailable:true`；**删除** `shelfGroups` 与 `expansion` 字段——货架随区域而非等级）。CONFIG：删 `rentPerDay` 的 Lv8 语义注释，加：

```js
    zonePrices: { B: 1500, C: 3200, W: 900 },
    zoneLevels: { B: 5, C: 8, W: 3 },
    rentPerZone: { W: 20, B: 30, C: 40 },
```

- [ ] **Step 4: customers.js 公式**

```js
  function zonesOpenCount() {
    var z = (G.state && G.state.zones) || {};
    return ((z.B ? 1 : 0) + (z.C ? 1 : 0));   // zonesOpen = 已开购物区数 − 1（A 恒开）
  }
  function spawnInterval() {
    var base = num(cfg().spawnIntervalBase, 20);
    var day = num(G.state && G.state.day, 1);
    var lv = num(G.state && G.state.level, 1);
    return G.clamp(base - 0.8 * (day - 1) - 1.2 * (lv - 1) - 2.0 * zonesOpenCount(), 3.0, 20) * G.rand(0.75, 1.25);
  }
  function concurrentCap() {
    var lv = num(G.state && G.state.level, 1);
    return G.clamp(4 + lv + 3 * zonesOpenCount(), 5, 20);
  }
```

- [ ] **Step 5: main.js endDay 房租**

```js
    var rentZ = cfg.rentPerZone || {};
    var rent = cfg.rentPerDay
      + (G.state.zones.W ? rentZ.W : 0) + (G.state.zones.B ? rentZ.B : 0) + (G.state.zones.C ? rentZ.C : 0)
      + racks.shelfGroups * cfg.utilPerShelf
      + racks.fridges * cfg.utilPerFridge
      + staffedWage();
    // staffedWage(): G.state.registers 里 staffed 计数 × cfg.cashierWage（main 内一行工具）
```

同步 `summary.rent` 期望值（见 Step 1 注）。shop.buyZone 改读 `CONFIG.zonePrices/zoneLevels`（删 T3 的字面量对象）。

- [ ] **Step 6: ui.js 雇佣按台**（最小改动：现雇佣按钮区域改为逐台一行「收银台① [雇佣/解雇]」，仅显示 owned 的台；调 `G.shop.hireCashier(i)`）

- [ ] **Step 7: node --check 六文件 + 两态自测**（重点：`summary.rent/rentCharged` 新期望、`boot.state`、`shop.*`、`customer.queued` 在新 spawn 公式下仍触发）
- [ ] **Step 8: 提交推送**，信息 `feat(C-T5): 等级经济重平衡——分区房租与新节奏公式`

---

## Task 6：存档 v2 与迁移

**Files:**
- Modify: `js/state.js`（save/load/resetSave/migrateV1）
- Modify: `js/main.js`（迁移 toast + 断言）

**Interfaces:**
- Produces: `gss-save-v2` 结构（spec §7）；`G.state._migrated`
- Consumes: T4 serializeStorage/restoreStorage、T1 zones、T3 registers

- [ ] **Step 1: 写失败断言（main.js，save.shelves 断言之后插）**

```js
      /* --- C-T6 存档 v2 --- */
      ck('save.v2Key', !!localStorage.getItem('gss-save-v2'), 'nextDay 后应存在 v2 键');
      var v2raw = JSON.parse(localStorage.getItem('gss-save-v2') || '{}');
      ck('save.v2Shape', v2raw.v === 2 && v2raw.zones && Array.isArray(v2raw.registers) && Array.isArray(v2raw.shelves),
        'v2 结构字段');
      // v1 迁移：构造最小 v1 档 → 清 v2 → load → 折现与继承
      var fakeV1 = { money: 500, day: 3, xp: 200, level: 3, prices: G.state.prices,
        licenses: ['食品', '饮料'], cashier: true, negDays: 1,
        shelves: [{ id: 'shelf0_0_0', productId: 'f_noodle', count: 5 }] };
      localStorage.setItem('gss-save-v1', JSON.stringify(fakeV1));
      localStorage.removeItem('gss-save-v2');
      var migOk = G.load();
      var refund = 5 * 1.20;   // 5 × f_noodle.cost
      ck('save.migrateV1', migOk === true && G.state.day === 3 && G.state.level === 3 &&
        Math.abs(G.state.money - (500 + refund)) < 0.01 &&
        G.state.registers[0].staffed === true && G.state.zones.B === false &&
        !!localStorage.getItem('gss-save-v1'),
        '迁移：money=' + G.state.money + '（期 ' + (500 + refund) + '），v1 键保留');
      ck('save.roundtripZones', (function () {
        G.state.zones.W = true; G.save();
        var rr = JSON.parse(localStorage.getItem('gss-save-v2'));
        return rr.zones.W === true;
      })(), 'zones 入档往返');
```

（此块脏化 state——位于 save.shelves 之后、perf 闸门之前，与既有「闸门会脏化」的布局一致；migrate 后 `G.state.zones` 被重置为仅 A——后续 perf 闸门自己会全开区域，兼容。）

- [ ] **Step 2: FAIL 确认**（save.v2Key）

- [ ] **Step 3: state.js 实现**

- `SAVE_KEY = 'gss-save-v2'`、`SAVE_KEY_V1 = 'gss-save-v1'`
- `G.save()`：data 加 `v:2, zones: G.state.zones, registers: G.state.registers, storage: (G.world.serializeStorage ? G.world.serializeStorage() : [])`；删 `cashier` 字段
- `G.load()`：

```js
  G.load = function () {
    try {
      var raw = localStorage.getItem(SAVE_KEY);
      var data = raw ? JSON.parse(raw) : null;
      var migrated = null;
      if (!data) {
        var v1raw = localStorage.getItem(SAVE_KEY_V1);
        if (!v1raw) return false;
        data = migrateV1(JSON.parse(v1raw));
        migrated = data._refund;
      }
      // 字段级降级
      G.state.money = (typeof data.money === 'number') ? data.money : G.data.CONFIG.startMoney;
      G.state.day = data.day || 1;
      G.state.xp = data.xp || 0;
      G.state.level = G.clamp(data.level || 1, 1, 10);
      G.state.prices = sanePrices(data.prices);
      G.state.licenses = saneLicenses(data.licenses);
      G.state.negDays = data.negDays || 0;
      G.state.zones = data.zones && typeof data.zones === 'object'
        ? { A: true, B: !!data.zones.B, C: !!data.zones.C, W: !!data.zones.W }
        : { A: true, B: false, C: false, W: false };
      G.state.registers = saneRegisters(data.registers);
      if (migrated != null) G.state._migrated = { refund: migrated };
      if (G.world && G.world.syncLayout) {
        if (G.state.zones.W && G.world.buildZone) G.world.buildZone('W');
        if (G.state.zones.B && G.world.buildZone) G.world.buildZone('B');
        if (G.state.zones.C && G.world.buildZone) G.world.buildZone('C');
        G.world.syncLayout();
      }
      if (G.world && G.world.restoreShelves && data.shelves) G.world.restoreShelves(data.shelves);
      if (G.world && G.world.restoreStorage && data.storage) G.world.restoreStorage(data.storage);
      return true;
    } catch (e) { return false; }
  };
```

- `migrateV1(v1)`：

```js
  function migrateV1(v1) {
    var refund = 0;
    if (Array.isArray(v1.shelves)) {
      v1.shelves.forEach(function (s) {
        var p = s && s.productId ? G.data.productById(s.productId) : null;
        if (p && s.count > 0) refund += s.count * p.cost;
      });
    }
    refund = Math.round(refund * 10) / 10;   // MIGRATION_REFUND_RATE = 1.0（全额按进价）
    return {
      v: 2, money: (v1.money || 0) + refund, day: v1.day, xp: v1.xp, level: v1.level,
      prices: v1.prices, licenses: v1.licenses, negDays: v1.negDays,
      zones: { A: true, B: false, C: false, W: false },
      registers: [{ owned: true, staffed: !!v1.cashier }, { owned: false, staffed: false }, { owned: false, staffed: false }],
      shelves: null, storage: [], _refund: refund
    };
  }
```

- `sanePrices/saneLicenses/saneRegisters` 三个小校验函数（prices 过滤未知 pid 与非正数回退 `round1(market×1.2)`；licenses 求交并保底 ['食品','饮料']；registers 非法回默认三元组且 [0].owned=true）
- `resetSave` 双键删除

- [ ] **Step 4: main.js 迁移 toast**（startGame 内检 `G.state._migrated` → toast「存档已升级：店铺按新布局重建，原货架库存 ¥X 已按进价折现入余额」→ 清标记）

- [ ] **Step 5: node --check 两文件 + 两态自测**（重点：save.* 全组、日结后 v2 写入、`onContinueGame` 路径）
- [ ] **Step 6: 提交推送** `git add js/state.js js/main.js`，信息 `feat(C-T6): 存档 gss-save-v2 与 v1 折现迁移`

---

## Task 7：性能收尾（TAG_TEX 治漏 + 手持上限 + 满配闸门 760）

**Files:**
- Modify: `js/world.js`（TAG_TEX 缓存键与 dispose——该缓存在 world.js 的 tagTexture，非 textures.js）
- Modify: `js/customers.js`（addHandCube 视觉上限 6）
- Modify: `js/main.js`（perf 闸门满配化 + 断言）

**Interfaces:** Produces `perf.drawCallCeiling < 760` 满配版；`world.tagTexNoLeak`；`cust.handVisualCap`

- [ ] **Step 1: 写失败断言（main.js，C-T6 块后、perf 闸门前）**

```js
      /* --- C-T7 治漏与上限 --- */
      for (var pi2 = 0; pi2 < 200; pi2++) G.shop.setPrice('f_noodle', 2.0 + (pi2 % 30) * 0.1);
      var ts = G.world._tagStats();
      var texCountAfter = 0, tk;
      for (tk in ts.cache) texCountAfter++;
      ck('world.tagTexNoLeak', texCountAfter <= 72 && ts.disposed > 0,
        '改价 200 次后缓存 ' + texCountAfter + '（≤72），disposed=' + ts.disposed);
      G.shop.setPrice('f_noodle', 2.2);
      var capC = G.customers._test.spawnOne();
      for (var hc = 0; hc < 12; hc++) G.customers._test.addHand(capC, 'f_noodle');
      var handMeshes = capC.hands.children.length;
      ck('cust.handVisualCap', handMeshes === 6, '12 件手持只渲染 ' + handMeshes + '（应 6）');
      G.customers._test.remove(capC);
```

（需要 `_test.addHand(c, pid)` 钩子——本 task 在 customers._test 加一行 `addHand: function (c, pid) { addHandCube(c, pid); },`；`G.world._tagTexCache/_tagTexDisposed` 为治漏实现的自测导出。）

- [ ] **Step 2: FAIL 确认**

- [ ] **Step 3: world.js TAG_TEX 治漏**

`tagTexture(state, product, price)` 改造：
- 缓存键 `state + '|' + (product ? product.id : '-')`（**价格不入键**）
- stocked/out 贴图上画价格 → 同 pid 改价时需重绘：`updateSlotTag` 里对 stocked 态直接查缓存条目的 `_price` 标记，价不同则 `dispose()` 旧 texture、删条目、重建；模块级 `var tagTexDisposed = 0;` 每次 dispose ++
- 导出 `_tagStats: function () { return { cache: TAG_TEX, disposed: tagTexDisposed }; }`（自测钩子；Step 1 断言即按此 API 写）

- [ ] **Step 4: customers.js 手持上限**

`addHandCube` 开头：`if (c.hands.children.length >= 6) { layoutHands(c); return; }`（逻辑 items 由调用方维护，不受影响）。`_test` 加 `addHand`。

- [ ] **Step 5: main.js 闸门满配化**

perf 块的场景搭建改为（替换现 level/licenses/syncLayout/灌店/箱/顾客段）：

```js
      G.state.level = 10;
      G.state.licenses = ['食品', '饮料', '日用品', '生鲜'];
      G.state.zones.W = true; G.state.zones.B = true; G.state.zones.C = true;
      G.world.buildZone('W'); G.world.buildZone('B'); G.world.buildZone('C');
      G.world.syncLayout();
      G.world.setCashierVisible(0, true); G.world.setCashierVisible(1, true); G.world.setCashierVisible(2, true);
      /* 轮转灌店 120 格 */
      var fillGuard = 0, filled = true;
      while (filled && fillGuard < 20000) {
        filled = false;
        for (var fpi = 0; fpi < G.data.PRODUCTS.length; fpi++) {
          var fpid = G.data.PRODUCTS[fpi].id;
          var fSlot = G.world.findEmptyOrMatchingSlot(fpid);
          if (fSlot && G.world.addItem(fSlot, fpid)) { filled = true; fillGuard++; }
        }
      }
      /* 24 仓库箱入位 + 卸货区满 12 箱 + 20 顾客 ×12 手持（渲染 6） */
      for (var sbi = 0; sbi < 24; sbi++) {
        var sb = G.world.spawnBox(G.data.PRODUCTS[sbi % 24].id, G.world.storageSlots[sbi].pos);
        if (sb) G.world.storageSlots[sbi].box = sb;
      }
      for (var ybi = 0; ybi < 12; ybi++) G.world.spawnBox(G.data.PRODUCTS[ybi % 24].id);
      for (var ci = 0; ci < 20; ci++) {
        var pc = G.customers._test.spawnOne();
        for (var hi = 0; hi < 12; hi++) G.customers._test.addHand(pc, G.data.PRODUCTS[hi % 24].id);
      }
      var drawN = countDrawables(scene);
      ck('perf.drawCallCeiling', drawN < 760, '满配（120 格/3 台/仓库/20 客）drawable=' + drawN + '，天花板 760');
      /* 多台并行断言（此时 3 台在场）*/
      ck('checkout.threeRegisters', G.world.registers.length === 3 &&
        G.checkout._test.registers().length === 3, '三台就位');
```

（T1 曾把天花板临时放 700——本步统一定 760 并更新 detail；若实测 ≥760 写报告 BLOCKED 附分项，不自行抬。）

- [ ] **Step 6: node --check 三文件 + 两态自测 + 提交推送**，信息 `feat(C-T7): TAG_TEX 治漏、手持渲染上限与满配闸门 760`

---

## Task 8：校准回写与收官

**Files:** Modify `DESIGN.md`、`docs/GDD.md`、`docs/CONTRACTS.md`、（如有）`js/main.js` 文档性字串

- [ ] **Step 1: 全量两态自测**，记录实测 drawN、断言总数
- [ ] **Step 2: 文档校准**——DESIGN §5.8 满配实测回写（drawN/闸门/分项）；§5.10 实际参数；GDD §4 格位表与实测一致性；CONTRACTS 残差（T3 后 nav.registerFront/queueSpots 是否已删、_test 签名、buildZone/spawnBox 签名与实现一致）；写实际值不写计划值
- [ ] **Step 3: 收官提交推送**，信息 `docs(C-T8): 大卖场校准回写，子项目 C 收官`
- [ ] **Step 4: 人工验收清单**（真机）：扩建三连购动线（卷帘门提示/扣款/开门/新区营业）、三台并行收银与接管切换、顾客绕货架寻路观感、仓库囤货-补货循环、卸货区满仓提示、v1 旧档迁移弹提示、20 人满场帧率、lowfx 回退。可复用 CDP 截图管线机器预验。

---

## 附：spec 覆盖对照

| spec 章节 | 对应 task |
|---|---|
| §1 盲测裁定 10 处 | 全计划贯彻（frame per-reg=T3；Dijkstra+lane=T2；R2/R3 随区=T1/T3；仓库 Lv3=T5 表；拒收+延迟=T4；无图集=T7 预算；手持 6=T7；全额折现=T6；日结不改=T3；断言最小迁移=各 task） |
| §2 布局/卷帘门/视觉配套 | T1 |
| §3 多台 | T3 |
| §4 寻路 | T2 |
| §5 仓库 | T4 |
| §6 等级经济 | T5（+T0 GDD） |
| §7 存档 v2 | T6 |
| §8 性能 | T1（视锥）+T7（治漏/上限/闸门） |
| §9 验收断言 | 各 task Step 1 + T7/T8 |
| §10 施工顺序 | 任务结构本身 |
| §11 明确不做 | 全计划未触及员工寻路/互避/图集/电脑扩建页/完整迁移/预留槽 |

**断言迁移总账**：改 5（boot.nav/stanceBehind/cashierFigure/cashierFits/perf 天花板）+ 调用点 2（currentTx/serveTx）+ summary.rent 期望值 1；删 0；新增 ~24（T1×3/T2×4/T3×3/T4×6/T5×1/T6×4/T7×3+threeRegisters）。终态 ~110 条。


