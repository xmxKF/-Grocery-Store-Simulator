# 物理与投掷（子项目 D）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** vendor cannon.js 0.6.2 刚体引擎，让纸箱变成真刚体：可蓄力投掷、翻滚、撞墙、砸倒顾客、被顾客局部绕行，朝向入档。**零玩法后果**——不扣钱、不降满意度、不算损耗、不触发投诉或罚款。

**Architecture:** 新模块 `js/physics.js` → `G.physics` 独占 cannon world；**静态刚体全量从 `G.world.colliders` 自动生成**（`syncStatics()`，绝不手写第二份几何表），collider 新增纯加法的高度字段 `h`；箱三态（held / slotted / loose）由「`G.player.carrying` / `storageSlotOf` / 其余」唯一判据决定，刚体引用锁死 `box.rb`；蓄力投掷挂右键（与左键既有语义完全分离）；顾客被砸是纯姿态动画（一根 `rotation.x`，根节点位置一帧不动）；绕行是纯观感的横向偏移，不写 `colliders`、不重建导航图。

**Tech Stack:** Three.js r128 UMD + cannon.js 0.6.2 UMD（`libs/cannon.min.js`，全局 `window.CANNON`）、ES5、经典 script 标签、`window.G`。

**依据文档：** `docs/superpowers/specs/2026-08-06-物理与投掷-design.md`（spec，逐字为准）

**基线：** HEAD `5217757`，自测正常态 132 / lowfx 127 全绿。`libs/cannon.min.js` 已 vendor 入库（132,199 字节，MIT，无 sourceMappingURL）。

---

## Global Constraints

- 纯 ES5（`var` / `function`、IIFE + `'use strict'`）、无构建、`file://` 双击即玩、运行时零网络请求。
- Three.js r128 UMD + 新 vendor 的 `libs/cannon.min.js`（UMD 全局 `window.CANNON`），经典 script 标签，全局 `window.G`。
- 自测：`?selftest=1` 无头跑，**正常态与 lowfx 态两态必须全绿**（判据：`document.title === 'SELFTEST:PASS'` 且 `#selftest` JSON 中无 `"ok": false`）。
- **跑无头 Edge 的 Bash/PowerShell 调用必须带 `dangerouslyDisableSandbox`**，否则静默产出 0 字节 DOM（假绿）。
- Edge 需 `--no-sandbox --allow-file-access-from-files`；**`--user-data-dir` 必须是全新独立目录、跑前 `rm -rf`**——profile 残留 `gss-lowfx` 会让正常态静默跑成 lowfx（`tex.*` 整块消失）。
- **`&lowfx=1` 参数不存在**：lowfx 态须用跳板页（scratchpad 写 html，`localStorage.setItem('gss-lowfx','1')` 后 `location.replace` 到 `index.html?selftest=1`）。
- **不得移动或削弱自测块的 localStorage 快照保护**（`main.js:373-390` 起手快照 `gss-save-v1` / `gss-save-v2` 两键、主 `try` 的 `finally` 无条件 `restoreSaveKeys()`）；**任何新增写 localStorage 的断言必须落在该保护内**（即写在主 `try` 之内）。
- push 用 `git -c credential.helper=wincred push origin main`（GCM 会打 .NET 异常噪声，以 ref 更新行为准）；**禁 `git add .`**（仓库根目录有用户私人笔记文件 `超市模拟器笔记.txt`）。
- 每完成一个 task 立即 commit + push，不要攒（此前有会话中断丢失全部未落盘工作的教训）。
- 每个改动过的 `.js` 文件必须过 `node --check`。
- **数值一律以 spec 的表为准**；实施时按手感调整了任何数字，**必须回写 spec §6.3 的数值表并在报告里点名**。

### 本期的九条硬约束（违反其一即为返工，实施者不知道这些，逐条抄在你的 task 里）

1. **刚体引用字段名锁死 `box.rb`，绝不能叫 `box.body`。** `box.body` 已经是纸箱的**纸板 mesh**（`world.js:1252`：`var box = { mesh: group, body: body, label: label, ... }`），且 `destroyBox` 会对 `[box.body, box.label]` 逐个 `geometry.dispose()` / `material.dispose()`（`world.js:1222-1227`）。用 `box.body` 装刚体会当场覆盖纸板 mesh 并让 dispose 对着刚体调用。
2. **静态刚体一律由 `G.physics.syncStatics()` 从 `G.world.colliders` 全量生成，绝不手写第二份几何表。** `rebuildGraph()` 的三个调用点（`world.js:854` buildZone / `world.js:1011` syncLayout / `world.js:1341` init）各自在其后加一行 `if (G.physics) G.physics.syncStatics();`。这是针对 C 期「同一事实存两份」反复出问题的核心设计，任何 task 不得破坏。
3. **collider 的 `h` 字段是纯加法**：寻路（`world.js` 的 `segHitsBox`）与玩家碰撞（`player.js:275` `collidesAt`）只读 `minX/maxX/minZ/maxZ` 四个 2D 字段，加 `h` 对它们不可见、零影响。各设施的 `h` 值以本计划 Task 1 的高度表为准，逐字采用。
4. **天花板 plane 必须在 `y = 3.40`**（= `WALL_H` 3.6 − 0.20），不是 3.55、不是 3.45。实测：plane 在 3.55 时箱以角撞顶 aabb 顶冲到 **3.7041 > WALL_H**；plane 在 3.40 时 v₀=9.0 实测 **3.5115**、v₀=12.0 实测 **3.5177**，均 ≤ 3.6。这个数字是「阴影视锥完全不用改」这个裁定的**唯一依据**，不得擅自调整。
5. **禁止使用 `interpolatedQuaternion`。** cannon 0.6.2 对**醒着的动态体**根本不写入该字段（实测：`quaternion.y = 0.0033` 而 `interpolatedQuaternion = (0,0,0,1)` 恒为单位四元数）。失效是静默的，表现为「箱不转」而不报错。位置写回用 `body.interpolatedPosition`，旋转写回用 `body.quaternion`。断言 `physics.noInterpolatedQuaternion` 源码级钉死。
6. **`THROW_V_MAX = 9.0` 有推导**：cannon 0.6.2 无 CCD，防隧穿硬上界 **12.0 m/s**（最薄静态体半厚 0.2 = 墙 collider，最坏竖直速度 `v² = v₀² + 2gh`，令 `v/60 = 0.225` 得 `v = 13.5` → `v₀ = 12.0`），取 25% 安全余量 → 9.0。**调高必须重跑 `physics.noTunneling` 断言并重算 spec §6.3 那一段。**
7. **slotted 态锁死为静态刚体（`mass = 0`）**，不是「无刚体」也不是「休眠动态体」。休眠体被撞醒会离开存储位而 `slot.box` 仍指着它 —— 那正是 C 期「同一事实存两份」的复发。
8. **倒地期间 `stepState` 照常跑**（`patience` / `shopTime` 继续累计）。否则砸倒顾客可无限拖延耐心，是可利用漏洞。
9. **顾客 group 必须先设 `rotation.order = 'YXZ'` 再用 `rotation.x` 倒地**。默认 `'XYZ'` 下 `rotation.x` 绕世界 X 轴，顾客朝 ±z 时会歪着倒；`'YXZ'` 下绕的是朝向之后的自身左右轴。**`rotation.x === 0` 时两种 order 完全等价，对既有行为零影响**（收银员站桩只设 `rotation.y`，不受影响）。

### 三条「别顺手改」

- **`applyGait` 里的 `if (c.ragdoll) return;` 钩子（`customers.js:324`）早已存在**，本期是第一次真正置位它 —— 不要重复添加钩子。
- **`discardBox` 不调 `destroyBox`**（`player.js:587-595` 只 `parent.remove(mesh)`，不 dispose 几何/材质、不摘交互体）是 C 期遗留债务。held 态无刚体，本期不产生新泄漏，**按「只清自己的烂摊子」原则挂账不修**。不要顺手改它。
- **checkout.js 本期零改动**（spec §1.1 已裁定不加 `abortTx(reg)`）。

---

## File Structure

| 文件 | D 期改动 |
|---|---|
| `docs/GDD.md` | T0：新增「§15 物理与投掷」节；§12 操作表补右键；§13 UI 清单补 `#charge` |
| `docs/CONTRACTS.md` | T0：登记 `G.physics` 全部 API、collider `h` 字段契约、`G.world.registerBoxInteractable` 导出、`G.ui.setCharge`、`G.player._test` / `G.customers._test` 新钩子；T7 校准 |
| `DESIGN.md` | T0：§3 补 `#charge` 一行；§5.8 预写 draw call 影响；T7 实测回写（726 / 760，余量 34） |
| `index.html` | T1：`libs/cannon.min.js` + `js/physics.js` 脚本序；T3：`#charge` 节点 |
| `js/physics.js` | **T1 新建**（骨架 + world + 两个 plane + `syncStatics` + `update` + `_test` 探针）；T2 补三态生命周期 API |
| `js/world.js` | T1：collider 加 `h`、三处 `rebuildGraph` 后加 `syncStatics()`；T2：`spawnBox` / `destroyBox` / `storeBox` / `takeBox` 物理接线 + 导出 `registerBoxInteractable`；T6：`serializeBoxes` / `restoreBoxes` 的 `ry` 字段 |
| `js/player.js` | T2：`pickUpBox` / `putDownBox` 物理接线；T3：右键蓄力 + `contextmenu` 抑制 + `releaseThrow` + `_test` 钩子 |
| `js/customers.js` | T4：`stepKnockdown` + `rotation.order` + `stepCustomer` 抽取；T5：`stepAvoid` + 横向偏移 |
| `js/ui.js` | T3：`setCharge(v)` + `#charge` 构建 |
| `css/style.css` | T3：`#charge` 样式 |
| `js/main.js` | T1：`boot` / `tick` 接入 + 断言 1-5、12；T2-T6 各自断言；T7：断言收口 + 闸门扩场景 |

**任务依赖：** T0 → T1 → T2 → {T3 ∥ T6} → T4 → T5 → T7。
T4 与 T5 都动 `customers.update` 的同一个循环体，**必须串行**（T4 先，T5 后）。T3（player/ui/css/index.html）与 T6（world.js 存档段）与 T4/T5 文件不相交，可并行，但都依赖 T2。

---

## 自测运行方法（每个 task 的验证步骤都要跑，逐字照抄）

设 `SP` = 你的 scratchpad 目录。**两条命令都必须带 `dangerouslyDisableSandbox`。**

**正常态：**
```bash
rm -rf "$SP/edge-d"
"C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu \
  --no-sandbox --allow-file-access-from-files --user-data-dir="$SP/edge-d" \
  --dump-dom --virtual-time-budget=40000 \
  "file:///C:/Users/quito/超市模拟器/index.html?selftest=1" 2>/dev/null > "$SP/normal.html"
grep -o "SELFTEST:[A-Z]*" "$SP/normal.html" | head -1
grep -c '"ok": false' "$SP/normal.html"
grep -B 2 '"ok": false' "$SP/normal.html" | head -60
grep -c '"name":' "$SP/normal.html"
```

**lowfx 态：**先写跳板页（每次跑前重写，内容固定）：
```bash
cat > "$SP/lowfx.html" <<'EOF'
<!doctype html><meta charset="utf-8"><script>
try { localStorage.setItem('gss-lowfx', '1'); } catch (e) {}
location.replace('file:///C:/Users/quito/超市模拟器/index.html?selftest=1');
</script>
EOF
rm -rf "$SP/edge-lowfx"
"C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu \
  --no-sandbox --allow-file-access-from-files --user-data-dir="$SP/edge-lowfx" \
  --dump-dom --virtual-time-budget=40000 "file:///$SP/lowfx.html" 2>/dev/null > "$SP/lowfx.html.out"
grep -o "SELFTEST:[A-Z]*" "$SP/lowfx.html.out" | head -1
grep -c '"ok": false' "$SP/lowfx.html.out"
```

**判据**：`SELFTEST:PASS` 且 `"ok": false` 计数为 0。`grep -c '"name":'` 给出该态的断言总数，每个 task 报告里都要写。

**基线数字**：正常态 132、lowfx 127（本期结束预期：正常态 162、lowfx 157；若实际不同，以「无 `ok:false`」为准，并在报告里说明差值来源）。

---

## Task 0：文档先行（GDD / CONTRACTS / DESIGN）

**Files:**
- Modify: `docs/GDD.md`（新增 §15；§12 操作表；§13 UI 清单）
- Modify: `docs/CONTRACTS.md`（新增 `G.physics` 段；`G.world` / `G.player` / `G.ui` / `G.customers` 段补行；不变式段补条）
- Modify: `DESIGN.md`（§3 HUD 补 `#charge`；§5.8 预写 draw call 影响）

**Interfaces:**
- Consumes: spec 全文
- Produces: 全部后续 task 的规范依据（无代码接口）

- [ ] **Step 1: `docs/GDD.md` —— 在 §14「明确的范围裁剪」之前插入新的一节 §15（逐字）**

```markdown
## 15. 物理与投掷（`js/physics.js`）

纯物理玩具，**零玩法后果**：扔箱不扣钱、不降满意度、不算损耗、不触发投诉/罚款/差评。它存在的唯一理由是手感与场景可信度。

- **物理范围只有纸箱**。商品单件是 InstancedMesh（DESIGN §5.8），不参与物理；顾客不参与物理（被砸是纯姿态动画）。
- **纸箱三态**：`held`（在玩家手上，无刚体）/ `slotted`（在仓库存储位上，静态刚体 mass 0）/ `loose`（其余，动态刚体）。
- **投掷**：持箱时**按住右键**蓄力（0.8s 到满，HUD 力度条 `#charge`），松开右键沿相机朝向扔出。出手速度在 1.5 m/s（轻放）与 9.0 m/s（满力）之间线性插值，附带 1.5–7.0 rad/s 的随机三轴角速度。左键的既有语义（点击交互 / 持箱按住连续上架）一字不动。玩家自身移速不叠加到初速。
- **被砸倒地**：箱速度 ≥ 2.5 m/s 命中顾客 → 顾客原地倒下 2.5s 后爬起（爬起动画 0.35s，总占用 2.85s），继续原行为。倒地期间步态与移动冻结，**交易状态机与耐心计时照常推进**（不存在「砸倒顾客拖延耐心」的漏洞），手上商品随姿态旋转不散落。**不改交易状态机、金钱 / XP / 满意度一律不动。**
- **挡路 = 局部绕行**：顾客检测前方 1.2m 内的散落箱并向侧向让开最多 0.60m（总偏移钳制 ±0.80m）。绕行是**纯观感**：箱不进 `G.world.colliders`、不重建导航图，绕行失败时顾客直接穿过箱子，无任何后果。
- **天花板**：全图（含户外卸货区）在 y = 3.40 有一层看不见的物理天花板，保证任何箱的几何体永远在 `y ≤ WALL_H (3.6)` 之内。往天上扔箱会撞到它，这是有意取舍。
- **存档**：所有箱一律按「落地平放」口径入档 —— 只存偏航 `ry`，`y` 恒 0.225，俯仰与翻滚丢弃。飞行中的箱读档后静止落地。旧档无 `ry` 视为 0。
```

- [ ] **Step 2: `docs/GDD.md` §12 操作方式表 —— 在 `Q` 那一行之后插入一行**

```markdown
| 右键（按住 / 松开） | 持箱时蓄力投掷：按住蓄力（0.8s 满），松开扔出；与准星指向何处无关 |
```

- [ ] **Step 3: `docs/GDD.md` §13 UI 界面清单 —— 第 3 条改为**

```markdown
3. `#crosshair` 准星；`#charge` 蓄力力度条（准星正下方 28px，120×4px，仅持箱按住右键时可见）；`#prompt` 交互提示（底部居中）；`#toast` 浮动消息（右下，2.5s 淡出，最多堆 3 条）。
```

- [ ] **Step 4: `DESIGN.md` §3 HUD 布局 —— ASCII 图中 `#crosshair 正中` 一行之后加一行，并在下方条目列表 `#crosshair` 那条之后插入 `#charge` 条目**

ASCII 图内加：
```
│                 #charge 准星下方 28px（蓄力时可见）        │
```

条目列表插入（放在 `#crosshair` 条之后、`#prompt` 条之前）：
```markdown
- `#charge`：蓄力力度条，`#crosshair` 正下方 28px 居中，120×4px，`radius 2px`；底 `--line`，填充 `--accent-2`（与 `#hud-level` 的 XP 条同规格，不引入新视觉语言）。持箱按住右键蓄力时显示，填充宽度 = `charge × 120px`；松开 / 取消后 `display: none`。`pointer-events: none`。
```

- [ ] **Step 5: `DESIGN.md` §5.8 —— 预写 draw call 影响**

在「| **理论满配合计** | **702** | |」这一行**之前**插入一行：

```markdown
| 散落地面箱 | 24 | `BOX_HARD_CAP` 48 − 仓库 24 − 卸货 12 = 最多 12 只 × 2 mesh（D 期投掷落地箱） |
```

并把紧随其后的两行改为：

```markdown
| **理论满配合计** | **726** | 含 D 期 12 只散落地面箱（702 + 24） |
| 自测现场实测 | **726** | D-T7 起闸门场景在满配基础上再放 12 只散落地面箱后测量 |
```

把「**总闸门 550→760**，余量 58。」改为：

```markdown
**总闸门 550→760**，余量 **34**（D 期把 12 只散落地面箱纳入闸门场景后，余量从 58 收窄到 34）。
```

把「**未纳入闸门场景的负载**（下次加装饰前须先补进来）：传送带上的商品 mesh、玩家举起的纸箱。余量 58 比看上去薄。」改为：

```markdown
**未纳入闸门场景的负载**（下次加装饰前须先补进来）：传送带上的商品 mesh、玩家举起的纸箱。余量 **34** 比看上去薄得多 —— 散落地面箱已于 D 期纳入闸门场景（12 只 × 2 = 24），下一次加装饰前必须先重估这 34 格。
```

- [ ] **Step 6: `docs/CONTRACTS.md` —— 在「### G.tex（textures.js）」段之后、「### G.world（world.js）」段之前插入新段**

```markdown
### G.physics（physics.js）
```js
G.physics.init()                  // 建 CANNON.World（gravity (0,-9.82,0)、NaiveBroadphase、solver.iterations 10、allowSleep）
                                  // + 地面 plane(y=0) + 天花板 plane(y=CEIL_Y=3.40)。
                                  // 【必须在 G.world.init(scene) 之前调用】——world.init 会走到 rebuildGraph → syncStatics
G.physics.syncStatics()           // 按 G.world.colliders 全量重建静态刚体（先清后建）；地/天花板 plane 不参与重建。
                                  // 【唯一真相源】引擎静态刚体一律由此生成，绝不手写第二份几何表
G.physics.update(dt)              // world.step(1/60, dt, 10)，然后把全部 loose 箱的 interpolatedPosition / quaternion 写回 box.mesh
G.physics.attach(box)             // 由 mesh 当前 position/quaternion 建【动态】刚体，挂到 box.rb；幂等（先 detach）
G.physics.attachStatic(box)       // 同上但 mass = 0（存储位用）
G.physics.detach(box)             // 从 world 移除 box.rb，box.rb = null；幂等
G.physics.throwBox(box, dir /*THREE.Vector3 单位向量*/, speed, spin /*{x,y,z}*/)   // attach 后设 velocity/angularVelocity
G.physics.looseBoxes()            // -> [box]（箱实体，不是 body）。【只读、共用同一个内部数组，调用方不得持有跨帧】
                                  // 调用方从 box.mesh.position 与 box.rb.velocity 取值
G.physics.CEIL_Y                  // 3.40 = WALL_H − 0.20（spec §9.2 实测定值，改它必须重跑 physics.ceilingCaps）
G.physics._test                   // 仅供自测：bodyCount() / staticCount()（只数 collider 静态体 + 2 plane，不含 slotted 箱）
                                  // / hasBody(body) / stateOf(box) -> 'held'|'slotted'|'loose' / stepOnce()
                                  // / src() / probeCeiling(v0) / probeTunnel(speed)
```
- **刚体引用字段名是 `box.rb`，不是 `box.body`**（`box.body` 已被占用：它是纸箱的纸板 mesh，且 `destroyBox` 会对它调 `geometry.dispose()`）。
- **箱实体的所有权仍归 world.js**：`G.physics` 只持 `box.rb` 引用，`allBoxes()` 仍是唯一枚举口径。
- `G.world.colliders` 的每条可带**可选**高度字段 `h`（米，缺省视为 `WALL_H` 3.6）。**`h` 只被 physics.syncStatics 读取；寻路（`segHitsBox`）与玩家碰撞（`collidesAt`）只读 `minX/maxX/minZ/maxZ` 四个 2D 字段，不得读 `h`。**
- `rebuildGraph()` 的每一个调用点都必须紧跟一行 `if (G.physics) G.physics.syncStatics();`。漂移由断言 `physics.staticsMatchColliders` 钉死。
```

- [ ] **Step 7: `docs/CONTRACTS.md` —— `G.world` 段、`G.player` 段、`G.ui` 段补行**

`G.world` 代码块内，`G.world.destroyBox(box)` 那一行之后加：
```js
G.world.registerBoxInteractable(box)   // D 期导出；箱交互体的唯一登记入口（起手 retire 一次，保证「一 mesh 一交互体」），投掷落地后由 player.releaseThrow 调用
```
`G.world.serializeBoxes()` 那一行的返回结构改为：
```js
G.world.serializeBoxes() /*->[{pid,left,where:'storage'|'yard'|'floor',slotId?,x?,z?,ry?}]*/
```
并在其后加一行说明：
```
// ry（D 期）：绕 Y 的偏航（弧度）。所有箱一律按「落地平放」口径存档——y 恒 0.225 不入档，俯仰与翻滚丢弃。旧档无 ry 视为 0，v:2 不变更。
```

`G.player` 代码块内 `G.player.setPose(pose)` 之后加：
```js
G.player._test   // 仅供自测：prompt(entry) / pickUp(box) / putDown() / storeInto(slot) / throwConsts()
                 // / charge(tSec) / chargeState() / peekThrow() / throwNow() / setRmb(down)
                 // / stepCharge(dt) / setHover(entry) / setMouseDown(down) / doInteractions()
                 // / resetStockCooldown()
```
并在该段的条目列表末尾加两条：
```markdown
- **持箱时按住右键蓄力、松开投掷**（D 期）：与准星指向何处无关，左键的两个既有语义（`mouseJustPressed` 点击交互、`mouseDown` 持箱按住连续上架）零改动、可与右键蓄力同时独立生效。蓄力中 `inRegister` / 有全屏界面 / 失去指针锁定 → 取消蓄力（箱留在手上）。
- canvas 上注册 `contextmenu` → `e.preventDefault()`：指针锁定下多数浏览器已抑制右键菜单，但不得依赖。断言 `player.contextMenuSuppressed`。
```

`G.ui` 代码块内加一行：
```js
G.ui.setCharge(v /*0..1 显示并设填充宽度；null 隐藏*/)   // D 期；#charge 蓄力力度条
```

- [ ] **Step 8: `docs/CONTRACTS.md` —— 「### 不变式（改动前先读）」段末尾追加四条**

```markdown
- **静态刚体的单一真相源是 `G.world.colliders`**（D 期）：`G.physics.syncStatics()` 全量重建，绝不手写第二份几何表；`rebuildGraph()` 的每个调用点必须同步调 `syncStatics()`。断言 `physics.staticsMatchColliders` / `physics.staticsAfterZoneOpen`。
- **`interpolatedQuaternion` 在本项目中禁止使用**（D 期）：cannon 0.6.2 对醒着的动态体根本不写入该字段（永远是单位四元数），失效是静默的（表现为「箱不转」）。位置写回用 `body.interpolatedPosition`，旋转写回用 `body.quaternion`。断言 `physics.noInterpolatedQuaternion` 源码级钉死。
- **箱三态与其刚体形态一一对应**（D 期）：`held`（`G.player.carrying === box`）无刚体；`slotted`（`storageSlotOf(box) !== null`）静态刚体 `mass 0`；`loose`（其余）动态刚体。四个转移入口 `pickUpBox` / `putDownBox` / `storeBox` / `releaseThrow` 各恰好一行物理调用，另加 `spawnBox` / `takeBox` / `destroyBox` / `restoreBoxes` 四处非玩家路径。
- **`G.customers._test.stepOne(c, dt)`**（D 期，仅供自测）：跑一名顾客的完整每帧流程（`stepKnockdown` → 非倒地时 `stepAvoid`/`stepMove`/`applyGait` → `stepState`），与 `update()` 的循环体同源同函数。
```

- [ ] **Step 9: 跑一遍两态自测确认无回归（纯文档改动，应仍是 132 / 127 全绿），提交推送**

```bash
git add docs/GDD.md docs/CONTRACTS.md DESIGN.md
git commit -m "docs(D-T0): 物理与投掷契约先行——GDD/CONTRACTS/DESIGN 修订"
git -c credential.helper=wincred push origin main
```
（commit message 结尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。）

---

## Task 1：引擎接入与静态刚体（地基）

**Files:**
- Create: `js/physics.js`
- Modify: `index.html:30-40`（脚本序）
- Modify: `js/world.js:616`、`js/world.js:620`（卷帘门 collider 加 `h`）、`js/world.js:726`（收银台）、`js/world.js:751`（电脑）、`js/world.js:760`（垃圾桶）、`js/world.js:978`（货架）、`js/world.js:854`、`js/world.js:1011`、`js/world.js:1341`（三处 `rebuildGraph()` 后加 `syncStatics()`）
- Modify: `js/main.js:102-112`（`boot`）、`js/main.js:141-148`（`tick`）、`js/main.js:824-825` 之间（新 `physics.*` 断言组）、`js/main.js:1294-1297` 之后（开区后再测一次）

**Interfaces:**
- Consumes: T0 契约
- Produces:
  - `G.physics.init()`、`G.physics.syncStatics()`、`G.physics.update(dt)`、`G.physics.CEIL_Y`
  - `G.physics._test.bodyCount()` / `.staticCount()` / `.stepOnce()` / `.src()` / `.probeCeiling(v0)` / `.probeTunnel(speed)`
  - collider 可选字段 `h`（number，米）

### 高度表（逐字采用，视觉高度已从建造代码实读）

| collider 来源 | world.js 行 | 视觉高度依据 | **`h`** |
|---|---|---|---:|
| 墙段 `wallAlongX` / `wallAlongZ` | 594 / 603 | `BoxGeometry(len, WALL_H, 0.2)` @ `y = 1.8`，`WALL_H = 3.6` | **省略**（默认 3.6） |
| 卷帘门 `buildShutters` | 616 / 620 | `BoxGeometry(w, 3.0, 0.12)` @ `y = 1.5` | **3.0** |
| 世界边界围栏 ×6 | 693-698 | 无 mesh，纯挡人隐形墙 | **省略**（默认 3.6，让院里的箱不飞进虚空） |
| 收银台 `buildRegister` | 726 | 柜体顶 1.0；传送带 `BoxGeometry(1.6,0.08,0.9)` @ `y=1.04` → 顶 **1.08**，且 1.6×0.9 完全落在 2.0×1.2 足迹内 | **1.08** |
| 订货电脑 `buildComputer` | 751 | 机身 `BoxGeometry(0.5,0.9,0.4)` @ `y=0.45` → 顶 0.9 | **0.9** |
| 垃圾桶 `buildTrash` | 760 | `CylinderGeometry(0.3,0.25,0.6,8)` @ `y=0.3` → 顶 0.6 | **0.6** |
| 货架 / 冷藏柜 `buildRack` | 978 | `SHELF_H = 1.8` | **1.8** |
| 存储位标记 `buildStorage` | — | 本来就没有 collider，箱直接躺地面 | — |

静态体计数：开局（仅 A 区 + W）29 collider + 2 plane；满配（全开）44 collider + 2 plane = **46**。

- [ ] **Step 1: 写失败断言（`js/main.js`）**

在 `ck('light.count', ...)` 那一段之后（现 `js/main.js:824` 与 `825` 之间，即 `ck('shadow.rendererState'` 之前）插入整块：

```js
      /* --- D-T1 物理引擎与静态刚体（spec §2/§3/§4）--- */
      ck('physics.engineLoaded',
        typeof CANNON !== 'undefined' && CANNON.version === '0.6.2' &&
        typeof CANNON.NaiveBroadphase === 'function' && !!G.physics && !!G.physics._test,
        'CANNON ' + (typeof CANNON !== 'undefined' ? CANNON.version : 'undefined') +
        '，G.physics ' + (G.physics ? 'ok' : '缺'));
      ck('physics.staticsMatchColliders',
        !!G.physics && G.physics._test.staticCount() === G.world.colliders.length + 2,
        '静态体 ' + (G.physics ? G.physics._test.staticCount() : -1) +
        ' / collider ' + G.world.colliders.length + ' + 2 plane');
      var chOk = true, chBad = '';
      for (var chi = 0; chi < G.world.colliders.length; chi++) {
        var chc = G.world.colliders[chi];
        var chH = (typeof chc.h === 'number') ? chc.h : (G.world.WALL_H || 3.6);
        var chW = round2(chc.maxX - chc.minX), chD = round2(chc.maxZ - chc.minZ);
        var want = null;
        if (chc.zoneGate) want = 3.0;
        else if (chW === 2 && chD === 0.8) want = 1.8;
        else if (chW === 2 && chD === 1.2) want = 1.08;
        if (!(chH > 0 && chH <= (G.world.WALL_H || 3.6))) { chOk = false; chBad = '#' + chi + ' h=' + chH + ' 越界'; break; }
        if (want !== null && chH !== want) { chOk = false; chBad = '#' + chi + ' ' + chW + '×' + chD + ' h=' + chH + '，应 ' + want; break; }
      }
      ck('physics.colliderHeights', chOk, chBad || ('全部 ' + G.world.colliders.length + ' 条 collider 的 h 合规'));
      var ceil9 = G.physics ? G.physics._test.probeCeiling(9.0) : 99;
      var ceil12 = G.physics ? G.physics._test.probeCeiling(12.0) : 99;
      var wallH = G.world.WALL_H || 3.6;
      ck('physics.ceilingCaps', ceil9 <= wallH && ceil12 <= wallH,
        '12 只箱 ×300 步 aabb 顶最大：v0=9.0 → ' + round2(ceil9) + '，v0=12.0 → ' + round2(ceil12) +
        '（天花板 plane y=' + (G.physics ? G.physics.CEIL_Y : '?') + '，须 ≤ WALL_H ' + wallH + '）');
      var tunV = G.player._test.throwConsts ? G.player._test.throwConsts().vMax : 9.0;
      var tun = G.physics ? G.physics._test.probeTunnel(tunV) : { wallZ: 99, dropY: -1 };
      ck('physics.noTunneling', tun.wallZ < 2.8 && tun.dropY > 0,
        'v=' + tunV + ' 正对半厚 0.2 的墙（内面 z=2.8）120 步后 z=' + round2(tun.wallZ) +
        '；竖直下扔 120 步后 y=' + round2(tun.dropY));
      ck('physics.noInterpolatedQuaternion',
        !!G.physics && G.physics._test.src().indexOf('interpolatedQuaternion') === -1,
        'js/physics.js 全部函数源码不得出现 interpolatedQuaternion（cannon 0.6.2 对醒着的动态体永不写入该字段，失效静默）');
```

并在 `G.world.buildZone('W');` + `G.world.syncLayout();` 之后（现 `js/main.js:1297` 之后）插入：

```js
      ck('physics.staticsAfterZoneOpen',
        !!G.physics && G.physics._test.staticCount() === G.world.colliders.length + 2,
        '全开后静态体 ' + (G.physics ? G.physics._test.staticCount() : -1) +
        ' / collider ' + G.world.colliders.length + ' + 2 plane（buildZone 后必须已 syncStatics）');
```

> **注意**：`physics.noTunneling` 依赖 `G.player._test.throwConsts()`，那是 T3 才建的。本 task 里用 `G.player._test.throwConsts ? ... : 9.0` 的短路写法（上面已写好），T3 落地后自动切到真常量，无需回头改。

- [ ] **Step 2: 自跑自测确认 FAIL**

跑正常态。**期望的失败信息**：`SELFTEST:FAIL`，且 `"ok": false` 出现在
`physics.engineLoaded`（detail 形如 `CANNON undefined，G.physics 缺`）、
`physics.staticsMatchColliders`（`静态体 -1 / collider 44 + 2 plane`）、
`physics.ceilingCaps`（`v0=9.0 → 99`）、
`physics.noTunneling`（`120 步后 z=99`）、
`physics.noInterpolatedQuaternion`（G.physics 为空）、
`physics.staticsAfterZoneOpen`。
`physics.colliderHeights` 此时可能已绿（全部 collider 无 `h` → 默认 3.6，且尚无 2.0×0.8 的判据命中……实际会红，因为货架 collider 是 2.0×0.8 且 h 缺省 3.6 ≠ 1.8）——**确认它也是红的**。

- [ ] **Step 3: 新建 `js/physics.js`（全文逐字）**

```js
// js/physics.js — cannon.js 刚体世界：静态体从 colliders 自动生成、纸箱刚体、每帧写回 mesh
// 归属：physics agent。只通过 CONTRACTS.md 中定义的 API 与其它模块通信。
(function () {
  'use strict';

  var G = (window.G = window.G || {});

  /* ---- 数值（spec §5.1）：改这里必须同步回写 spec §6.3 数值表 ---- */
  var BOX_MASS = 1.2;
  var BOX_HALF = 0.225;          // mesh 是 BoxGeometry(0.45³)、落位 y=0.225，几何与物理零偏移
  var BOX_LIN_DAMP = 0.02;
  var BOX_ANG_DAMP = 0.15;       // 偏大：落地后很快停转，不会像陀螺一样转半天
  var BOX_FRICTION = 0.35;
  var BOX_RESTITUTION = 0.15;    // 纸箱：落地就停，几乎不弹
  var SLEEP_SPEED = 0.15;
  var SLEEP_TIME = 0.6;

  /* 天花板高度 = WALL_H(3.6) − 0.20。求解器允许的穿透 + 箱以角朝上撞击会让 aabb 顶
     比 plane 高出约 0.17m：plane 放 3.55 时实测 aabb 顶冲到 3.7041 > WALL_H。
     3.40 是「阴影视锥完全不用改」这个裁定的唯一依据（spec §9.2），不得擅自调整。 */
  var CEIL_Y = 3.40;

  var FIXED_STEP = 1 / 60;
  var MAX_SUB_STEPS = 10;        // 主循环 dt 已钳到 0.1s，最坏 6 个子步，永不触发截断
  var DEFAULT_H = 3.6;           // collider 省略 h 时的默认高度（= WALL_H）

  var world = null;
  var boxMat = null, staticMat = null;
  var staticBodies = [];         // 由 colliders 生成的静态体（不含 2 个 plane）
  var planeCount = 0;
  var looseScratch = [];         // looseBoxes() 复用的数组，零分配；调用方不得跨帧持有

  /* 世界的唯一建造式：init() 与 _test 的两个离线探针共用，保证探针测的就是真实配置 */
  function buildWorld() {
    var w = new CANNON.World();
    w.gravity.set(0, -9.82, 0);
    w.broadphase = new CANNON.NaiveBroadphase();
    w.solver.iterations = 10;
    w.allowSleep = true;

    var sm = new CANNON.Material('gssStatic');
    var bm = new CANNON.Material('gssBox');
    w.addContactMaterial(new CANNON.ContactMaterial(bm, sm,
      { friction: BOX_FRICTION, restitution: BOX_RESTITUTION }));
    w.addContactMaterial(new CANNON.ContactMaterial(bm, bm,
      { friction: BOX_FRICTION, restitution: BOX_RESTITUTION }));

    var ground = new CANNON.Body({ mass: 0, material: sm });
    ground.addShape(new CANNON.Plane());
    ground.quaternion.setFromAxisAngle(new CANNON.Vec3(1, 0, 0), -Math.PI / 2);
    w.addBody(ground);

    var ceil = new CANNON.Body({ mass: 0, material: sm });
    ceil.addShape(new CANNON.Plane());
    ceil.quaternion.setFromAxisAngle(new CANNON.Vec3(1, 0, 0), Math.PI / 2);
    ceil.position.set(0, CEIL_Y, 0);
    w.addBody(ceil);

    return { world: w, staticMat: sm, boxMat: bm, planes: 2 };
  }

  function init() {
    if (world || typeof CANNON === 'undefined') return;
    var W = buildWorld();
    world = W.world;
    staticMat = W.staticMat;
    boxMat = W.boxMat;
    planeCount = W.planes;
  }

  /* 全量重建：先清后建。地/天花板 plane 不参与。
     【唯一真相源】静态刚体只从 G.world.colliders 生成，绝不手写第二份几何表 */
  function syncStatics() {
    if (!world) return;
    var i;
    for (i = 0; i < staticBodies.length; i++) world.removeBody(staticBodies[i]);
    staticBodies.length = 0;
    var cols = (G.world && G.world.colliders) || [];
    for (i = 0; i < cols.length; i++) {
      var col = cols[i];
      var h = (typeof col.h === 'number' && col.h > 0) ? col.h : DEFAULT_H;
      var body = new CANNON.Body({ mass: 0, material: staticMat });
      body.addShape(new CANNON.Box(new CANNON.Vec3(
        (col.maxX - col.minX) / 2, h / 2, (col.maxZ - col.minZ) / 2)));
      body.position.set((col.minX + col.maxX) / 2, h / 2, (col.minZ + col.maxZ) / 2);
      world.addBody(body);
      staticBodies.push(body);
    }
  }

  function update(dt) {
    if (!world || !(dt > 0)) return;
    world.step(FIXED_STEP, dt, MAX_SUB_STEPS);
    var loose = looseBoxes();
    for (var i = 0; i < loose.length; i++) {
      var b = loose[i];
      b.mesh.position.copy(b.rb.interpolatedPosition);   // 平滑
      /* 旋转必须读 quaternion：cannon 0.6.2 变步长路径下对醒着的动态体不写插值四元数
         （永远是单位四元数），用它的后果是「箱不转」且不报错。见 spec §2.2 */
      b.mesh.quaternion.copy(b.rb.quaternion);
    }
  }

  function makeBoxBody(box, mass) {
    var body = new CANNON.Body({
      mass: mass, material: boxMat,
      linearDamping: BOX_LIN_DAMP, angularDamping: BOX_ANG_DAMP
    });
    body.addShape(new CANNON.Box(new CANNON.Vec3(BOX_HALF, BOX_HALF, BOX_HALF)));
    var p = box.mesh.position, q = box.mesh.quaternion;
    body.position.set(p.x, p.y, p.z);
    body.quaternion.set(q.x, q.y, q.z, q.w);
    body.allowSleep = true;
    body.sleepSpeedLimit = SLEEP_SPEED;
    body.sleepTimeLimit = SLEEP_TIME;
    world.addBody(body);
    /* 字段名锁死 rb：box.body 已经是纸箱的纸板 mesh（world.js:1252），
       且 destroyBox 会对 box.body 调 geometry.dispose() */
    box.rb = body;
    return body;
  }

  function attach(box) {
    if (!world || !box || !box.mesh) return null;
    detach(box);
    return makeBoxBody(box, BOX_MASS);
  }

  function attachStatic(box) {
    if (!world || !box || !box.mesh) return null;
    detach(box);
    return makeBoxBody(box, 0);
  }

  function detach(box) {
    if (!world || !box || !box.rb) return;
    world.removeBody(box.rb);
    box.rb = null;
  }

  function throwBox(box, dir, speed, spin) {
    var body = attach(box);
    if (!body) return;
    body.wakeUp();
    body.velocity.set(dir.x * speed, dir.y * speed, dir.z * speed);
    body.angularVelocity.set(spin.x, spin.y, spin.z);
  }

  /* loose = 有动态刚体的箱。判据直接从 allBoxes() + rb.type 推导，不另存一份名单 */
  function looseBoxes() {
    looseScratch.length = 0;
    if (typeof CANNON === 'undefined') return looseScratch;
    var list = (G.world && G.world.allBoxes) ? G.world.allBoxes() : [];
    for (var i = 0; i < list.length; i++) {
      var b = list[i];
      if (b.rb && b.rb.type === CANNON.Body.DYNAMIC) looseScratch.push(b);
    }
    return looseScratch;
  }

  function stateOf(box) {
    if (G.player && G.player.carrying === box) return 'held';
    var slots = (G.world && G.world.storageSlots) || [];
    for (var i = 0; i < slots.length; i++) { if (slots[i].box === box) return 'slotted'; }
    return 'loose';
  }

  /* 离线探针：在一个与 init() 同配置的独立 world 里跑，绝不脏化游戏世界 */
  function probeCeiling(v0) {
    if (typeof CANNON === 'undefined') return 99;
    var W = buildWorld(), bodies = [], max = -Infinity, i, s;
    for (i = 0; i < 12; i++) {
      var b = new CANNON.Body({
        mass: BOX_MASS, material: W.boxMat,
        linearDamping: BOX_LIN_DAMP, angularDamping: BOX_ANG_DAMP
      });
      b.addShape(new CANNON.Box(new CANNON.Vec3(BOX_HALF, BOX_HALF, BOX_HALF)));
      b.position.set(i * 1.5 - 8, 1.9, (i % 3) * 1.2 - 1.2);
      b.velocity.set(0, v0, 0);
      b.angularVelocity.set((i % 5) - 2, (i % 7) - 3, (i % 3) - 1);
      W.world.addBody(b);
      bodies.push(b);
    }
    for (s = 0; s < 300; s++) {
      W.world.step(FIXED_STEP, 0.017, MAX_SUB_STEPS);
      for (i = 0; i < bodies.length; i++) {
        /* NaiveBroadphase 默认 useBoundingBoxes=false，body.aabb 全程不会被 step 更新。
           不显式 computeAABB() 这条断言会一直读到初始 aabb（0.225）而恒绿 —— 空断言。 */
        bodies[i].computeAABB();
        if (bodies[i].aabb.upperBound.y > max) max = bodies[i].aabb.upperBound.y;
      }
    }
    return max;
  }

  function probeTunnel(speed) {
    if (typeof CANNON === 'undefined') return { wallZ: 99, dropY: -1 };
    var W = buildWorld(), i;
    var wall = new CANNON.Body({ mass: 0, material: W.staticMat });
    wall.addShape(new CANNON.Box(new CANNON.Vec3(4, 1.8, 0.2)));   // 半厚 0.2 = 墙 collider
    wall.position.set(0, 1.8, 3);
    W.world.addBody(wall);

    var h = new CANNON.Body({
      mass: BOX_MASS, material: W.boxMat,
      linearDamping: BOX_LIN_DAMP, angularDamping: BOX_ANG_DAMP
    });
    h.addShape(new CANNON.Box(new CANNON.Vec3(BOX_HALF, BOX_HALF, BOX_HALF)));
    h.position.set(0, 1.3, 0);
    h.velocity.set(0, 0, speed);
    W.world.addBody(h);

    var d = new CANNON.Body({
      mass: BOX_MASS, material: W.boxMat,
      linearDamping: BOX_LIN_DAMP, angularDamping: BOX_ANG_DAMP
    });
    d.addShape(new CANNON.Box(new CANNON.Vec3(BOX_HALF, BOX_HALF, BOX_HALF)));
    d.position.set(-3, 1.9, 0);
    d.velocity.set(0, -speed, 0);
    W.world.addBody(d);

    for (i = 0; i < 120; i++) W.world.step(FIXED_STEP, 0.017, MAX_SUB_STEPS);
    return { wallZ: h.position.z, dropY: d.position.y };
  }

  G.physics = {
    init: init,
    syncStatics: syncStatics,
    update: update,
    attach: attach,
    attachStatic: attachStatic,
    detach: detach,
    throwBox: throwBox,
    looseBoxes: looseBoxes,
    CEIL_Y: CEIL_Y,
    _test: {
      bodyCount: function () { return world ? world.bodies.length : 0; },
      staticCount: function () { return staticBodies.length + planeCount; },
      hasBody: function (body) { return !!world && !!body && world.bodies.indexOf(body) !== -1; },
      stateOf: stateOf,
      stepOnce: function () { if (world) world.step(FIXED_STEP, FIXED_STEP, MAX_SUB_STEPS); },
      probeCeiling: probeCeiling,
      probeTunnel: probeTunnel,
      /* 源码级自检面：把模块全部函数的 toString 拼起来，供 physics.noInterpolatedQuaternion
         断言扫描。无构建步骤，源码即所写；file:// 下不能用 XHR 读自身文件，故走这条路。 */
      src: function () {
        return [buildWorld, init, syncStatics, update, makeBoxBody, attach, attachStatic,
          detach, throwBox, looseBoxes, stateOf, probeCeiling, probeTunnel].join('\n');
      }
    }
  };
})();
```

- [ ] **Step 4: `index.html` 脚本序**

把 `<script src="libs/three.min.js"></script>` 之后到 `<script src="js/world.js"></script>` 之间改为（`cannon` 紧跟 `three`，`physics` 必须在 `world` **之前**）：

```html
<script src="libs/three.min.js"></script>
<script src="libs/cannon.min.js"></script>
<script src="js/data.js"></script>
<script src="js/state.js"></script>
<script src="js/textures.js"></script>
<script src="js/physics.js"></script>
<script src="js/world.js"></script>
```

- [ ] **Step 5: `js/world.js` —— collider 加 `h`（六处，逐字）**

`buildShutters`（现 616 / 620 两行）：
```js
        colliders.push({ minX: s.minX, maxX: s.maxX, minZ: s.z - 0.2, maxZ: s.z + 0.2, zoneGate: s.zone, h: 3.0 });
```
```js
        colliders.push({ minX: s.x - 0.2, maxX: s.x + 0.2, minZ: s.minZ, maxZ: s.maxZ, zoneGate: s.zone, h: 3.0 });
```

`buildRegister`（现 726）：
```js
    // h = 传送带顶面 1.08（1.6×0.9 的传送带完全落在 2.0×1.2 的柜体足迹内），箱可以扔上台面
    colliders.push({ minX: def.x - 1.0, maxX: def.x + 1.0, minZ: REGISTER_Z - 0.6, maxZ: REGISTER_Z + 0.6, h: 1.08 });
```

`buildComputer`（现 751）：
```js
    colliders.push({ minX: 13.75, maxX: 14.25, minZ: 9.2, maxZ: 9.6, h: 0.9 });
```

`buildTrash`（现 760）：
```js
    // 已知瑕疵：桶是圆柱 mesh 但 collider 是 0.6×0.6 方盒，箱会以方形边界弹开（观感可接受）
    colliders.push({ minX: -7.7, maxX: -7.1, minZ: 9.1, maxZ: 9.7, h: 0.6 });
```

`buildRack`（现 978）：
```js
    colliders.push({ minX: centerX - SHELF_W / 2, maxX: centerX + SHELF_W / 2, minZ: centerZ - SHELF_D / 2, maxZ: centerZ + SHELF_D / 2, h: SHELF_H });
```

墙段（`wallAlongX` / `wallAlongZ`）与 6 条世界边界围栏 **不动**（省略 `h` = 默认 3.6）。在 `wallAlongX` 上方的注释块里加一句：
```js
  /* collider 的可选字段 h（米）：只被 G.physics.syncStatics 读，省略视为 WALL_H。
     寻路与玩家碰撞只读 minX/maxX/minZ/maxZ 四个 2D 字段，加 h 对它们零影响。 */
```

- [ ] **Step 6: `js/world.js` —— 三处 `rebuildGraph()` 调用点各加一行**

`buildZone`（现 854）：
```js
    rebuildGraph();   // 该区货架建造新增 collider → 边失效，全量重建（微秒级）
    if (G.physics) G.physics.syncStatics();   // 静态刚体与 colliders 同源，绝不留第二份
```

`syncLayout`（现 1011）：
```js
    if (built > 0 && nav.entry) {
      rebuildGraph();
      if (G.physics) G.physics.syncStatics();
    }
```

`init`（现 1341）：
```js
    rebuildGraph();
    if (G.physics) G.physics.syncStatics();
```

> `if (G.physics)` 的守卫让 world.js 不硬依赖 physics.js（脚本缺失时游戏仍能跑）。

- [ ] **Step 7: `js/main.js` —— `boot()` 与 `tick()` 接入**

`boot()`（现 102-112），在 `buildRenderer();` 与 `G.world.init(scene);` 之间插入：
```js
    G.physics.init();          // 必须在 world.init 之前：world.init 会走到 rebuildGraph → syncStatics
```

`tick(dt)`（现 141-148），在 `G.player.update(dt);` 与 `G.customers.update(dt);` 之间插入：
```js
    G.physics.update(dt);      // 在 player 之后：松手那一帧箱当帧就飞出去；在 customers 之前：顾客看到的是本帧最新箱位
```

- [ ] **Step 8: `node --check js/physics.js js/world.js js/main.js`，两态自跑自测**

**关键回归面**（必须逐个确认仍绿）：`shadow.frustumCovers`（collider 加了 `h` 字段，断言只读 4 个 2D 字段，必须不受影响）、`nav.*` 全组（`segBlocked` 只读 2D 字段）、`world.shellSize`、`shop.delivery`、`world.boxShape`、`save.boxesRoundtrip`、`world.boxHardCap`、`perf.mainPassDrawables`、`perf.renderCalls`。
本 task 新增 6 条断言，两态都应 +6（正常 138 / lowfx 133）。

- [ ] **Step 9: 提交推送**

```bash
git add js/physics.js js/world.js js/main.js index.html
git commit -m "feat(D-T1): vendor cannon.js、静态刚体从 colliders 自动生成、collider 高度字段"
git -c credential.helper=wincred push origin main
```

---

## Task 2：箱三态与 `box.rb` 生命周期

**Files:**
- Modify: `js/world.js:785-796`（`storeBox`）、`js/world.js:798-803`（`takeBox`）、`js/world.js:1216-1229`（`destroyBox`）、`js/world.js:1232-1255`（`spawnBox`）、`js/world.js:1345-1384`（导出 `registerBoxInteractable`）
- Modify: `js/player.js:555-565`（`pickUpBox`）、`js/player.js:567-585`（`putDownBox`）、`js/player.js:52`（`_test`）
- Modify: `js/main.js`（`physics.*` 断言组补 6-8、10；`save.boxesNoDup` 之后补 9）

**Interfaces:**
- Consumes: T1 的 `G.physics.attach/attachStatic/detach/looseBoxes/update` 与 `_test.bodyCount/staticCount/stateOf`
- Produces:
  - `G.world.registerBoxInteractable(box)`（导出既有内部函数）
  - `G.player._test.pickUp(box) -> bool`、`G.player._test.putDown()`、`G.player._test.storeInto(slot) -> bool`
  - 不变式：`box.rb` 的有无与三态一一对应

### 三态与四个转移入口（本 task 的全部语义）

| 态 | 唯一判据 | 刚体 | 交互体 | mesh 位置的写入者 |
|---|---|---|---|---|
| **held** | `G.player.carrying === box` | 无（`box.rb === null`） | 无 | `player.syncCarriedBox()` 每帧 |
| **slotted** | `storageSlotOf(box) !== null` | **静态**（mass 0） | 有 | `world.storeBox()` 一次性写 |
| **loose** | 其余 | **动态** | 有 | `G.physics.update()` 每帧 |

| 入口 | 位置 | 态转移 | 物理动作 |
|---|---|---|---|
| 拿起 | `player.pickUpBox` | loose/slotted → held | `detach(box)` |
| 放下 | `player.putDownBox` | held → loose | `attach(carrying)` |
| 存入 | `world.storeBox` | held/loose → slotted | `detach` 后 `attachStatic` |
| 扔出 | `player.releaseThrow`（T3） | held → loose | `throwBox(...)` |
| 新箱 | `world.spawnBox` | — → loose | `attach(box)` |
| 取位 | `world.takeBox` | slotted → （调用方接手） | `detach(box)` |
| 销毁 | `world.destroyBox` | 任意 → 无 | `detach(box)` |
| 读档 | `world.restoreBoxes`（T6） | — → loose + `sleep()` | T6 处理 |

> **spec §5.3 说 `spawnBox` 要判断「`atPos` 是不是存储位」再决定 attach —— 本计划改为「一律 `attach`，由 `storeBox` 随后 `detach` + `attachStatic`」。** 理由：`spawnBox` 无从可靠判断一个坐标是不是存储位（`restoreStorage` / `restoreBoxes` / 自测都是先 `spawnBox(pid, slot.pos)` 再 `storeBox`），加判断只会引入一条会漂移的第二判据；一律 attach 后由 storeBox 覆盖是幂等的，多一次 add/remove，无成本。

- [ ] **Step 1: 写失败断言（`js/main.js`）**

在 T1 的 `ck('physics.noInterpolatedQuaternion', ...)` 之后追加：

```js
      /* --- D-T2 箱三态与刚体生命周期（spec §3.4 / §5）--- */
      var stSlotP = null;
      for (var spi = 0; spi < G.world.storageSlots.length; spi++) {
        if (!G.world.storageSlots[spi].box) { stSlotP = G.world.storageSlots[spi]; break; }
      }
      var pbox = G.world.spawnBox('f_noodle', { x: 6.0, z: 6.0 });
      var trail = [];
      if (pbox && stSlotP) {
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
        G.player._test.pickUp(pbox);
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
        G.player._test.putDown();
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
        G.player._test.pickUp(pbox);
        G.player._test.storeInto(stSlotP);
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
        G.player._test.pickUp(pbox);
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
        G.player._test.putDown();
        trail.push(G.physics._test.stateOf(pbox) + ':' + (pbox.rb ? pbox.rb.type : 'none'));
      }
      ck('physics.boxStates',
        trail.join('|') === 'loose:' + CANNON.Body.DYNAMIC + '|held:none|loose:' + CANNON.Body.DYNAMIC +
        '|slotted:' + CANNON.Body.STATIC + '|held:none|loose:' + CANNON.Body.DYNAMIC,
        '拿起→放下→存入→取出→放下 全环：' + trail.join(' → '));

      var slotStaticOk = false, slotDrift = -1;
      if (pbox && stSlotP) {
        G.player._test.pickUp(pbox);
        G.player._test.storeInto(stSlotP);
        if (pbox.rb && pbox.rb.type === CANNON.Body.STATIC) {
          pbox.rb.applyImpulse(new CANNON.Vec3(50, 0, 0), new CANNON.Vec3(pbox.rb.position.x, pbox.rb.position.y, pbox.rb.position.z));
          for (var sst = 0; sst < 60; sst++) G.physics._test.stepOnce();
          slotDrift = Math.sqrt(
            Math.pow(pbox.mesh.position.x - stSlotP.pos.x, 2) +
            Math.pow(pbox.mesh.position.z - stSlotP.pos.z, 2));
          slotStaticOk = slotDrift < 1e-6;
        }
      }
      ck('physics.slottedIsStatic', slotStaticOk,
        'slotted 箱须为 STATIC 且 50N 冲量 + 60 步后不离位，实测位移 ' + slotDrift);

      var dBefore = G.physics._test.bodyCount();
      var dRb = pbox ? pbox.rb : null;
      if (pbox) G.world.destroyBox(pbox);
      var dAfter = G.physics._test.bodyCount();
      var dGone = !!dRb && !G.physics._test.hasBody(dRb) && pbox.rb === null;
      ck('physics.destroyRemovesBody', dAfter === dBefore - 1 && dGone,
        'destroyBox 后 body 数 ' + dBefore + ' → ' + dAfter +
        '，原 body 已不在 world.bodies 且 box.rb 已置空=' + dGone);

      var mfBox = G.world.spawnBox('d_water', { x: 6.0, z: 5.0 });
      var mfOk = false, mfDetail = '无箱';
      if (mfBox && mfBox.rb) {
        mfBox.rb.wakeUp();
        mfBox.rb.velocity.set(0.6, 2.0, 0.4);
        mfBox.rb.angularVelocity.set(2, 3, 1);
        for (var mfi = 0; mfi < 30; mfi++) G.physics.update(1 / 60);
        var mfDp = Math.sqrt(
          Math.pow(mfBox.mesh.position.x - mfBox.rb.interpolatedPosition.x, 2) +
          Math.pow(mfBox.mesh.position.y - mfBox.rb.interpolatedPosition.y, 2) +
          Math.pow(mfBox.mesh.position.z - mfBox.rb.interpolatedPosition.z, 2));
        var mfDq = Math.max(
          Math.abs(mfBox.mesh.quaternion.x - mfBox.rb.quaternion.x),
          Math.abs(mfBox.mesh.quaternion.y - mfBox.rb.quaternion.y),
          Math.abs(mfBox.mesh.quaternion.z - mfBox.rb.quaternion.z),
          Math.abs(mfBox.mesh.quaternion.w - mfBox.rb.quaternion.w));
        // 箱必须真的转过：否则「写回 quaternion」与「压根没写」在数值上无法区分（空断言）
        var mfSpun = Math.abs(mfBox.mesh.quaternion.w - 1) > 1e-3;
        mfOk = mfDp < 1e-6 && mfDq < 1e-6 && mfSpun;
        mfDetail = 'Δpos ' + mfDp + '，Δquat ' + mfDq + '，已转过（quat.w=' +
          round2(mfBox.mesh.quaternion.w) + '）=' + mfSpun;
      }
      ck('physics.meshFollowsBody', mfOk, '30 帧后 mesh 必须逐字跟随 body：' + mfDetail);
      if (mfBox) G.world.destroyBox(mfBox);
```

并在 `ck('save.boxesNoDup', ...)`（现 `js/main.js:1222-1227`）**之后**追加：

```js
      var orphanLoose = G.physics.looseBoxes().length;
      var orphanNonHeld = 0, orphanList = G.world.allBoxes();
      for (var obi = 0; obi < orphanList.length; obi++) {
        if (G.player.carrying !== orphanList[obi]) orphanNonHeld++;
      }
      ck('physics.noOrphanBodies',
        G.physics._test.bodyCount() === G.physics._test.staticCount() + orphanNonHeld,
        '连读 3 次后 body 总数 ' + G.physics._test.bodyCount() + ' = 静态 ' +
        G.physics._test.staticCount() + ' + 非 held 箱 ' + orphanNonHeld + '；其中 loose ' + orphanLoose);
```

- [ ] **Step 2: 自跑自测确认 FAIL**

**期望的失败信息**：`physics.boxStates` detail 形如 `拿起→放下→存入→取出→放下 全环：loose:1 → held:1 → loose:1 → slotted:1 → held:1 → loose:1`（`held` 仍带刚体、`slotted` 仍是 DYNAMIC=1），且很可能直接 `selftest.exception`（`G.player._test.pickUp is not a function`）。若抛异常，先只加 `physics.noOrphanBodies` 一条跑一次，确认它红（`body 总数 46 = 静态 46 + 非 held 箱 3` 不成立）。

- [ ] **Step 3: `js/world.js` 实现**

`storeBox`（现 785-796），在 `registerBoxInteractable(box);` **之前**插入：
```js
    // slotted 锁死为静态刚体：休眠动态体被撞醒会离开存储位而 slot.box 仍指着它
    if (G.physics) { G.physics.detach(box); G.physics.attachStatic(box); }
```

`takeBox`（现 798-803），改为：
```js
  function takeBox(slot) {
    if (!slot || !slot.box) return null;
    var box = slot.box;
    slot.box = null;
    if (G.physics) G.physics.detach(box);   // 调用方接手定位；不留静态刚体钉在原位
    return box;
  }
```

`destroyBox`（现 1216），在 `if (!box || !box.mesh) return;` 之后插入：
```js
    if (G.physics) G.physics.detach(box);
```

`spawnBox`（现 1232-1255），把结尾两行改为：
```js
    var box = { mesh: group, body: body, label: label, productId: pid, itemsLeft: product.boxSize, rb: null };
    registerBoxInteractable(box);
    // 一律建动态刚体；落到存储位的箱由随后的 storeBox 覆盖成静态（幂等，多一次 add/remove）
    if (G.physics) G.physics.attach(box);
    return box;
```

导出（现 1345-1384 的对象字面量），在 `destroyBox: destroyBox,` 之后加一行：
```js
    registerBoxInteractable: registerBoxInteractable,
```

- [ ] **Step 4: `js/player.js` 实现**

`pickUpBox`（现 555-565），在 `if (G.world && G.world.releaseStorageOf) G.world.releaseStorageOf(box);` 之后插入：
```js
    if (G.physics) G.physics.detach(box);   // held 态无刚体
```

`putDownBox`（现 567-585），把 `G.player.carrying = null;` 那一行改为：
```js
    if (G.physics) G.physics.attach(carrying);   // held → loose，零初速
    G.player.carrying = null;
```

`_test`（现 52），整体替换为：
```js
    _test: {
      prompt: function (entry) { return computePrompt(entry); },
      pickUp: function (box) {
        var list = (G.world && G.world.interactables) || [];
        for (var i = 0; i < list.length; i++) {
          if (list[i].type === 'box' && list[i].data && list[i].data.box === box) {
            pickUpBox(list[i]);
            return true;
          }
        }
        return false;
      },
      putDown: function () { putDownBox(); },
      storeInto: function (slot) {
        storeCarriedBox({ data: { slot: slot } });
        return G.player.carrying === null;
      }
    }
```

> `storeCarriedBox` 内部会 `clearHover()`，走的是玩家的真实路径，正是我们要测的。

- [ ] **Step 5: `node --check js/world.js js/player.js js/main.js`，两态自跑自测**

**关键回归面**：`world.storageStore` / `world.storageTake` / `world.storageRoundtrip` / `world.storageRestore`（存储位路径现在多了刚体切换）、`save.boxesRoundtrip` / `save.boxesNoDup`（读档不得留孤儿刚体）、`world.boxHardCap`、`shop.delivery`、`world.boxShape`、`perf.mainPassDrawables`。
本 task 新增 5 条，两态 +5（正常 143 / lowfx 138）。

- [ ] **Step 6: 提交推送**

```bash
git add js/world.js js/player.js js/main.js
git commit -m "feat(D-T2): 箱三态与 box.rb 生命周期，每帧写回 mesh"
git -c credential.helper=wincred push origin main
```

---

## Task 3：蓄力投掷（右键）与 HUD 力度条

**Files:**
- Modify: `js/player.js`（常量区、`init` 事件注册、`onMouseDown` / `onMouseUp`、`update` 三处、新增 `stepCharge` / `cancelCharge` / `releaseThrow` / `setChargeUI`、`_test` 扩充）
- Modify: `js/ui.js:749-772`（`buildHud`）、`js/ui.js:892-897`（导出）
- Modify: `index.html:19`（`#charge` 节点）
- Modify: `css/style.css:65-75`（选择器组）、`:154-164` 之后（`#charge` 样式）
- Modify: `js/main.js`（`player.*` / `ui.*` 新断言追加到既有 `player` 分组之后，即 `ck('player.poseApi', ...)` 之后）

**Interfaces:**
- Consumes: T2 的 `G.physics.throwBox`、`G.world.registerBoxInteractable`、`G.player._test.pickUp`
- Produces:
  - `G.ui.setCharge(v)`
  - `G.player._test.throwConsts()` → `{chargeFull:0.8, vMin:1.5, vMax:9.0, spinMin:1.5, spinMax:7.0}`
  - `G.player._test.charge(tSec)` / `.chargeState()` / `.peekThrow()` / `.throwNow()` / `.setRmb(down)` / `.stepCharge(dt)` / `.setHover(entry)` / `.setMouseDown(down)` / `.doInteractions()`
  - DOM `#charge`

### 数值表（spec §6.3，逐字进代码；实调后必须回写 spec）

| 参数 | 常量名 | 值 |
|---|---|---:|
| 蓄力满时长 | `CHARGE_FULL` | 0.8 s |
| 蓄力曲线 | — | 线性 `charge = clamp(t / 0.8, 0, 1)` |
| 最小出手速度 | `THROW_V_MIN` | 1.5 m/s |
| 最大出手速度 | `THROW_V_MAX` | 9.0 m/s（硬上界 12.0，见 Global Constraints 第 6 条） |
| 角速度上限（轻 / 满） | `THROW_SPIN_MIN` / `THROW_SPIN_MAX` | 1.5 / 7.0 rad/s，三轴各取 `U(−S, +S)` |

- [ ] **Step 1: 写失败断言（`js/main.js`，插在 `ck('player.poseApi', ...)` 那一段之后）**

```js
      /* --- D-T3 蓄力投掷（spec §6）--- */
      var tc = G.player._test.throwConsts ? G.player._test.throwConsts() : null;
      var curveOk = !!tc, curveTrace = [];
      if (tc) {
        [0, 0.4, 0.8, 1.5].forEach(function (ct) {
          G.player._test.charge(ct);
          var got = G.player._test.peekThrow().speed;
          var k = Math.min(ct / tc.chargeFull, 1);
          var want = tc.vMin + (tc.vMax - tc.vMin) * k;
          curveTrace.push('t=' + ct + '→' + round2(got) + '/' + round2(want));
          if (Math.abs(got - want) > 1e-6) curveOk = false;
        });
        G.player._test.charge(0);
      }
      ck('player.chargeCurve', curveOk, curveTrace.join(' ') || '缺 _test.throwConsts');

      var toBox = G.world.spawnBox('h_tissue', { x: 6.0, z: 4.0 });
      var toOk = false, toDetail = '无箱';
      if (toBox && G.player._test.pickUp(toBox)) {
        var camDir = new THREE.Vector3();
        G.player.camera.getWorldDirection(camDir);
        var want = G.player.camera.position.clone().addScaledVector(camDir, 0.6);
        want.y -= 0.35;
        G.player._test.charge(0.4);
        withRandom(0.5, function () { G.player._test.throwNow(); });
        toOk = toBox.mesh.position.distanceTo(want) < 1e-6 && !!toBox.rb &&
          toBox.rb.type === CANNON.Body.DYNAMIC && G.player.carrying === null;
        toDetail = '箱心 ' + round2(toBox.mesh.position.x) + ',' + round2(toBox.mesh.position.y) + ',' +
          round2(toBox.mesh.position.z) + ' / 期望 ' + round2(want.x) + ',' + round2(want.y) + ',' + round2(want.z) +
          '，距差 ' + toBox.mesh.position.distanceTo(want);
      }
      ck('player.throwOrigin', toOk, '出手起点须与 syncCarriedBox 同式：' + toDetail);
      if (toBox) G.world.destroyBox(toBox);

      /* 蓄力仲裁：准星指向任何类型都不影响蓄力；左键既有行为不受影响、可与右键同时生效；
         中途失效（全屏界面）取消蓄力且箱仍在手上 */
      var arbBox = G.world.spawnBox('f_noodle', { x: 6.0, z: 3.0 });
      var arbOk = !!arbBox, arbDetail = '';
      if (arbBox && G.player._test.pickUp(arbBox)) {
        var arbTypes = ['shelfSlot', 'computer', 'register', 'shutter', 'storage', 'trash', null];
        for (var ai = 0; ai < arbTypes.length; ai++) {
          var ent = null;
          if (arbTypes[ai]) {
            for (var aj = 0; aj < G.world.interactables.length; aj++) {
              if (G.world.interactables[aj].type === arbTypes[ai]) { ent = G.world.interactables[aj]; break; }
            }
          }
          G.player._test.charge(0);
          G.player._test.setHover(ent);
          G.player._test.setRmb(true);
          G.player._test.stepCharge(0.4);
          if (Math.abs(G.player._test.chargeState().charge - 0.5) > 1e-6) {
            arbOk = false; arbDetail = '准星指 ' + arbTypes[ai] + ' 时蓄力 ' + G.player._test.chargeState().charge;
            break;
          }
        }
        // 左键连续上架与右键蓄力同时生效
        var arbSlotEnt = null;
        for (var ak = 0; ak < G.world.interactables.length; ak++) {
          var ake = G.world.interactables[ak];
          if (ake.type === 'shelfSlot' && ake.data.slot.fridge === false &&
              (ake.data.slot.productId === null || ake.data.slot.productId === 'f_noodle') &&
              ake.data.slot.count < 4) { arbSlotEnt = ake; break; }
        }
        if (arbSlotEnt) {
          var arbCount0 = arbSlotEnt.data.slot.count, arbLeft0 = arbBox.itemsLeft;
          G.player._test.charge(0);
          G.player._test.resetStockCooldown();   // 前序断言可能刚上过架，冷却未走完会让本条空转
          G.player._test.setHover(arbSlotEnt);
          G.player._test.setRmb(true);
          G.player._test.setMouseDown(true);
          G.player._test.doInteractions();
          G.player._test.stepCharge(0.4);
          var arbStocked = arbSlotEnt.data.slot.count === arbCount0 + 1 && arbBox.itemsLeft === arbLeft0 - 1;
          var arbCharged = Math.abs(G.player._test.chargeState().charge - 0.5) < 1e-6;
          if (!arbStocked || !arbCharged) {
            arbOk = false;
            arbDetail = '左键上架=' + arbStocked + '（' + arbCount0 + '→' + arbSlotEnt.data.slot.count +
              '），同帧右键蓄力=' + G.player._test.chargeState().charge;
          }
          G.player._test.setMouseDown(false);
        } else {
          arbOk = false; arbDetail = '找不到可上架的空格位';
        }
        /* 中途失效：全屏界面（'screen' 事件 → update 的 blocked 早退分支）必须取消蓄力、箱仍在手上。
           `inRegister` 走的是同一个 blocked 判据（blocked = screenIsOpen || registerActive），
           无头下失去指针锁定走的是第二个早退分支，两处都调 cancelCharge。 */
        G.player._test.setRmb(true);
        G.player._test.charge(0.4);
        G.bus.emit('screen', { name: 'computer' });
        G.player.update(0.05);
        var arbCancelled = G.player._test.chargeState().charge === 0 && G.player.carrying === arbBox;
        G.bus.emit('screen', { name: null });
        if (!arbCancelled) { arbOk = false; arbDetail = (arbDetail ? arbDetail + '；' : '') + '全屏界面未取消蓄力'; }
        G.player._test.setRmb(false);
        G.player._test.charge(0);
      } else {
        arbOk = false; arbDetail = '拿不起测试箱';
      }
      ck('player.chargeArbitration', arbOk, arbDetail || '任意准星目标均可蓄力；左键上架与右键蓄力互不影响；中途失效取消蓄力');

      var barEl = document.getElementById('charge');
      var barOk = !!barEl && !!barEl.firstChild;
      var barDetail = barEl ? '' : '缺 #charge 节点';
      if (barOk) {
        G.ui.setCharge(0.5);
        var barVis = barEl.style.display !== 'none' && barEl.firstChild.style.width === '60px';
        G.ui.setCharge(null);
        var barHid = barEl.style.display === 'none';
        barOk = barVis && barHid;
        barDetail = 'charge=0.5 时 display=' + barEl.style.display + ' 宽 ' + barEl.firstChild.style.width +
          '（应 60px）；setCharge(null) 后 display=' + barEl.style.display;
      }
      ck('ui.chargeBar', barOk, barDetail);

      var cmEvt = new MouseEvent('contextmenu', { bubbles: true, cancelable: true });
      var cmSuppressed = (canvasEl.dispatchEvent(cmEvt) === false);
      ck('player.contextMenuSuppressed', cmSuppressed,
        'canvas 上的 contextmenu 必须 preventDefault（dispatchEvent 返回 ' + !cmSuppressed + '）');
      if (arbBox) { if (G.player.carrying === arbBox) G.player.carrying = null; G.world.destroyBox(arbBox); }
```

- [ ] **Step 2: 自跑自测确认 FAIL**

**期望的失败信息**：`player.chargeCurve` detail = `缺 _test.throwConsts`；`ui.chargeBar` detail = `缺 #charge 节点`；`player.contextMenuSuppressed` detail = `...（dispatchEvent 返回 true）`；`player.throwOrigin` / `player.chargeArbitration` 因 `_test` 钩子缺失多半直接抛 → `selftest.exception`。若抛异常，先只保留 `ui.chargeBar` 与 `player.contextMenuSuppressed` 两条跑一次确认红，再补齐其余。

- [ ] **Step 3: `index.html` —— 在 `<div id="crosshair"></div>` 之后加一行**

```html
<div id="charge"></div>
```

- [ ] **Step 4: `css/style.css`**

把选择器组（现 65-75）里的 `#crosshair,` 之后加一行 `#charge,`：
```css
#hud-money,
#hud-day,
#hud-clock,
#hud-level,
#crosshair,
#charge,
#prompt,
#toast {
  position: fixed;
  pointer-events: none;
  z-index: 10;
}
```

在 `#crosshair { ... }` 规则之后插入：
```css
/* 蓄力力度条：与 #hud-level 的 XP 条同规格（DESIGN §3），不引入新视觉语言 */
#charge {
  top: 50%;
  left: 50%;
  margin-top: 28px;
  transform: translateX(-50%);
  width: 120px;
  height: 4px;
  background: var(--line);
  border-radius: 2px;
  overflow: hidden;
  display: none;
}
#charge > div {
  height: 100%;
  width: 0;
  background: var(--accent-2);
  border-radius: 2px;
}
```

- [ ] **Step 5: `js/ui.js`**

模块状态行（现 50）末尾加两个变量：
```js
  var moneyValEl, levelValEl, xpFillEl, hudDayEl, hudClockEl, promptEl, crosshairEl, toastEl;
  var chargeEl = null, chargeFillEl = null;
```

`buildHud()` 内，`crosshairEl = document.getElementById('crosshair');` 之后插入：
```js
    chargeEl = document.getElementById('charge');
    if (chargeEl) {
      chargeFillEl = document.createElement('div');
      chargeEl.appendChild(chargeFillEl);
      chargeEl.style.display = 'none';
    }
```

在 `function onHover(p) { ... }` 之后插入：
```js
  /* 蓄力力度条：v ∈ [0,1] 显示并设填充宽度；null 隐藏。填充宽度口径 = charge × 120px（DESIGN §3） */
  function setCharge(v) {
    if (!chargeEl || !chargeFillEl) return;
    if (v === null || v === undefined) { chargeEl.style.display = 'none'; return; }
    chargeEl.style.display = 'block';
    chargeFillEl.style.width = (G.clamp(v, 0, 1) * 120) + 'px';
  }
```

导出对象加一行：
```js
  G.ui = {
    init: init,
    showScreen: showScreen,
    prompt: setPrompt,
    toast: toast,
    setCharge: setCharge
  };
```

- [ ] **Step 6: `js/player.js` 实现**

常量区（现 7-12）之后加：
```js
  // ---- D-T3 蓄力投掷（spec §6.3 数值表；改这里必须回写 spec）----
  var CHARGE_FULL = 0.8;
  var THROW_V_MIN = 1.5;
  var THROW_V_MAX = 9.0;      // 硬上界 12.0（cannon 0.6.2 无 CCD，防隧穿），取 25% 余量
  var THROW_SPIN_MIN = 1.5;
  var THROW_SPIN_MAX = 7.0;
```

模块状态区（现 31-37）加：
```js
  var charge = 0;
  var rmbDown = false;
  var rmbReleased = false;
  var chargeShown = false;
```

`init()` 内，`domElement.addEventListener('mouseup', onMouseUp);` 之后插入：
```js
    domElement.addEventListener('contextmenu', onContextMenu);
```

`onMouseDown` / `onMouseUp` 改为（**右键分支必须在左键早退之前**）：
```js
  function onMouseDown(e) {
    if (e.button === 2) {
      if (isLocked()) rmbDown = true;
      return;
    }
    if (e.button !== 0) return;
    if (!isLocked()) return;
    mouseDown = true;
    mouseJustPressed = true;
  }

  function onMouseUp(e) {
    if (e.button === 2) {
      if (rmbDown) rmbReleased = true;
      rmbDown = false;
      return;
    }
    if (e.button !== 0) return;
    mouseDown = false;
  }

  /* 指针锁定下多数浏览器已抑制右键菜单，但不得依赖：显式阻止（断言 player.contextMenuSuppressed） */
  function onContextMenu(e) { e.preventDefault(); }
```

`update(dt)`：
1. blocked 早退分支（现 187-196）内，`oJustPressed = false;` 之后加一行：
```js
      cancelCharge();
```
2. 未锁定早退分支（现 217-223）内，`qJustPressed = false;` 之后加一行：
```js
      cancelCharge();
```
3. `doInteractions();` 之后加一行：
```js
    stepCharge(dt);
```

在 `function syncCarriedBox() { ... }` 之后插入整块：
```js
  // ---- 蓄力与投掷 ----
  function setChargeUI(v) {
    if (v === null) {
      if (!chargeShown) return;
      chargeShown = false;
    } else {
      chargeShown = true;
    }
    if (G.ui && G.ui.setCharge) G.ui.setCharge(v);
  }

  function throwSpeed() { return THROW_V_MIN + (THROW_V_MAX - THROW_V_MIN) * charge; }
  function throwSpinMax() { return THROW_SPIN_MIN + (THROW_SPIN_MAX - THROW_SPIN_MIN) * charge; }

  function stepCharge(dt) {
    if (rmbReleased) {
      rmbReleased = false;
      releaseThrow();
      return;
    }
    if (!rmbDown || !G.player.carrying) {
      if (charge !== 0) charge = 0;
      setChargeUI(null);
      return;
    }
    charge = G.clamp(charge + dt / CHARGE_FULL, 0, 1);
    setChargeUI(charge);
  }

  /* 中途失效（inRegister / 全屏界面 / 失去指针锁定）：取消蓄力，箱留在手上 */
  function cancelCharge() {
    rmbDown = false;
    rmbReleased = false;
    charge = 0;
    setChargeUI(null);
  }

  function releaseThrow() {
    var carrying = G.player.carrying;
    var speed = throwSpeed(), spinMax = throwSpinMax();
    charge = 0;
    rmbDown = false;
    setChargeUI(null);
    if (!carrying || !carrying.mesh || !camera) return;
    // 起点 = 松手瞬间箱心的当前位置，与 syncCarriedBox 逐字同式（玩家看到的是「手上那只直接飞出去」）
    syncCarriedBox();
    camera.getWorldDirection(_camDir);
    G.player.carrying = null;
    if (G.world && G.world.registerBoxInteractable) G.world.registerBoxInteractable(carrying);
    if (G.physics && G.physics.throwBox) {
      G.physics.throwBox(carrying, _camDir, speed, {
        x: G.rand(-spinMax, spinMax),
        y: G.rand(-spinMax, spinMax),
        z: G.rand(-spinMax, spinMax)
      });
    }
  }
```

`_test`（T2 已改过一次），整体替换为：
```js
    _test: {
      prompt: function (entry) { return computePrompt(entry); },
      pickUp: function (box) {
        var list = (G.world && G.world.interactables) || [];
        for (var i = 0; i < list.length; i++) {
          if (list[i].type === 'box' && list[i].data && list[i].data.box === box) {
            pickUpBox(list[i]);
            return true;
          }
        }
        return false;
      },
      putDown: function () { putDownBox(); },
      storeInto: function (slot) {
        storeCarriedBox({ data: { slot: slot } });
        return G.player.carrying === null;
      },
      throwConsts: function () {
        return { chargeFull: CHARGE_FULL, vMin: THROW_V_MIN, vMax: THROW_V_MAX,
          spinMin: THROW_SPIN_MIN, spinMax: THROW_SPIN_MAX };
      },
      charge: function (tSec) { charge = G.clamp(tSec / CHARGE_FULL, 0, 1); setChargeUI(charge); return charge; },
      chargeState: function () { return { charge: charge, rmbDown: rmbDown }; },
      peekThrow: function () {
        if (camera) camera.getWorldDirection(_camDir);
        return { speed: throwSpeed(), spinMax: throwSpinMax(), dir: _camDir.clone() };
      },
      throwNow: function () { releaseThrow(); },
      setRmb: function (down) { rmbDown = !!down; },
      stepCharge: function (dt) { stepCharge(dt); },
      setHover: function (entry) { hovered = entry || null; },
      setMouseDown: function (down) { mouseDown = !!down; },
      doInteractions: function () { doInteractions(); },
      resetStockCooldown: function () { stockCooldown = 0; }
    }
```

- [ ] **Step 7: `node --check js/player.js js/ui.js js/main.js`，两态自跑自测**

**关键回归面**：`player.poseApi`、`checkout.stance*` 全组（player 让出相机控制权的路径新增了 `cancelCharge`）、`world.stock`（左键上架路径）、`world.tagState*`、`shop.*`、`ui` 无独立断言但 `#charge` 不得影响 `#crosshair` 布局。
本 task 新增 5 条，两态 +5（正常 148 / lowfx 143）。
**手感自查（写进报告）**：`stockCooldown` 与右键蓄力同时按住时不得互相吞事件。

- [ ] **Step 8: 提交推送**

```bash
git add js/player.js js/ui.js js/main.js index.html css/style.css
git commit -m "feat(D-T3): 右键蓄力投掷与 HUD 力度条"
git -c credential.helper=wincred push origin main
```

---

## Task 4：顾客被砸倒地

**Files:**
- Modify: `js/customers.js:184`（`buildBody` 设 `rotation.order`）、`:240-279`（顾客对象新字段）、`:481-501`（`update` 抽出 `stepCustomer`）、新增 `findKnockBox` / `stepKnockdown`、`:512-534`（`_test` 扩充）
- Modify: `js/main.js`（`cust.*` 新断言追加到 `ck('cust.gaitDecay', ...)` 之后；`cust.knockdownNoConsequence` 单独放在 `ck('customer.queued', ...)` 之后）

**Interfaces:**
- Consumes: T2 的 `G.physics.looseBoxes()`、`box.rb.velocity`
- Produces:
  - `c.ragdoll:bool`、`c.ragdollT:number`、`c.ragdollDir:±1`
  - `G.customers._test.stepOne(c, dt)`（跑一名顾客的完整每帧流程，与 `update()` 循环体同源同函数）
  - `G.customers._test.spawnOne()` 返回对象新增 `id` / `path` / `lane` / `avoid` / `avoidApplied` / `avoidSide` / `ragdoll` / `ragdollT` / `ragdollDir` 字段（T5 要用，本 task 一次加齐）

### 判定与时序（spec §7）

- **箱侧**：等效球，半径 0.30。**触发阈值**：`rb.velocity` 模长 ≥ **2.5 m/s**。
- **顾客侧**：直立圆柱，旋转不变。半径 `r = 0.22 × c.dims.w + 0.13`（0.32–0.40），高 `top = 1.77 × c.dims.h`（1.59–1.91）。
- **判定**：水平距离 `< r + 0.30` 且球心 `y ∈ [−0.30, top + 0.30]`。
- **朝向**：`dot(v, forward) > 0`（从背后砸来）→ 前扑 `rotation.x = +π/2`；否则后仰 `−π/2`。`forward = (sin(rot.y), 0, cos(rot.y))`。
- **时序**（单变量 `c.ragdollT`）：0→0.25s 线性倒下；0.25→2.50s 躺平；2.50→2.85s 线性爬起；≥2.85s 结束（`ragdoll=false`、`rotation.x=0`、`ragdollT=0`）。
- **倒地期间再次被砸**：不重播倒下动作，只把躺平计时拉回 `c.ragdollT = Math.min(c.ragdollT, 0.25)`。
- **冻结**：`applyGait` / `stepMove` / `stepAvoid` 冻结；**`stepState` 照常推进**（耐心继续累计）；`c.hands` 随根节点旋转不散落；金钱 / XP / `dayStats.cogs` / 满意度 / toast 一律不动。

- [ ] **Step 1: 写失败断言（`js/main.js`，插在 `ck('cust.gaitDecay', ...)` 那一段之后、`world.cashierFigure` 块之前）**

```js
      /* --- D-T4 被砸倒地（spec §7）--- */
      function makeKnockBox(c, speed) {
        var p = c.mesh.position;
        var b = G.world.spawnBox('f_noodle', { x: p.x, z: p.z - 0.2 });
        if (!b) return null;
        b.mesh.position.set(p.x, 1.0, p.z - 0.2);
        if (b.rb) {
          b.rb.position.set(p.x, 1.0, p.z - 0.2);
          b.rb.wakeUp();
          b.rb.velocity.set(0, 0, speed);
        }
        return b;
      }
      var kc = G.customers._test.spawnOne();
      kc.mesh.position.set(6.0, 0, 0.0);
      kc.mesh.rotation.y = 0;                       // 正前方 = 局部 +Z
      var kbox = makeKnockBox(kc, 5.0);
      G.customers._test.stepOne(kc, 0.05);          // 触发帧：只置位，姿态从下一帧起推进
      var kTriggered = kc.ragdoll === true;
      var kOrder = kc.mesh.rotation.order;
      for (var kfi = 0; kfi < 5; kfi++) G.customers._test.stepOne(kc, 0.05);   // 累计 0.25s
      var kFallen = Math.abs(Math.abs(kc.mesh.rotation.x) - Math.PI / 2) < 0.01;
      ck('cust.knockdown', kTriggered && kOrder === 'YXZ' && kFallen,
        'ragdoll=' + kc.ragdoll + '，rotation.order=' + kOrder +
        '，0.25s 后 rotation.x=' + round2(kc.mesh.rotation.x) + '（应 ±' + round2(Math.PI / 2) + '）');
      if (kbox) G.world.destroyBox(kbox);

      var sc2 = G.customers._test.spawnOne();
      sc2.mesh.position.set(6.0, 0, 2.0);
      sc2.mesh.rotation.y = 0;
      var sbox = makeKnockBox(sc2, 1.0);            // < 2.5 m/s
      for (var sfi = 0; sfi < 10; sfi++) G.customers._test.stepOne(sc2, 0.05);
      ck('cust.slowBoxNoKnockdown', sc2.ragdoll !== true,
        '1.0 m/s 的箱不得触发倒地，实测 ragdoll=' + sc2.ragdoll);
      if (sbox) G.world.destroyBox(sbox);

      var kp0 = kc.mesh.position.clone();
      var kTorso0 = kc.torso.position.y;
      var kFrozen = true;
      for (var kri = 0; kri < 53; kri++) {           // 已推进 0.25s，再推进 2.65s → 合计 2.90s ≥ 2.85
        G.customers._test.stepOne(kc, 0.05);
        if (kc.ragdoll && (kc.torso.position.y !== kTorso0 || kc.mesh.position.distanceTo(kp0) > 1e-9)) kFrozen = false;
      }
      ck('cust.knockdownRecovers',
        kc.ragdoll === false && kc.mesh.rotation.x === 0 && kc.mesh.position.distanceTo(kp0) < 1e-6,
        '2.9s 后 ragdoll=' + kc.ragdoll + '，rotation.x=' + kc.mesh.rotation.x +
        '，位移 ' + kc.mesh.position.distanceTo(kp0));
      ck('cust.ragdollFreezesGait', kFrozen,
        '倒地期间 torso.position.y 与 mesh.position 必须一帧不动（applyGait 与 stepMove 双冻结）');

      var hc = G.customers._test.spawnOne();
      hc.mesh.position.set(6.0, 0, -2.0);
      hc.mesh.rotation.y = 0;
      G.customers._test.addHand(hc, 'f_noodle');
      G.customers._test.addHand(hc, 'd_water');
      var hHands0 = hc.hands.children.length;
      var hbox = makeKnockBox(hc, 5.0);
      for (var hfi = 0; hfi < 10; hfi++) G.customers._test.stepOne(hc, 0.05);
      ck('cust.knockdownKeepsHands',
        hc.ragdoll === true && hc.hands.children.length === hHands0 && hc.hands.parent === hc.mesh,
        '倒地中手持 ' + hc.hands.children.length + '/' + hHands0 + ' 件，hands.parent === mesh=' +
        (hc.hands.parent === hc.mesh));
      if (hbox) G.world.destroyBox(hbox);

      var pc2 = G.customers._test.spawnOne();
      pc2.mesh.position.set(6.0, 0, -4.0);
      pc2.mesh.rotation.y = 0;
      pc2.state = 'queueing';
      pc2.patience = 0;
      var pbox2 = makeKnockBox(pc2, 5.0);
      for (var pfi = 0; pfi < 57; pfi++) G.customers._test.stepOne(pc2, 0.05);   // 2.85s
      if (pbox2) G.world.destroyBox(pbox2);
      ck('cust.knockdownPatienceRuns', Math.abs(pc2.patience - 2.85) < 0.05,
        '倒地 2.85s 期间 patience 增量 ' + round2(pc2.patience) + '（应 ≈2.85，证明 stepState 照常推进）');
      G.customers._test.remove(kc);
      G.customers._test.remove(sc2);
      G.customers._test.remove(hc);
      G.customers._test.remove(pc2);
```

并在 `ck('customer.queued', ready, ...)`（现 `js/main.js:1006`）**之后**插入：

```js
      /* D-T4 #23：对真实排队顾客做同一测试——交易状态机与经济一律不动 */
      var ncCust = null, ncRegs = G.checkout._test.registers();
      for (var nci = 0; nci < ncRegs.length && !ncCust; nci++) {
        var ncQ = ncRegs[nci].queue;
        if (ncQ && ncQ.length) ncCust = ncQ[0];
      }
      var ncOk = false, ncDetail = '队列里没有顾客，无法测';
      if (ncCust) {
        var ncPhase0 = G.checkout._test.tx(0) ? G.checkout._test.tx(0).phase : null;
        var ncMoney0 = G.state.money, ncXp0 = G.state.xp, ncCogs0 = G.state.dayStats.cogs;
        var ncPos0 = ncCust.mesh.position.clone();
        var ncBox = G.world.spawnBox('f_noodle', { x: ncCust.mesh.position.x, z: ncCust.mesh.position.z - 0.2 });
        if (ncBox) {
          ncBox.mesh.position.set(ncCust.mesh.position.x, 1.0, ncCust.mesh.position.z - 0.2);
          if (ncBox.rb) { ncBox.rb.position.set(ncBox.mesh.position.x, 1.0, ncBox.mesh.position.z); ncBox.rb.wakeUp(); ncBox.rb.velocity.set(0, 0, 5.0); }
        }
        G.customers._test.stepOne(ncCust, 0.05);
        var ncRag = ncCust.ragdoll === true;
        if (ncBox) G.world.destroyBox(ncBox);
        for (var ncj = 0; ncj < 57; ncj++) G.customers._test.stepOne(ncCust, 0.05);
        var ncPhase1 = G.checkout._test.tx(0) ? G.checkout._test.tx(0).phase : null;
        ncOk = ncRag && ncPhase1 === ncPhase0 &&
          G.state.money === ncMoney0 && G.state.xp === ncXp0 && G.state.dayStats.cogs === ncCogs0 &&
          ncCust.mesh.position.distanceTo(ncPos0) < 1e-6 && ncCust.ragdoll === false;
        ncDetail = '被砸=' + ncRag + '，tx.phase ' + ncPhase0 + '→' + ncPhase1 +
          '，money ' + ncMoney0 + '→' + G.state.money + '，xp ' + ncXp0 + '→' + G.state.xp +
          '，cogs ' + ncCogs0 + '→' + G.state.dayStats.cogs + '，位移 ' + ncCust.mesh.position.distanceTo(ncPos0);
      }
      ck('cust.knockdownNoConsequence', ncOk, ncDetail);
```

- [ ] **Step 2: 自跑自测确认 FAIL**

**期望的失败信息**：多半直接 `selftest.exception`（`G.customers._test.stepOne is not a function`）。若如此，先只加 `cust.knockdown` 一条并把 `stepOne` 换成不存在的调用跑一次确认异常，再实现。实现后期望 `cust.knockdown` detail 从 `ragdoll=undefined，rotation.order=XYZ` 变绿。

- [ ] **Step 3: `js/customers.js` 实现**

模块常量区（现 30 `var bodyMats = {};` 之后）加：
```js
  /* ---------- D-T4 被砸倒地（spec §7）---------- */
  var KNOCK_SPEED2 = 2.5 * 2.5;   // 触发阈值 2.5 m/s，比模长平方省一次开方
  var BOX_HIT_R = 0.30;           // 箱等效球半径（介于内切 0.225 与外接 0.39 之间）
  var RAGDOLL_FALL = 0.25;        // 倒下动作
  var RAGDOLL_HOLD = 2.50;        // 躺平结束（决策 4「倒地 2.5 秒」含倒下动作）
  var RAGDOLL_END = 2.85;         // 爬起结束
```

`buildBody()` 内，`var g = new THREE.Group();` 之后插入：
```js
    /* 倒地必须绕「朝向之后的自身左右轴」转：默认 XYZ 下 rotation.x 绕世界 X 轴，
       顾客朝 ±z 时会歪着倒。rotation.x === 0 时两种 order 完全等价，对既有行为零影响。 */
    g.rotation.order = 'YXZ';
```

`spawn()` 的顾客对象字面量里，`lane: G.rand(-0.35, 0.35),` 之后插入：
```js
      avoid: 0,
      avoidApplied: 0,
      avoidSide: 0,
      ragdoll: false,
      ragdollT: 0,
      ragdollDir: 1,
```

在 `applyGait` 之后插入：
```js
  /* ---------- 被砸判定与倒地姿态（spec §7）---------- */
  /* 顾客不参与物理：判定由 customers 自己做，G.physics 只提供 looseBoxes() 只读列表。
     每帧代价 active(≤20) × loose(≤48) ≤ 960 次平方距离比较，无分配；loose 为空整段跳过。 */
  function findKnockBox(c, loose) {
    if (!loose || !loose.length || !c.dims) return null;
    var p = c.mesh.position;
    var r = 0.22 * c.dims.w + 0.13 + BOX_HIT_R;    // 最宽处是手臂：半展 0.22w + 0.125，取 0.13
    var topY = 1.77 * c.dims.h + BOX_HIT_R;        // 最高处是头发：1.70 + 最厚发型半高 0.065，×h
    for (var i = 0; i < loose.length; i++) {
      var b = loose[i];
      if (!b.rb || !b.mesh) continue;
      var v = b.rb.velocity;
      if (v.x * v.x + v.y * v.y + v.z * v.z < KNOCK_SPEED2) continue;
      var bp = b.mesh.position;
      var dx = bp.x - p.x, dz = bp.z - p.z;
      if (dx * dx + dz * dz > r * r) continue;
      if (bp.y < -BOX_HIT_R || bp.y > topY) continue;
      return b;
    }
    return null;
  }

  function stepKnockdown(c, dt, loose) {
    if (!c.ragdoll) {
      var hit = findKnockBox(c, loose);
      if (!hit) return;
      // 朝向约定：mesh.rotation.y = atan2(dx, dz) → 自身正前方是局部 +Z。
      // YXZ 下绕局部 +X 正转把 +Y 推向 +Z：+π/2 = 前扑（脸朝下），−π/2 = 后仰。
      var fx = Math.sin(c.mesh.rotation.y), fz = Math.cos(c.mesh.rotation.y);
      var v = hit.rb.velocity;
      c.ragdollDir = (v.x * fx + v.z * fz > 0) ? 1 : -1;   // 从背后砸来 → 前扑
      c.ragdoll = true;
      c.ragdollT = 0;
      return;      // 触发帧只置位，姿态从下一帧起推进
    }
    // 倒地期间再次被砸：不重播倒下动作，只把躺平计时拉回
    if (findKnockBox(c, loose)) c.ragdollT = Math.min(c.ragdollT, RAGDOLL_FALL);
    c.ragdollT += dt;
    var t = c.ragdollT, a;
    if (t <= RAGDOLL_FALL) a = t / RAGDOLL_FALL;
    else if (t <= RAGDOLL_HOLD) a = 1;
    else if (t < RAGDOLL_END) a = 1 - (t - RAGDOLL_HOLD) / (RAGDOLL_END - RAGDOLL_HOLD);
    else a = 0;
    c.mesh.rotation.x = c.ragdollDir * a * Math.PI / 2;
    if (t >= RAGDOLL_END) {
      c.ragdoll = false;
      c.ragdollT = 0;
      c.mesh.rotation.x = 0;
    }
  }

  /* 一名顾客的完整每帧流程。update() 的循环体与 _test.stepOne 同源同函数，
     不得在两处各写一份（否则自测测的与真实跑的会分叉）。 */
  function stepCustomer(c, dt, loose) {
    stepKnockdown(c, dt, loose);
    if (!c.ragdoll) {
      var hadPath = c.path.length > 0;
      stepMove(c, dt);
      applyGait(c, dt, hadPath);
    }
    stepState(c, dt);      // 无论倒地与否照常：patience/shopTime 继续累计，杜绝「砸倒顾客拖延耐心」
  }
```

`update(dt)` 的循环体（现 493-500）改为：
```js
    var loose = (G.physics && G.physics.looseBoxes) ? G.physics.looseBoxes() : EMPTY_LOOSE;
    for (var i = active.length - 1; i >= 0; i--) {
      var c = active[i];
      stepCustomer(c, dt, loose);
      if (c.removed) active.splice(i, 1);
    }
```
并在模块状态区（现 `var nextId = 1;` 之后）加：
```js
  var EMPTY_LOOSE = [];
```

`_test.spawnOne()` 返回的对象字面量里，`items: [], popItem: popItem };` 改为：
```js
          items: [], popItem: popItem,
          id: nextId++, path: [], lane: 0, avoid: 0, avoidApplied: 0, avoidSide: 0,
          ragdoll: false, ragdollT: 0, ragdollDir: 1 };
```

`_test` 对象末尾（`remove` 之后）加：
```js
      stepOne: function (c, dt) {
        stepCustomer(c, dt, (G.physics && G.physics.looseBoxes) ? G.physics.looseBoxes() : EMPTY_LOOSE);
      }
```

- [ ] **Step 4: `node --check js/customers.js js/main.js`，两态自跑自测**

**关键回归面**：`cust.parts` / `cust.height` / `cust.matShared` / `cust.rootY` / `cust.gaitAmp` / `cust.gaitDecay`（`rotation.order` 改动不得影响任何一条）、`customer.queued`、`checkout.*` 全组（#23 在真实排队顾客身上跑了 2.85s，`tx.phase` 与队列必须一字不变）、`world.cashierFigure` / `world.cashierFits`（站桩只设 `rotation.y`，YXZ 与 XYZ 等价）、`cust.handVisualCap` / `cust.handPopInvariant`。
本 task 新增 7 条，两态 +7（正常 155 / lowfx 150）。

- [ ] **Step 5: 提交推送**

```bash
git add js/customers.js js/main.js
git commit -m "feat(D-T4): 顾客被砸倒地，2.85s 原地爬起，状态机照常推进"
git -c credential.helper=wincred push origin main
```

---

## Task 5：局部绕行

**Files:**
- Modify: `js/customers.js`（新增 `stepAvoid` 与常量、`stepCustomer` 插入调用、`_test.stepAvoid`）
- Modify: `js/main.js`（`cust.dodgeLooseBox` / `cust.dodgeNoNavChange` 追加到 T4 的 `cust.knockdownPatienceRuns` 之后）

**Interfaces:**
- Consumes: T2 的 `G.physics.looseBoxes()`、T4 的 `stepCustomer(c, dt, loose)`
- Produces: `c.avoid` / `c.avoidApplied` / `c.avoidSide`（T4 已在两处对象字面量里初始化）、`G.customers._test.stepAvoid(c, dt)`

### ⚠ 本 task 对 spec §8 的一处**必要偏离**（实施者必须照本计划做，不要照 spec 做）

spec §8.1 要求把绕行偏移叠进 `c.lane`、由 `stepMove` 偏移**路点**。**实测该做法几乎不产生位移，等于没有绕行**：

> 复现（离线数值仿真，速度 1.6 m/s、`AVOID_STEP 0.60`、`AVOID_RANGE 1.2`、平滑 `dt×6`）：箱在正前方 1.0m、路点在 2.5–6.0m，**1 秒内顾客的实际侧向位移峰值只有 0.058–0.104 m**。原因有二：① 路点在数米外时，偏移 0.6m 只产生 `atan(0.6/4) ≈ 8.5°` 的转向角；② 顾客 0.7s 就走过了箱子，`avoid` 随即衰减、顾客又拐回来。spec §10 #25 要求的「1s 后 |Δ| ≥ 0.30m」在该设计下**不可能达成**。

**改为**：`c.avoid` 是**已施加到位置上的横向位移量**，每帧沿行进法向补差值（carrier 式）。侧移量即 `|c.avoid|`（上限 0.60m），立刻可见，箱走开后自动滑回。平滑系数由 `dt × 6` 改为 **`dt × 3`**（`dt×6` 的峰值横移速度 3.6 m/s 像瞬移；`dt×3` 峰值 1.8 m/s，与冲刺同量级，时间常数 0.33s）。

**`c.lane` 一字不动**（仍走路点偏移，仍只对中途路点生效）：`stepMove` 的第 302 行退化保护判据读 `c.lane` **保持原样**，因为 `avoid` 已不再进入路点。总偏移的 ±0.80m 钳制改为对 `c.avoid` 施加 `[−0.80 − c.lane, 0.80 − c.lane]`，等价保证 `|c.lane + c.avoid| ≤ 0.80`。

**另需迟滞 `c.avoidSide`**（spec 未提，但不加必然自激振荡）：侧移本身会改变箱在车道法向上的投影 `lat`。手算：顾客侧移 0.57m、路点在 6m 外时，`lat` 从 0 变到 −0.09，越过 `|lat| ≥ 0.05` 的判据后 `sign(lat)` 反号 → 让位方向翻转 → 顾客在箱两侧来回摆。**让位方向一旦选定就锁住，直到箱脱离探测窗口才清零。**

### 常量（spec §8.2，除平滑系数外逐字采用）

| 常量 | 值 | 依据 |
|---|---:|---|
| `AVOID_RANGE` | 1.2 m | 决策 5 |
| `AVOID_HALF_W` | 0.55 m | 顾客半径 ≤0.40 + 箱半宽 0.225 − 重叠容差 |
| `AVOID_STEP` | 0.60 m | 让开一个箱宽 + 半个身位 |
| 偏移总钳制 | ±0.80 m | 主通道净宽 2.4m → 半宽 1.2m − 顾客半径 0.40 |
| 平滑系数 | `dt × 3` | **偏离 spec 的 `dt × 6`**，理由见上 |

- [ ] **Step 1: 写失败断言（`js/main.js`，插在 T4 的 `ck('cust.knockdownPatienceRuns', ...)` 之后、`G.customers._test.remove(kc);` 之前）**

```js
      /* --- D-T5 局部绕行（spec §8；侧移改为 carrier 式，见计划 Task 5 的偏离说明）--- */
      var dc = G.customers._test.spawnOne();
      dc.mesh.position.set(6.0, 0, -6.0);
      dc.mesh.rotation.y = 0;                                  // 正前方 = +Z
      dc.lane = 0;
      dc.path = [new THREE.Vector3(6.0, 0, 0.0), new THREE.Vector3(6.0, 0, 6.0)];
      var dBox = G.world.spawnBox('f_noodle', { x: 6.0, z: -5.0 });
      var dLat = 0, dSum = 0;
      if (dBox) {
        for (var ddi = 0; ddi < 60; ddi++) {
          // 障碍始终保持在顾客正前方 1.0m（测的是绕行响应，不是「走过箱子」的时序）
          dBox.mesh.position.set(dc.mesh.position.x, 0.225, dc.mesh.position.z + 1.0);
          if (dBox.rb) { dBox.rb.position.set(dBox.mesh.position.x, 0.225, dBox.mesh.position.z); dBox.rb.wakeUp(); dBox.rb.velocity.set(0, 0, 0); }
          G.customers._test.stepAvoid(dc, 1 / 60);
        }
        dLat = Math.abs(dc.mesh.position.x - 6.0);
        dSum = Math.abs(dc.lane + dc.avoid);
      }
      ck('cust.dodgeLooseBox', dLat >= 0.30 && dSum <= 0.80,
        '正前方 1.0m 的 loose 箱：1s 后侧移 ' + round2(dLat) + 'm（应 ≥0.30），|lane+avoid| = ' +
        round2(dSum) + '（应 ≤0.80），avoid=' + round2(dc.avoid));
      if (dBox) G.world.destroyBox(dBox);

      var dNav0 = G.world.nav._nodes.length, dCol0 = G.world.colliders.length;
      var dPathA = G.world.nav.findPath(G.world.nav.entry, G.world.nav.aisleSpots[0]);
      var dBox2 = G.world.spawnBox('d_water', { x: 6.0, z: -6.5 });
      for (var dni = 0; dni < 30; dni++) G.customers._test.stepAvoid(dc, 1 / 60);
      var dPathB = G.world.nav.findPath(G.world.nav.entry, G.world.nav.aisleSpots[0]);
      var dSame = dPathA.length === dPathB.length;
      for (var dpi = 0; dpi < dPathA.length && dSame; dpi++) {
        if (dPathA[dpi].distanceTo(dPathB[dpi]) > 1e-9) dSame = false;
      }
      ck('cust.dodgeNoNavChange',
        G.world.colliders.length === dCol0 && G.world.nav._nodes.length === dNav0 && dSame,
        'collider ' + dCol0 + '→' + G.world.colliders.length + '，nav 节点 ' + dNav0 + '→' +
        G.world.nav._nodes.length + '，findPath 逐点相同=' + dSame);
      if (dBox2) G.world.destroyBox(dBox2);
      G.customers._test.remove(dc);
```

- [ ] **Step 2: 自跑自测确认 FAIL**

**期望的失败信息**：`selftest.exception`（`G.customers._test.stepAvoid is not a function`）。先临时把两条断言里的 `stepAvoid` 调用注释掉、只跑 `cust.dodgeLooseBox` 的度量，确认 detail 为 `1s 后侧移 0m（应 ≥0.30）`，再实现。

- [ ] **Step 3: `js/customers.js` 实现**

在 T4 的常量块之后加：
```js
  /* ---------- D-T5 局部绕行（spec §8）---------- */
  var AVOID_RANGE = 1.2;      // 前向探测距离
  var AVOID_HALF_W = 0.55;    // 侧向关心带半宽
  var AVOID_STEP = 0.60;      // 让开量
  var AVOID_MAX = 0.80;       // 偏移总钳制（主通道净宽 2.4m 半宽 1.2m − 顾客半径 0.40）
  var AVOID_SMOOTH = 3;       // 平滑系数（峰值横移 1.8 m/s；spec 的 6 会到 3.6 m/s，像瞬移）
```

在 `stepKnockdown` 之后、`stepCustomer` 之前插入：
```js
  /* 纯观感的横向让位：箱不进 G.world.colliders、不重建导航图，绕行失败时顾客直接穿过箱子。
     c.avoid 是【已施加到位置上的横向位移量】，每帧沿行进法向补差值——
     spec 原案（把偏移叠进 c.lane、偏移数米外的路点）实测 1s 只挪 0.06–0.10m，等于没绕行。 */
  function stepAvoid(c, dt, loose) {
    var p = c.mesh.position;
    var fx, fz;
    if (c.path.length) {
      fx = c.path[0].x - p.x;
      fz = c.path[0].z - p.z;
      var fl = Math.sqrt(fx * fx + fz * fz);
      if (fl < 1e-4) { fx = Math.sin(c.mesh.rotation.y); fz = Math.cos(c.mesh.rotation.y); }
      else { fx /= fl; fz /= fl; }
    } else {
      fx = Math.sin(c.mesh.rotation.y);
      fz = Math.cos(c.mesh.rotation.y);
    }
    /* 车道法向：与 stepMove 里 c.lane 用的 (-ndz/nl, ndx/nl) 同一条 */
    var nx = -fz, nz = fx;
    var target = 0;
    if (c.path.length > 1 && loose && loose.length) {
      var bestD = Infinity, bestLat = 0;
      for (var i = 0; i < loose.length; i++) {
        var b = loose[i];
        if (!b.mesh) continue;
        var dx = b.mesh.position.x - p.x, dz = b.mesh.position.z - p.z;
        var fp = dx * fx + dz * fz;
        if (!(fp > 0) || fp > AVOID_RANGE) continue;
        var lat = dx * nx + dz * nz;                  // 在车道法向上的投影
        if (Math.abs(lat) >= AVOID_HALF_W) continue;
        if (fp < bestD) { bestD = fp; bestLat = lat; }
      }
      if (bestD < Infinity) {
        /* 让位方向一旦选定就锁住，直到箱脱离探测窗口（迟滞）。
           不锁会自激振荡：侧移本身会改变箱相对车道法向的投影，lat 越过 0 后 sgn 反号，
           顾客会在箱两侧来回摆。箱正对准（|lat| < 0.05）时按 id 奇偶定侧，保证确定性。 */
        if (!c.avoidSide) {
          c.avoidSide = (Math.abs(bestLat) < 0.05) ? ((c.id % 2) ? 1 : -1) : (bestLat > 0 ? 1 : -1);
        }
        target = -c.avoidSide * AVOID_STEP;
      } else {
        c.avoidSide = 0;
      }
    } else {
      c.avoidSide = 0;
    }
    c.avoid += (target - c.avoid) * Math.min(1, dt * AVOID_SMOOTH);
    var lo = -AVOID_MAX - c.lane, hi = AVOID_MAX - c.lane;   // 保证 |lane + avoid| ≤ 0.80
    if (c.avoid < lo) c.avoid = lo;
    if (c.avoid > hi) c.avoid = hi;
    var da = c.avoid - c.avoidApplied;
    if (da) {
      p.x += nx * da;
      p.z += nz * da;
      c.avoidApplied = c.avoid;
    }
  }
```

`stepCustomer` 的非倒地分支改为：
```js
  function stepCustomer(c, dt, loose) {
    stepKnockdown(c, dt, loose);
    if (!c.ragdoll) {
      var hadPath = c.path.length > 0;
      stepAvoid(c, dt, loose);
      stepMove(c, dt);
      applyGait(c, dt, hadPath);
    }
    stepState(c, dt);
  }
```

`_test` 加一条（放在 `stepOne` 之后）：
```js
      stepAvoid: function (c, dt) {
        stepAvoid(c, dt, (G.physics && G.physics.looseBoxes) ? G.physics.looseBoxes() : EMPTY_LOOSE);
      }
```

- [ ] **Step 4: `node --check js/customers.js js/main.js`，两态自跑自测**

**关键回归面**：`customer.queued`（绕行会让顾客沿途微微侧移，到点判据不得被破坏）、`checkout.*` 全组、`cust.*` 全组、`nav.*` 全组（`colliders` 与 `nav._nodes` 一字不改）。
本 task 新增 2 条，两态 +2（正常 157 / lowfx 152）。
**报告里必须写**：`cust.dodgeLooseBox` 实测的侧移米数与 `avoid` 终值。

- [ ] **Step 5: 提交推送**

```bash
git add js/customers.js js/main.js
git commit -m "feat(D-T5): 散落箱局部绕行（横向让位，不动导航图与 colliders）"
git -c credential.helper=wincred push origin main
```

---

## Task 6：存档 `ry` 字段

**Files:**
- Modify: `js/world.js:1262-1281`（`serializeBoxes`）、`js/world.js:1285-1314`（`restoreBoxes`）
- Modify: `js/main.js`（`save.*` 新断言追加到 `ck('save.boxesNoDup', ...)` + T2 的 `physics.noOrphanBodies` 之后）

**Interfaces:**
- Consumes: T2 的 `G.physics.attach/detach`
- Produces: 存档记录新增可选字段 `ry`（number，弧度）

### 序列化规则（spec §9.3，一条规则同时解决「朝向」与「飞行中」）

> 所有箱一律按**落地平放**口径存档：`y` 恒为 0.225（不入档，读档时写死）、姿态**只取偏航**。

- `ry = new THREE.Euler().setFromQuaternion(box.mesh.quaternion, 'YXZ').y`
- 俯仰与翻滚**丢弃**；飞行中的箱不需要任何特判，天然走同一条规则
- **读档**：`spawnBox` 落位后设 `mesh.rotation.set(0, ry || 0, 0)`，然后 `detach` + `attach` + **立即 `rb.sleep()`**（读完档满地箱不会同时醒着抢一帧）
- **降级**：旧档无 `ry` → 视为 0。**不需要版本号变更，`v: 2` 不动。**

> **注意实现顺序**：`spawnBox` 内部已经 `attach` 过一次（T2），此时 mesh 还是零旋转。必须**先**改 `mesh.rotation`、**再** `detach` + `attach`，否则刚体的姿态与 mesh 不一致。`state.js` 本 task **零改动**（`boxes` 字段整体由 `world.serializeBoxes()` 产出）。

- [ ] **Step 1: 写失败断言（`js/main.js`，插在 T2 的 `ck('physics.noOrphanBodies', ...)` 之后）**

```js
      /* --- D-T6 存档朝向字段 ry（spec §9.3）--- */
      var oriBox = G.world.spawnBox('f_noodle', { x: 7.5, z: 3.5 });
      var oriOk = false, oriDetail = '无箱';
      if (oriBox && oriBox.rb) {
        oriBox.mesh.rotation.set(0, 1.1, 0);
        G.physics.detach(oriBox); G.physics.attach(oriBox); oriBox.rb.sleep();
        var oriY0 = new THREE.Euler().setFromQuaternion(oriBox.mesh.quaternion, 'YXZ').y;
        G.save();
        G.load();
        var oriAfter = null, oriList = G.world.allBoxes();
        for (var obi2 = 0; obi2 < oriList.length; obi2++) {
          if (Math.abs(oriList[obi2].mesh.position.x - 7.5) < 0.05 &&
              Math.abs(oriList[obi2].mesh.position.z - 3.5) < 0.05) { oriAfter = oriList[obi2]; break; }
        }
        if (oriAfter) {
          oriOk = Math.abs(oriAfter.mesh.rotation.y - oriY0) < 0.01 &&
            Math.abs(oriAfter.mesh.position.y - 0.225) < 1e-6 &&
            oriAfter.mesh.rotation.x === 0 && oriAfter.mesh.rotation.z === 0;
          oriDetail = 'ry ' + round2(oriY0) + '→' + round2(oriAfter.mesh.rotation.y) +
            '，y=' + oriAfter.mesh.position.y + '，rx=' + oriAfter.mesh.rotation.x +
            '，rz=' + oriAfter.mesh.rotation.z;
        } else { oriDetail = '读档后找不到该箱'; }
      }
      ck('save.boxOrientation', oriOk, '扔出的箱只存偏航、读档一律平放：' + oriDetail);

      var flyBox = G.world.spawnBox('d_water', { x: 8.5, z: 3.5 });
      var flyOk = false, flyDetail = '无箱';
      if (flyBox && flyBox.rb) {
        flyBox.mesh.position.set(8.5, 1.6, 3.5);
        flyBox.rb.position.set(8.5, 1.6, 3.5);
        flyBox.rb.wakeUp();
        flyBox.rb.velocity.set(0, 3, 0);
        var flyAirborne = flyBox.mesh.position.y > 1;
        G.save();
        G.load();
        var flyAfter = null, flyList = G.world.allBoxes();
        for (var fbi = 0; fbi < flyList.length; fbi++) {
          if (Math.abs(flyList[fbi].mesh.position.x - 8.5) < 0.05 &&
              Math.abs(flyList[fbi].mesh.position.z - 3.5) < 0.05) { flyAfter = flyList[fbi]; break; }
        }
        if (flyAfter && flyAfter.rb) {
          flyOk = flyAirborne && Math.abs(flyAfter.mesh.position.y - 0.225) < 1e-6 &&
            flyAfter.rb.sleepState === CANNON.Body.SLEEPING;
          flyDetail = '存档前 y=1.6（空中=' + flyAirborne + '），读档后 y=' + flyAfter.mesh.position.y +
            '，sleepState=' + flyAfter.rb.sleepState + '（SLEEPING=' + CANNON.Body.SLEEPING + '）';
        } else { flyDetail = '读档后找不到该箱或无刚体'; }
      }
      ck('save.inFlightBoxLands', flyOk, '空中的箱读档后静止落地：' + flyDetail);

      var legacyRaw = JSON.parse(localStorage.getItem('gss-save-v2'));
      legacyRaw.boxes = [{ pid: 'f_noodle', left: 4, where: 'floor', x: 9.5, z: 3.5 }];   // 无 ry 的旧档
      localStorage.setItem('gss-save-v2', JSON.stringify(legacyRaw));
      var legacyLoaded = false, legacyBox = null, legacyThrew = false;
      try {
        legacyLoaded = G.load();
        var legList = G.world.allBoxes();
        for (var lgi = 0; lgi < legList.length; lgi++) {
          if (Math.abs(legList[lgi].mesh.position.x - 9.5) < 0.05) { legacyBox = legList[lgi]; break; }
        }
      } catch (lge) { legacyThrew = true; }
      ck('save.boxOrientationLegacy',
        legacyLoaded === true && !legacyThrew && !!legacyBox && legacyBox.mesh.rotation.y === 0,
        '无 ry 字段的旧档：load=' + legacyLoaded + '，抛异常=' + legacyThrew +
        '，rotation.y=' + (legacyBox ? legacyBox.mesh.rotation.y : 'n/a'));
```

> 这三条都写 `localStorage`，**必须落在主 `try` 之内**（上面的插入点已经在主 `try` 里，`finally` 的 `restoreSaveKeys()` 会还原玩家真实存档）。

- [ ] **Step 2: 自跑自测确认 FAIL**

**期望的失败信息**：`save.boxOrientation` detail 形如 `ry 1.1→0，y=0.225，rx=0，rz=0`（偏航丢失）；`save.inFlightBoxLands` detail 形如 `...读档后 y=0.225，sleepState=0（SLEEPING=2）`（未 sleep）；`save.boxOrientationLegacy` 可能已绿（旧行为就是 rotation.y=0），**这条允许起手即绿**，实现后必须仍绿。

- [ ] **Step 3: `js/world.js` 实现**

`serializeBoxes`（现 1262-1281）改为：
```js
  function serializeBoxes() {
    var out = [], list = allBoxes();
    var held = (G.player && G.player.carrying) || null;
    var pose = (held && G.player.getPose) ? G.player.getPose() : null;
    var euler = new THREE.Euler();
    for (var i = 0; i < list.length; i++) {
      var box = list[i], slot = storageSlotOf(box);
      /* 所有箱一律按「落地平放」口径存档：y 恒 0.225 不入档、姿态只取偏航。
         俯仰与翻滚丢弃 → 任何存档状态都对应一个合法静止姿态，读档必不穿地、
         不需要额外字段，与 putDownBox / storeBox 现有的 rotation.set(0, yaw, 0) 同口径。
         飞行中的箱天然走同一条规则，不需要任何特判。 */
      var ry = euler.setFromQuaternion(box.mesh.quaternion, 'YXZ').y;
      if (slot) {
        out.push({ pid: box.productId, left: box.itemsLeft, where: 'storage', slotId: slot.id, ry: ry });
        continue;
      }
      var x = box.mesh.position.x, z = box.mesh.position.z;
      var where = isOnYard(box.mesh) ? 'yard' : 'floor';
      if (box === held) {
        where = 'floor';
        if (pose) { x = pose.x; z = pose.z; }
      }
      out.push({ pid: box.productId, left: box.itemsLeft, where: where, x: x, z: z, ry: ry });
    }
    return out;
  }
```

`restoreBoxes`（现 1285-1314）的非 storage 分支（结尾四行）改为：
```js
      var at = { x: (typeof rec.x === 'number') ? rec.x : 0, z: (typeof rec.z === 'number') ? rec.z : 0 };
      box = spawnBox(rec.pid, at);
      if (!box) continue;
      box.itemsLeft = left;
      updateBoxVisual(box);
      // 旧档无 ry → 0（与现状完全一致）；先摆姿态再重建刚体，否则刚体姿态与 mesh 不一致
      box.mesh.rotation.set(0, (typeof rec.ry === 'number' && isFinite(rec.ry)) ? rec.ry : 0, 0);
      if (G.physics) {
        G.physics.detach(box);
        G.physics.attach(box);
        if (box.rb) box.rb.sleep();   // 读完档满地箱不会同时醒着抢一帧
      }
      registerBoxInteractable(box);
```

（storage 分支不动：`storeBox` 自己会 `rotation.set(0, 0, 0)` 并 `attachStatic`，slotted 箱的 `ry` 恒为 0。）

- [ ] **Step 4: `node --check js/world.js js/main.js`，两态自跑自测**

**关键回归面**：`save.boxesRoundtrip` / `save.boxesNoDup` / `physics.noOrphanBodies`（T2 新增，读档不得留孤儿刚体）、`world.storageRestore`、`save.v2Shape`、`save.corruptFallback`、`save.migrateV1`、`world.boxHardCap`。
本 task 新增 3 条，两态 +3（正常 160 / lowfx 155）。

- [ ] **Step 5: 提交推送**

```bash
git add js/world.js js/main.js
git commit -m "feat(D-T6): 存档 ry 偏航字段，读档一律落地平放并 sleep"
git -c credential.helper=wincred push origin main
```

---

## Task 7：断言收口、性能闸门扩场景、文档校准

**Files:**
- Modify: `js/main.js`（`perf` 段：闸门场景加 12 只散落地面箱；`world.boxHardCap` 之后加 `physics.bodyCountCeiling` 与 `perf.physicsStep`）
- Modify: `DESIGN.md` §5.8（把 T0 的预写值换成实测值）
- Modify: `docs/superpowers/specs/2026-08-06-物理与投掷-design.md`（§6.3 数值表按实调回写；§10 清单打勾）
- Modify: `docs/CONTRACTS.md`（如实现过程中接口有偏差则校准）

**Interfaces:**
- Consumes: T1-T6 全部
- Produces: 无新接口；产出**结项报告**

- [ ] **Step 1: 写失败断言（`js/main.js`）**

在 `var drawN = countDrawables(scene);`（现 `js/main.js:1485`）**之前**插入：

```js
      /* D-T7：闸门场景纳入散落地面箱（BOX_HARD_CAP 48 − 仓库 24 − 卸货 12 = 最多 12 只，
         每只 2 mesh = +24 drawable）。测完立刻销毁，让后面的 world.boxHardCap 场景不受影响 */
      var gateStray = [];
      for (var gsi = 0; gsi < 12; gsi++) {
        var gsb = G.world.spawnBox(G.data.PRODUCTS[gsi % G.data.PRODUCTS.length].id,
          { x: 6 + (gsi % 4) * 0.7, z: -1.0 - Math.floor(gsi / 4) * 0.7 });
        if (gsb) gateStray.push(gsb);
      }
```

在 `ck('perf.mainPassDrawables', ...)` 那一段的 detail 字符串末尾追加散落箱数（把 `'（每客塞 12 件手持，实渲染 ' + handRendered + '）');` 改为）：
```js
        G.customers.active.length + ' 名（每客塞 12 件手持，实渲染 ' + handRendered +
        '）、散落地面箱 ' + gateStray.length + ' 只');
      for (var gsj = 0; gsj < gateStray.length; gsj++) G.world.destroyBox(gateStray[gsj]);
```

在 `ck('world.boxHardCap', ...)` 那一段**之后**、`for (var cbj = 0; ...) G.world.destroyBox(capBoxes[cbj]);` **之前**插入：

```js
      /* D-T7：满配体量上界与步进耗时。此刻场上 = 全开静态 46（44 collider + 2 plane）+ 箱 48 */
      ck('physics.bodyCountCeiling', G.physics._test.bodyCount() <= 97,
        '满配灌店 body 总数 ' + G.physics._test.bodyCount() + '（静态 ' +
        G.physics._test.staticCount() + ' + 箱 ' + G.world.allBoxes().length + '），天花板 97');
      var wakeList = G.physics.looseBoxes(), wi;
      for (wi = 0; wi < wakeList.length; wi++) { wakeList[wi].rb.allowSleep = false; wakeList[wi].rb.wakeUp(); }
      var stepTimes = [];
      for (wi = 0; wi < 60; wi++) {
        var st0 = (window.performance && performance.now) ? performance.now() : Date.now();
        G.physics._test.stepOnce();
        stepTimes.push(((window.performance && performance.now) ? performance.now() : Date.now()) - st0);
      }
      stepTimes.sort(function (a, b) { return a - b; });
      var p95 = stepTimes[56];
      var wakeBack = G.physics.looseBoxes();
      for (wi = 0; wi < wakeBack.length; wi++) wakeBack[wi].rb.allowSleep = true;
      ck('perf.physicsStep', p95 <= 3.5,
        '满配 ' + G.physics._test.bodyCount() + ' 体（' + wakeList.length + ' 只箱强制不休眠）连续 60 步：p50 ' +
        round2(stepTimes[30]) + 'ms / p95 ' + round2(p95) + 'ms / max ' + round2(stepTimes[59]) +
        'ms，预算 3.5ms');
```

- [ ] **Step 2: 自跑自测确认 FAIL / 记录基线**

`physics.bodyCountCeiling` 与 `perf.physicsStep` 起手很可能就绿（它们是**天花板**断言，不是行为断言）——**这两条允许起手即绿**，但必须**记录实测值写进报告**（离线基准：97 体 60 步 p50 0.80ms / p95 1.60ms / max 2.00ms）。
`perf.mainPassDrawables` 起手会从 702 变成 **726**，仍 < 760 → 应保持绿；若变红说明散落箱多算了，先查 `gateStray.length`。
`perf.renderCalls` 会从 939 上升约 48（+24 主 pass、+24 阴影 pass）→ 约 987，天花板 1050 → 应保持绿。**若它红了，先把它的实测值写进报告再决定处置**（不得直接抬天花板）。

- [ ] **Step 3: 两态跑通、记录全部实测数字**

跑正常态与 lowfx 态，记录：
- 断言总数（正常 / lowfx，预期 162 / 157）
- `perf.mainPassDrawables` 的 drawN
- `perf.renderCalls` 的 rCalls
- `perf.physicsStep` 的 p50 / p95 / max
- `physics.bodyCountCeiling` 的 body 总数
- `physics.ceilingCaps` 的两个 aabb 峰值
- `physics.noTunneling` 的 wallZ / dropY
- `cust.dodgeLooseBox` 的侧移米数

- [ ] **Step 4: 【本 task 的核心一步】逐条对照 spec §10 的 31 条验收断言清单打勾**

把下表填完，**每一条要么指向一个真实存在且为绿的 `ck()` 名，要么显式裁定「补」或「免」并写明理由**。C 期就是因为跳过这一步，5 条断言缺失或空洞一直藏到终审才暴露。

| # | spec 断言名 | 实际 `ck()` 名 | 状态 | 备注 |
|---:|---|---|---|---|
| 1 | `physics.engineLoaded` | `physics.engineLoaded` | | |
| 2 | `physics.staticsMatchColliders`（开区前后各一次） | `physics.staticsMatchColliders` + `physics.staticsAfterZoneOpen` | | 拆成两条（同名两次无法区分） |
| 3 | `physics.colliderHeights` | `physics.colliderHeights` | | |
| 4 | `physics.ceilingCaps` | `physics.ceilingCaps` | | 必须确认断言里调了 `computeAABB()` |
| 5 | `physics.noTunneling` | `physics.noTunneling` | | |
| 6 | `physics.boxStates` | `physics.boxStates` | | |
| 7 | `physics.slottedIsStatic` | `physics.slottedIsStatic` | | |
| 8 | `physics.destroyRemovesBody` | `physics.destroyRemovesBody` | | |
| 9 | `physics.noOrphanBodies` | `physics.noOrphanBodies` | | |
| 10 | `physics.meshFollowsBody` | `physics.meshFollowsBody` | | |
| 11 | `physics.bodyCountCeiling` | `physics.bodyCountCeiling` | | 由 T2 移到 T7（满配场景只在闸门段存在） |
| 12 | `physics.noInterpolatedQuaternion` | `physics.noInterpolatedQuaternion` | | 口径 = `_test.src()`（模块全部函数 toString） |
| 13 | `player.chargeCurve` | `player.chargeCurve` | | |
| 14 | `player.throwOrigin` | `player.throwOrigin` | | |
| 15 | `player.chargeArbitration` | `player.chargeArbitration` | | |
| 16 | `ui.chargeBar` | `ui.chargeBar` | | |
| 17 | `player.contextMenuSuppressed` | `player.contextMenuSuppressed` | | |
| 18 | `cust.knockdown` | `cust.knockdown` | | |
| 19 | `cust.slowBoxNoKnockdown` | `cust.slowBoxNoKnockdown` | | |
| 20 | `cust.knockdownRecovers` | `cust.knockdownRecovers` | | |
| 21 | `cust.ragdollFreezesGait` | `cust.ragdollFreezesGait` | | |
| 22 | `cust.knockdownKeepsHands` | `cust.knockdownKeepsHands` | | |
| 23 | `cust.knockdownNoConsequence` | `cust.knockdownNoConsequence` | | 挂在真实排队顾客上（`tx.phase` 非空才算非空洞） |
| 24 | `cust.knockdownPatienceRuns` | `cust.knockdownPatienceRuns` | | |
| 25 | `cust.dodgeLooseBox` | `cust.dodgeLooseBox` | | **测量方法与 spec 不同**：障碍保持在正前方 1.0m；spec 原案 1s 只挪 0.06–0.10m |
| 26 | `cust.dodgeNoNavChange` | `cust.dodgeNoNavChange` | | |
| 27 | `save.boxOrientation` | `save.boxOrientation` | | |
| 28 | `save.inFlightBoxLands` | `save.inFlightBoxLands` | | |
| 29 | `save.boxOrientationLegacy` | `save.boxOrientationLegacy` | | |
| 30 | `perf.physicsStep` | `perf.physicsStep` | | |
| 31 | `perf.mainPassDrawables`（扩场景） | `perf.mainPassDrawables` | | 确认 detail 里写了「散落地面箱 12 只」 |

- [ ] **Step 5: `DESIGN.md` §5.8 实测回写**

把 T0 预写的 726 / 余量 34 换成 Step 3 实测的 drawN 与 `760 − drawN`；把「阴影深度 pass 237、一帧总提交 939」那一行改为 D 期实测值；`perf.renderCalls` 的实测值一并回写。

- [ ] **Step 6: spec §6.3 数值表回写**

若 T3 实施过程中按手感调整了 `CHARGE_FULL` / `THROW_V_MIN` / `THROW_V_MAX` / `THROW_SPIN_*` 中的任何一个，**逐项回写 spec §6.3 表**并同步 `js/player.js` 的注释。若一字未调，在报告里明写「数值表零调整」。
若 `THROW_V_MAX` 被调高，**必须重跑 `physics.noTunneling` 并重算 spec §6.3 末尾那段推导**。

- [ ] **Step 7: spec §10 清单打勾落盘**

把 Step 4 的表填好后，同步把 spec 文件 §10 里 31 条的 `- [ ]` 改成 `- [x]`（或对「免」的条目在行末补 `——【免】理由`）。

- [ ] **Step 8: 真机验收清单（跑一次真实浏览器，逐条记录观感，写进报告）**

- [ ] 蓄力手感：0.8s 到满是否偏慢/偏快；力度条是否够醒目
- [ ] 满力投掷的射程与翻滚观感
- [ ] 把箱扔上货架顶 / 收银台 / 传送带是否都能稳住
- [ ] 砸倒排队顾客后柜台恢复是否无异常
- [ ] 走廊撒一地箱，顾客绕行观感（侧移是否自然、有没有「瞬移」感）
- [ ] 20 人满场 + 48 箱散落的帧率
- [ ] 右键蓄力时浏览器右键菜单确实不弹

- [ ] **Step 9: 提交推送**

```bash
git add js/main.js DESIGN.md docs/superpowers/specs/2026-08-06-物理与投掷-design.md docs/CONTRACTS.md
git commit -m "feat(D-T7): 断言收口、闸门纳入散落箱、文档与 spec 清单校准"
git -c credential.helper=wincred push origin main
```

---

## 附录 A：本计划相对 spec 的四处偏离（实施者以本计划为准）

| # | spec 原文 | 本计划 | 理由 |
|---:|---|---|---|
| 1 | §8.1 绕行叠进 `c.lane`、偏移路点；§10 #25「1s 后 \|Δ\| ≥ 0.30m」 | `c.avoid` 改为**已施加到位置上的横向位移量**（carrier 式），平滑系数 `dt×6` → `dt×3`，新增 `c.avoidSide` 迟滞；`c.lane` 与 `stepMove` 第 302 行的退化保护判据不动 | 离线数值仿真实测：spec 原案 1 秒内实际侧移只有 **0.058–0.104 m**（路点在数米外时偏移 0.6m 只有 ~8.5° 转向角，且顾客 0.7s 就走过箱子），绕行等于没做，#25 的阈值不可能达成。迟滞是 spec 未提但必需的：不加则 `sign(lat)` 会因自身侧移而反号，顾客左右自激摆动 |
| 2 | §5.3「`spawnBox` 尾部：`atPos` 是存储位 → 由 `storeBox` 接手；否则 `attach`」 | `spawnBox` **一律 `attach`**，落到存储位的箱由随后的 `storeBox` 覆盖成 `attachStatic` | `spawnBox` 无从可靠判断一个坐标是不是存储位；加判断只会多一条会漂移的第二判据。覆盖是幂等的，成本是一次 add/remove |
| 3 | §10 #2「开区前后各测一次」（单条 `physics.staticsMatchColliders`） | 拆成 `physics.staticsMatchColliders`（boot 态）+ `physics.staticsAfterZoneOpen`（全开态） | 同名 `ck()` 出现两次时无法从报告里区分是哪一次红；且自测的全开时机固定在 `js/main.js:1294-1297`，就地各测一次最简单 |
| 4 | §13 把 #11 `physics.bodyCountCeiling` 划给 D-T2 | 移到 D-T7 | 满配灌店场景只在 `js/main.js` 的闸门段存在；放在 T2 的 `physics.*` 分组处只能测到 boot 态的 30 余个 body，是空洞断言（正是 C 期栽的跟头） |

## 附录 B：三条实施者最容易踩的静默陷阱

1. **`body.aabb` 不会被 `world.step` 更新。** cannon 0.6.2 的 `NaiveBroadphase` 默认 `useBoundingBoxes = false`，走的是包围球路径，`aabb` 全程停在初始值。`physics.ceilingCaps` 若不显式调 `body.computeAABB()`，会一直读到 `0.225 ≤ 3.6` 而**永远绿**。（另注：方法名是 `computeAABB`，**没有** `updateAABB`。）
2. **`world.step(dt)` 单参数形式不写 `interpolatedPosition`。** 实测单参调用后 `position.y = 0.0029` 而 `interpolatedPosition.y = 0.0000`。必须用三参形式 `world.step(1/60, dt, 10)`，否则所有箱的 mesh 会被写回原点。
3. **醒着的动态体 `interpolatedQuaternion` 恒为单位四元数**（实测 `quaternion.y = 0.0033` / `interpolatedQuaternion = (0,0,0,1)`）。**睡着的体会被 copy**，所以「静止时看着对、一扔就不转」——这正是它难以归因的原因。
