# 收银员站桩美术升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收银员站桩换用 B 期顾客 9 件三色构造，消除收银台同框的美术断层。

**Architecture:** `customers.js` 的 `buildBody` 参数化（省略项走随机，`spawn()` 零变化）并薄导出 `G.customers.buildFigure(opts)`；`world.js` 的 `buildCashierFigure` 改为消费该接口摆位。契约先行同步。

**Tech Stack:** Three.js r128 UMD、ES5、IIFE + window.G。

**依据文档：** `docs/superpowers/specs/2026-08-02-收银员美术升级-design.md`

## Global Constraints

- ES5、无构建、零网络请求；push 用 `git -c credential.helper=wincred push origin main`；禁 `git add .`
- 制服色恒 `#4C9BE8`（CONTRACTS 固定值）；`setCashierVisible` 幂等语义不变
- 实施者自跑无头自测：`& "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --no-sandbox --user-data-dir="$env:TEMP\edge-cashier" --dump-dom --virtual-time-budget=20000 "file:///C:/Users/quito/超市模拟器/index.html?selftest=1"`
- 基线：HEAD `5327a2a`，自测正常态 82 / lowfx 77 全绿

---

### Task 1: 收银员换装（唯一 task）

**Files:**
- Modify: `js/customers.js`（buildBody :166 参数化；导出对象 :483 加 buildFigure）
- Modify: `js/world.js`（buildCashierFigure :455 重写）
- Modify: `js/main.js`（B-T6 cust 断言块之后插新断言）
- Modify: `docs/CONTRACTS.md`（G.customers 段 + 数字同步）、`DESIGN.md`（§5.7 尾 + §5.8 数字同步）
- 顺带提交：`docs/superpowers/specs/2026-08-02-收银员美术升级-design.md`（已修正的发型措辞行，工作区未提交）

**Interfaces:**
- Produces: `G.customers.buildFigure(opts)` → `{group, hands, shirtMat, torso, head, hair, legL, legR, armL, armR, dims, baseY}`；`opts = {shirt?, pants?, skin?, hair?, hairStyle?, h?, w?}` 省略项随机

- [ ] **Step 1: 写失败断言（main.js，B-T6 `ck('cust.gaitDecay', ...)` 块的 `ct.remove(gc);` 之后插入）**

```js
      /* --- 收银员站桩美术（小任务）--- */
      function findCashierGroup() {
        var found = null, n = 0;
        scene.traverse(function (o) {
          if (o.isGroup && o.position.x === -7.75 && o.position.z === 0) { found = o; n++; }
        });
        return { group: found, n: n };
      }
      G.world.setCashierVisible(true);
      var cf = findCashierGroup();
      var cfMeshes = 0, cfShirtOk = false;
      if (cf.group) cf.group.traverse(function (o) {
        if (!o.isMesh) return;
        cfMeshes++;
        if (o.material && o.material.color && o.material.color.getHex() === 0x4C9BE8) cfShirtOk = true;
      });
      ck('world.cashierFigure', cf.n === 1 && cfMeshes === 9 && cfShirtOk,
        '站桩组 ' + cf.n + ' 个，mesh ' + cfMeshes + '（应 9），制服色命中 ' + cfShirtOk);
      G.world.setCashierVisible(true);
      ck('world.cashierIdempotent', findCashierGroup().n === 1, '重复 setVisible(true) 不得复制');
      G.world.setCashierVisible(false);
      ck('world.cashierRemoved', findCashierGroup().n === 0, 'setVisible(false) 应移除');
```

（定位靠唯一坐标 `(-7.75, 0)`——不可按色搜，顾客衬衫板含同色。此时门店未开、无真实顾客，且 cust 测试体已回收。）

- [ ] **Step 2: 自跑自测确认 FAIL**

期望 `SELFTEST:FAIL`：`world.cashierFigure` 因 `cfMeshes === 4` 而 false（旧 4 件构造）；另两条应绿（幂等/移除是既有行为）。

- [ ] **Step 3: customers.js — buildBody 参数化**

`buildBody()` 签名与前 6 行、`h/w`、`hair` 行改为（其余函数体一字不动）：

```js
  function buildBody(opts) {
    opts = opts || {};
    var shirtMat = matFor(bodyMats, opts.shirt || SHIRTS[G.randInt(0, SHIRTS.length - 1)]);
    var pantsMat = matFor(bodyMats, opts.pants || PANTS[G.randInt(0, PANTS.length - 1)]);
    var skinMat = matFor(bodyMats, opts.skin || SKINS[G.randInt(0, SKINS.length - 1)]);
    var hairMat = matFor(bodyMats, opts.hair || HAIRS[G.randInt(0, HAIRS.length - 1)]);
    var shoeMat = matFor(bodyMats, SHOE_HEX);

    var g = new THREE.Group();
    var h = (opts.h > 0) ? opts.h : G.rand(0.90, 1.08);
    var w = (opts.w > 0) ? opts.w : G.rand(0.88, 1.22);
```

`hair` 创建行改为：

```js
    var hair = new THREE.Mesh(
      HAIR_GEOS[(opts.hairStyle >= 0 && opts.hairStyle < HAIR_GEOS.length) ? opts.hairStyle : G.randInt(0, HAIR_GEOS.length - 1)],
      hairMat);
```

函数上方注释补一句：`参数化（收银员小任务）：opts 各项省略走随机；spawn()/_test 无参调用行为不变。`

导出对象（`init:` 行之后）新增：

```js
    buildFigure: function (opts) { return buildBody(opts); },
```

- [ ] **Step 4: world.js — buildCashierFigure 重写**

整函数（:455-475，含上方 5 行注释块）替换为：

```js
  // ---------------------------------------------------------------
  // 收银员站桩（GDD §9 / CONTRACTS.md setCashierVisible）
  // 复用顾客 9 件构造（G.customers.buildFigure），固定形象；站在收银台与西墙之间
  // 的 0.3m 空隙，旋转 90° 后身体进深占 x 轴（躯干 0.234 / 鞋 0.22 / 短块发 0.29 均 ≤0.3）。
  function buildCashierFigure() {
    var body = G.customers.buildFigure({
      shirt: '#4C9BE8',   // CONTRACTS：制服固定色
      pants: '#2E3742',
      skin: '#D9A878',
      hair: '#2A2118',
      hairStyle: 0,       // 短块：深度 0.29 ≤ 0.3m 空隙（后长 0.34/平顶 0.31 超限）
      h: 1.0, w: 1.0
    });
    var group = body.group;
    group.position.set(-7.75, 0, 0);
    group.rotation.y = Math.PI / 2;   // 面朝 +x：传送带 / 排队方向
    return group;
  }
```

（旧 uniformMat/skinMat/legMat 与 4 件构造随函数体一并消失，无外部引用——grep 确认。脚本顺序 customers.js 先于 world.js？**否**——world.js 先加载，但 `buildCashierFigure` 只在 `setCashierVisible(true)` 时惰性调用，届时 `G.customers` 必已就绪，无加载序问题。）

- [ ] **Step 5: 契约与 DESIGN 同步（含闸门数字连锁）**

CONTRACTS.md：
- G.customers 段（`G.customers.update...` 代码块内）加一行：

```js
G.customers.buildFigure(opts /*{shirt?,pants?,skin?,hair?,hairStyle?,h?,w?} 省略项随机*/) // -> 顾客构造体（收银员站桩复用）
```

- `:182` setCashierVisible 条款「体型同顾客规格」后补「（自收银员美术升级起字面为真：经 `G.customers.buildFigure` 构造）」。

DESIGN.md：
- §5.7 尾加：「**收银员站桩**：`buildFigure` 固定参数——衣 `#4C9BE8`（契约）/裤 `#2E3742`/肤 `#D9A878`/发 `#2A2118` 短块/h=w=1.0；无步态（站桩）。」
- §5.8 数字连锁更新：分项「收银员 4」→「收银员 9」，合计 ≈497→**≈502**；闸门句「满场闸门实测 502」→「≈507」（天花板 550 不动，余量 ≈8%）。

main.js：`perf.drawCallCeiling` 的 detail 字串同步（「实测 502」→「≈507」、「干净启动满场为 497」→「502」）。

- [ ] **Step 6: node --check 三 js + 两态自测**

正常态期望 **85/85**（82+3 新断言，`perf.drawCallCeiling` detail 报 ~507）；lowfx 期望 **80/80**（77+3）。任何回归（尤其 `cust.*` 六条——buildBody 参数化不得改变随机路径行为）即停并报告。

- [ ] **Step 7: 提交推送**

```bash
git add js/customers.js js/world.js js/main.js docs/CONTRACTS.md DESIGN.md "docs/superpowers/specs/2026-08-02-收银员美术升级-design.md"
git commit -m "feat: 收银员站桩换用顾客九件构造，同框美术统一

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git -c credential.helper=wincred push origin main
```

推完 `git log origin/main -1` 核实。
